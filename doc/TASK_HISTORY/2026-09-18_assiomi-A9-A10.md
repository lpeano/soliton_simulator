# TASK HISTORY — **completare `doc/ASSIOMI.md`: A9 e A10.** Sola documentazione

**Data:** 2026-09-18 · **Branch** `fork-su2` · **HEAD alla scrittura** `37e0b72`
**Blob:** `f8f46683` — **e non cambierà: nessun codice si tocca.**

---

## 1. RAGIONAMENTO PRELIMINARE

### 1.1 La verifica dal disco, fatta PRIMA di accettare il §0 del mandato (P1)

Il mandato dichiara uno stato. **L'ho controllato io, e regge** (`doc/ASSIOMI.md`, 15.003 byte,
timestamp 2026-09-17 21:47):

```
INTESTAZIONI TROVATE : A1 · A2 · A3 (+A3b +A3c) · A4 · A5 · A6 · A7 (+A7b) · A8 (+A8b)
                       APERTO · SCANSIONI MAI FATTE
grep "A9"  -> 0 occorrenze      grep "A10" -> 0 occorrenze
APERTO #2  -> gia' BARRATO, col controesempio `u_nodo = I/media_dei_VICINI` (:2607)
```

> **Non è una riscrittura: è un completamento.** E il mandato lo dice per primo, registrando che il
> mandato *precedente* chiedeva una riscrittura integrale già in buona parte fatta.

### 1.2 Cosa credo di A9

**A9 è la più solida delle due, e per una ragione strutturale: è l'unica che ha già FALLIFICATO se
stessa.** Il caso dell'encoding non è un aneddoto — la settima occorrenza colpì **lo script che
stava CONTANDO le sei precedenti**. Un assioma che documenta il proprio fallimento e ne deriva la
soglia operativa (**alla terza si smette di scrivere**) **non è un'opinione: è una misura.**

**E si applica a se stesso:** finché A9 è solo una riga in un documento, **A9 è una violazione di
A9.** *(Il meccanismo, per lui, esiste già: `csv/_presidio.py`.)*

### 1.3 Cosa credo di A10, e cosa ci metto di mio

**Luca ha deciso: entra come assioma, non come candidato. Non discuto la decisione.**

**Ma ho appena MISURATO il suo caso forte** (`Z38`, commit `37e0b72`, oggi), e la misura aggiunge
due cose che l'enunciato non contiene:

1. **il gauge di `r` sta a `cs/CS_M = 0.801`**, cioè **`(cs/CS_M)² = 0.64`** nel fattore di
   `STEP2`: **i due ponti differiscono di un fattore misurato, non di un principio soltanto.**
   **Questo RAFFORZA A10, e va nel documento** (§5 del mandato: *non perdere i numeri*);
2. **⚠ ma ho anche misurato che raddrizzare il ponte NON È GRATIS:** la frazione nella banda utile
   passa da `87.1 %` a `15.1 %`, e **`r` cambia SIGNIFICATO**. Quindi il corollario *«uno dei due è
   sbagliato»* è vero come **diagnosi**, ma **non dice QUALE**, e sceglierlo **non è una
   conseguenza di A10.**

> **Lo scrivo DENTRO A10, nella sezione che il mandato stesso chiama «stato della verifica
> empirica, dichiarato». Non è un'obiezione all'assioma: è il suo limite, e A10 è più forte con il
> limite scritto che senza.**

### 1.4 E una cosa che il mandato mi dà già risolta, e che oggi ha un numero nuovo

Il §4.2 vieta di cancellare la tensione *«A2 è violato da `Lam = mean(I)`, che FUNZIONA (fattore
1300)»*. **Oggi quel fattore l'ho misurato io** (`Z39`): `cs_std/cs` vale **17.6 %** sul blob
attuale, contro lo **0.0086 %** pre-cura — **~2050**, non 1300. **Non riporto il numero del mandato:
riporto il mio, e dichiaro che sono lo stesso fatto letto su due misure diverse.**

### Cosa NON so

- **se la soglia «alla TERZA» di A9 sia giusta**: è scelta, non derivata. Lo dichiaro nel testo;
- **se A10 abbia altre istanze vere** oltre a quella misurata: il mandato dice *«il caso forte è
  UNO»*, e **non ho cercato le altre in questo giro.**

---

## 2. PROGETTAZIONE

**Passo 1 — A9**, dopo A8b, col testo del mandato, **più** la dichiarazione che la soglia «tre» è
scelta e non derivata, **più** il fatto che il meccanismo per il suo stesso caso esiste
(`csv/_presidio.py`).

**Passo 2 — A10**, dopo A9, col testo del mandato, **più** i numeri misurati oggi (`Z38`), **più**
il limite del §1.3.2 nella sezione «stato della verifica empirica».

**Passo 3 — la tabella di A3**, cinque casi, ognuno col suo numero. *(A8 ce l'ha già: è la ragione
per cui A8 ne trova di più.)*

**Passo 4 — APERTO:** dichiarare la categoria metodologica; **aggiornare il conteggio** («due voci
su otto» → **tre su dieci**: A6 teorema, A8 e A9 metodologici); **non cancellare** la tensione su
`Lam = mean(I)`, e **aggiornarne il numero**; lasciare le scansioni mai fatte.

**Passo 5 — riferimenti incrociati** in `CLAUDE.md` e `doc/RAMIFICAZIONI.md` **solo dove citano
assiomi per numero**, e solo se A9/A10 li cambiano.

**COSA MI FA FERMARE:** se un'aggiunta richiedesse di toccare il codice, o di **rimaneggiare** un
assioma esistente invece di completare. **Il mandato vieta entrambi, e sono d'accordo.**

**E una cosa che NON farò:** non declasso A10 a candidato, non invento altri assiomi, non cancello
le tensioni, non tocco `soliton_simulator.py`.

---

## 3. TODO DEL NEXT STEP

- [ ] **A9** aggiunto dopo A8b
- [ ] **A10** aggiunto dopo A9, coi numeri di `Z38`
- [ ] **tabella dei casi di A3** (cinque)
- [ ] **APERTO**: categoria metodologica · conteggio aggiornato · tensione `Lam = mean(I)` col
      numero nuovo · scansioni invariate
- [ ] **riferimenti incrociati** `CLAUDE.md` / `RAMIFICAZIONI.md`
- [ ] relazione (par.5-ter) + **CHECKPOINT**
- [ ] **⚠ NON toccare:** il codice, gli assiomi esistenti, le tensioni aperte
