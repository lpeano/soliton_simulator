# TASK HISTORY — **il run lungo: 6000 passi sulla scena video, con l'archivio**

**Data:** 2026-09-19 · **Branch** `fork-su2` · **HEAD alla scrittura** `983ae2d`
**Blob del simulatore:** `7c4dec1d` *(byte grezzi `5216c891`)* — **è il blob SIGILLATO 12/12**, albero **PULITO**.
**⚠ IL SIMULATORE NON SI TOCCA FINO A FINE RUN:** ogni modifica cambia il blob e `carica_stato`
**RIFIUTA** i `.pkl` di un blob diverso. **L'archivio morirebbe.**

> **La ragione del run, che va in testa al referto:** *«dentro quei dati potremmo trovare qualcosa
> che ci fa cambiare idea sulle leggi.»* **Questo run non cerca una conferma: costruisce l'archivio
> su cui si faranno molte misure diverse.**

---

## 1. RAGIONAMENTO PRELIMINARE — **quattro cose verificate dal disco, e tre cambiano il mandato**

### 1.1 ⚠ `--db-serie` e `--db-ogni` sono INERTI nel percorso della scena

Verificato dal sorgente: **tutta la logica dell'archivio vive in `batch_condensazione`**
*(il blocco `if _db and _db_serie:`)*. Il driver `csv/_test_fork/_scena_video.py` **non passa di
lì**: costruisce la scena con `avvia_test("N-MASSE")` e fa il proprio ciclo, salvando con
`salva_stato(frame_%d.pkl)` **solo ai frame elencati sulla riga di comando**.

> **Quindi il comando del §2 del mandato, preso alla lettera, NON produrrebbe nessuna serie:**
> i due flag verrebbero accettati dal parser e **ignorati**. *(È la stessa forma del difetto già
> registrato per `--sync-db` in headless: «CARICA soltanto, e non chiama MAI `salva_stato`».)*
>
> **COSA FACCIO: estendo IL DRIVER**, che è uno **script** — **il blob del simulatore non cambia**,
> quindi l'archivio resta valido. **Il default del driver resta IDENTICO**, e il suo sigillo
> (`csv/_seal_fork/_sigillo_driver_video.py`) **va rigirato**: un driver modificato e non
> ri-sigillato è esattamente il difetto di `FintaRete` *(«non falliva: SI SCHIANTAVA»)*.

### 1.2 ⚠ IL SEME DELLA SCENA È `42`, NON `900` — e `--seed` è IGNORATO dal driver

```
:4531   net = Rete()               -> Rete.__init__(seed=42)   IL SEME E' 42
:4532   net.semina(SEME_INIZIALE)  -> 900 e' il NUMERO DI NODI, non il seme
:6407   seed = a.seed if a.seed is not None else SEME_INIZIALE  <- solo nel BATCH
:5842   if a.seed is not None ...  <- solo in esegui_headless, che il driver NON usa
```

**`SEME_INIZIALE` fa DUE MESTIERI:** *numero di nodi seminati* all'import **e** *valore di default
del seme RNG* nel batch. **Per questo il batch di `Z46` è «seed 900» ed è vero, mentre scrivere
«seed 900» per la scena video sarebbe FALSO.**

> **Il run è comunque DETERMINISTICO** *(nessun `--seed`, ma nessuna sorgente di casualità oltre
> `self.rng`: verificato in modo esaustivo il 2026-09-19, `2c92b9d`)*. **Ed è lo STESSO seme di
> `Z49` e `Z52`**, perché è lo stesso percorso — quindi **il confronto a 2400 passi è legittimo.**
> **Va DICHIARATO nel CSV e nel referto (P6): `seme_effettivo = 42`.**

### 1.3 ⚠ Nel driver `_db_step` è il **FRAME**, non il passo di motore

Il driver scrive `S.net._db_step = fr` e lo dichiara nel commento. **Ma la serie numera per
PASSO**, e `V4` del sigillo dell'archivio verifica *«il passo nel NOME == `_db_step` nei DATI»*.

> **Nella modalità serie scriverò il PASSO DI MOTORE** (`frame × PASSI_PER_FRAME`), che è anche
> l'unità con cui `Z9`/`Z46`/`Z49` sono scritte. **I `.pkl` vecchi (`frame_N.pkl`) NON sono
> confrontabili per nome** — ma non lo sono comunque: **blob diverso, e senza le coorti** (`Z53`).

### 1.4 Cosa NON so, e che il pilota deve misurare

- **quanto costa un frame a `n` grande.** A 2400 passi `n` andava da `2391` a `8018`; **a 6000 passi
  `n` è IGNOTO** e il costo per frame **cresce con `n`**, quindi l'estrapolazione lineare
  **sottostima** — lo dice il driver stesso nel suo output;
- **quanto pesa uno snapshot compresso a livello 1**, e **come cresce con `n`**: il conto della
  serie si fa sul peso FINALE, non sul primo;
- **il tasso `d(eta)/d(passo)` per FERMI e MOBILI separati** (A3c: **mai una mediana sulle due
  popolazioni**), e **`passi(ramp = 1) = TAU_A/(DT·r) = 5000/r`** per ciascuno.

---

## 2. PROGETTAZIONE

### 2.1 L'estensione del driver — e cosa NON cambia
- **serie**: ogni `OGNI` frame salva `<dest>/scena_%06d.pkl.gz` col **passo di motore** nel nome e
  in `_db_step`, riusando `_db_serie_path` del simulatore *(non una convenzione nuova)*;
- **CSV di progresso** con `blob`, `seme_effettivo`, i flag, `n`, `archi`, `coer_l`, `dil`, e i
  tempi — **P6: un file che si distingue solo per il nome non è un dato**;
- **contatori A8**: snapshot scritti / saltati / **FALLITI**, dichiarati a fine run. **Col disco al
  96 % un salvataggio fallito in silenzio è il difetto peggiore possibile qui**;
- **il comportamento di DEFAULT del driver resta byte-identico**: senza i nuovi argomenti fa
  esattamente quello che faceva. **Lo prova il rigiro del suo sigillo.**

### 2.2 La cadenza `--db-ogni` **si DERIVA, non si sceglie**
Dal pilota escono `peso(n)` e `n(passo)`. La cadenza è il più grande valore che soddisfa
**spazio_totale ≤ budget dichiarato**, con il budget fissato **prima** di guardare il risultato.
**ZERO NUMERI SCELTI A OCCHIO, e il conto si scrive.**

### 2.3 COSA MI FA FERMARE (fissato PRIMA)
- **lo spazio stimato supera il budget** → **riporto e propongo**, non taglio i passi;
- **il sigillo del driver non ripassa** → ho toccato la fisica con l'estensione: **STOP**;
- **un salvataggio FALLISCE** durante il run → il contatore lo dice, e **si riporta subito**;
- **il costo per frame esplode** oltre quanto il pilota prevede → **riporto il numero vero**, non
  aggiusto la stima a posteriori.

---

## 3. TODO
- [x] blob/branch, e le quattro verifiche di §1
- [ ] estensione del driver + **rigiro del suo sigillo**
- [ ] **il PILOTA: i tre numeri** → **riporta, e DERIVA la cadenza**
- [ ] previsioni qualitative committate **PRIMA** → `STATO_RUN.md` → **il run**
- [ ] *(il punto ⑥ del mandato precedente — registri, inventario, `CLAUDE.md`, relazione,
      presidio del blob — **è GIÀ FATTO**: `c90a02d`, `e6df983`, `983ae2d`)*
- [ ] **⚠ NON toccare:** il simulatore, il default del driver, i `.pkl` esistenti
