# TASK HISTORY — **il run lungo a 6000 passi: prima il PILOTA, e il comando del mandato ha un difetto**

**Data:** 2026-09-19 · **Branch** `fork-su2` · **HEAD alla scrittura** `326bb5d`
**Blob:** `b9e07c73` *(git blob)* · **byte grezzi:** `2a7207a4` · **albero PULITO** · **CPU LIBERA**
**Nessuna cura, nessun cablaggio, nessuna promozione.** **Il simulatore non si tocca finché il run gira.**

---

## 1. RAGIONAMENTO PRELIMINARE

### 1.1 ⚠ **`--sync-db` NON produce 30 snapshot: scrive UN SOLO FILE, sovrascritto**

**Verificato dal disco, `:7182-7185`:**

```python
if _db and step > 0 and _step_glob % _db_ogni == 0:
    net._db_step = _step_glob
    net.salva_stato(_db)          # <- SEMPRE LO STESSO PATH
```

**E `--ogni` non c'entra con gli snapshot: controlla la cadenza del CSV** (`:6229`,
*«batch: ogni quanti passi registrare una misura»*). **La cadenza del DB è `--db-ogni`, default 250.**

> **Quindi il comando del mandato — `--ogni 200 → 30 snapshot, ~1.2 GB` — NON produrrebbe 30
> snapshot: produrrebbe UN file da ~40 MB, sovrascritto 24 volte, e alla fine ci sarebbe SOLO
> L'ULTIMO STATO.**
> **E il §3 chiede `Z9-b` «a OGNI snapshot»: senza la serie, quella misura non si può fare.**

**Non è un errore del mandato sulla fisica: è come funziona il flag.** **La serie di snapshot delle
scene video l'ha prodotta un DRIVER** (`_scena_video.py`), **non il simulatore.**
**Per il batch servirà lo stesso meccanismo — un guardiano che copia il `_db` a ogni salvataggio,
con la cura del RACE già applicata una volta in questa sessione** *(il watcher che faceva
`shutil.copyfile` mentre `os.replace` scriveva produsse due snapshot col `_db_step` sbagliato: la
cura è ri-dumpare l'oggetto LETTO, con verifica)*.
**Va detto PRIMA di lanciare, non scoperto dopo 2 ore di run.**

### 1.2 Il pilota si può fare con **UNA SOLA CORSA**, e senza watcher

**Perché il CSV del batch registra già ciò che serve** (`:6396`, `:6402`):

```
cols['r_min'],   cols['r_max'],   cols['r_mean']
cols['eta_min'], cols['eta_max'], cols['eta_mean']
```

**`r` È GIÀ NEL CSV.** Se nel batch `r` fosse al pavimento, `r_min`/`r_mean` lo dicono senza aprire
nessun `.pkl`.

**E per la separazione FERMI/MOBILI che A3c impone, basta UN solo `.pkl`, non due:**
per i nodi presenti da `t = 0` (indice `< n0`), **`eta(T)/T` È la media di `DT·r` su tutto il run**
— nessuna differenza fra due istanti, nessuna coppia di snapshot. **Si divide per `_r_corrente`
letto dallo stesso file**, e si hanno i due tassi separati.

> **Quindi: UNA corsa, `--passi 300`, `--ogni 50` (6 righe di CSV), `--db-ogni 300` (il `.pkl`
> finale). Costo minimo, e risponde a tutte e tre le domande del §1 del mandato.**

### 1.3 Il seme si PINNA, e si sceglie quello di `Z46`

`--seed` ha `default=None` (`:5918`). **Si passa `--seed 900`, lo stesso del batch a 1200 passi di
`Z46`**, così il confronto col `93 % di nodi fermi` è **sullo stesso seme e sulla stessa scena**, non
per analogia. *(A3c: la popolazione e la scena devono essere le stesse, o si dichiara.)*

### 1.4 ⚠ Lo SPAZIO DISCO — **misurato, e va riportato**

```
C:  476G totali,  456G usati,  20G liberi,  96 % pieno
```

**20 GB liberi con il disco al 96 %.** **Se gli snapshot fossero ~40 MB × 30 = 1.2 GB, ci starebbero
— ma il margine è il 6 % del disco, e il batch potrebbe avere `.pkl` molto più piccoli della scena
video** *(là `n = 8018` e `436 636` archi; `Z46` dice che nel batch `n` è PIATTO intorno a `1204`)*.
**Il pilota misurerà la dimensione vera di UN `.pkl`, e il totale si calcolerà da quella.**

### 1.5 Cosa mi aspetto, e **cosa NON so**

**Mi aspetto** *(e se sbaglio lo annoto)*: che nel batch `r_mean` sia **molto** sotto 1, e che
`eta_mean` cresca **molto** più lentamente dei `0.014/passo` della scena video — perché `Z46` misura
`r/r_floor = 1.0000` sul 93 % dei nodi **in questa stessa scena**.

**NON so:**
- **se il 93 % di `Z46` valga già a 300 passi o si formi dopo.** `Z46` misura a `120-1200`; il pilota
  guarda `300`. **Se a 300 i fermi fossero pochi, non potrei concludere né in un senso né
  nell'altro**, e dovrei dirlo invece di estrapolare;
- **quanto pesi un `.pkl` del batch.** Va misurato;
- **se `eta_min` del CSV sia dei FERMI o dei NEONATI.** Sono due popolazioni diverse e il CSV non le
  separa: **è proprio la ragione per cui serve anche il `.pkl`.**

---

## 2. PROGETTAZIONE — *il pilota, e cosa decide*

**UNA corsa:**
```
--batch --nmasse 3 --sep 8 --passi 300 --ogni 50 --db-ogni 300 --seed 900
  <+ tutti i flag del mandato>  --sync-db <pilota.pkl>  --csv <pilota.csv>
```

**LE MISURE:**
1. **durata per 100 passi**, cronometrata → l'estrapolazione a 6000, **con la riserva che `n` cresce**;
2. **`r_min`/`r_mean`/`r_max` dal CSV**, a sei istanti → `r` è al pavimento nel batch?
3. **`eta_min`/`eta_mean`/`eta_max` dal CSV** → il tasso, e **se la crescita è lineare**;
4. **dal `.pkl`: `eta/300` per i nodi `indice < n0`, SEPARATI per `r/r_floor < 2`** → i due tassi,
   e **quanti nodi sono in ciascun gruppo**;
5. **l'estrapolazione a `ramp = 1` per ciascun gruppo**: `TAU_A/(DT·r) = 5000/r`;
6. **la dimensione del `.pkl`** → il totale per 30 snapshot.

**LE TRE LETTURE (del mandato, non mie):**
- **il batch matura entro 6000** → si lancia il batch;
- **il batch NON matura** → **lo dico e propongo la scena video**, e **NON lancio i 6000 sapendo che
  non rispondono**;
- **matura solo la minoranza mobile** → **lo riporto con quanti nodi sono.**

**⚠ E UNA QUARTA, che aggiungo io perché il §1.1 la rende possibile:**
**se il pilota è verde MA la serie di snapshot richiede un driver**, allora **il driver va scritto,
committato e SIGILLATO prima del run** — come fu per `_scena_video.py` — **e questo è un costo di
tempo che va dichiarato adesso, non dopo.**

### 2.1 COSA MI FA FERMARE
- **`r` al pavimento nel batch** → il conto dei 6000 non vale: **STOP e proposta alternativa;**
- **il `.pkl` così grande che 30 snapshot non ci stanno nei 20 GB** → **STOP e proposta `--ogni` più
  rado**, mai meno passi;
- **la durata estrapolata oltre ~6 ore** → **lo dico prima**, non a metà run.

---

## 3. TODO DEL NEXT STEP

- [x] blob/branch dal disco · CPU libera · spazio disco misurato
- [ ] **IL PILOTA** → **STOP e riporta** contro le tre letture
- [ ] (via libera) il driver degli snapshot, se serve → **committato e sigillato PRIMA**
- [ ] previsioni qualitative → `STATO_RUN.md` all'avvio → **il run**
- [ ] le misure del §3 → referto + registro + relazione + **CHECKPOINT**
- [ ] **⚠ NON toccare:** il simulatore (mai, finché il run gira), `TAU_A`, `ramp`, `_pesi()`
