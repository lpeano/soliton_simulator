# TASK HISTORY — **il passo 198: evento FISICO o ECCEZIONE NUMERICA?**

**Data** 2026-09-19 · **branch** `fork-su2` · **HEAD** `4d4ccd0` · **blob** `7c4dec1d` · albero
**pulito** · dati: `csv/_test_fork/_fin_B`, **11 snapshot** a 6 passi, dal 180 al 240
*(rigiocata **sigillata 3/3**)*.

> **NESSUN RUN NUOVO. NESSUNA CURA. Il simulatore e il driver non si toccano.**
> **Prima di attribuire significato fisico all'evento, si esclude che sia un artefatto numerico.**

---

## 1. IL FATTO CHE MOTIVA IL SOSPETTO

```
passo 192:  omega_s max = 0.31    nati = 3
passo 198:  omega_s max = 401     nati = 3     <- x1300 in SEI passi, popolazione COSTANTE
```

**Un sistema che accelera lo fa su una scala di tempo. Questo salta.** E la spiegazione facile —
la mitosi — **è già caduta**: a 60 passi sembrava coincidere, a 6 si separa, e la mitosi parte
**dopo** (`Z61`).

## 2. LE DUE LETTURE DEL §1, **coi numeri, fissate PRIMA**

**La domanda è: `omega_s` è una DISTRIBUZIONE che si sposta, o POCHI NODI che schizzano?**

| # | condizione | conclusione |
|---|---|---|
| **A — POPOLAZIONE** | `log(p50₁₉₈/p50₁₉₂) / log(max₁₉₈/max₁₉₂) ≥ 0.3` | **la mediana si sposta insieme al massimo: è un fenomeno del sistema**, e il sospetto numerico si indebolisce |
| **B — POCHI NODI** | i nodi sopra `10 × p99₁₉₂` sono **≤ 100** **e** `p50` cresce meno di **×2** | **un pugno di nodi che schizza: è il profilo di un ARTEFATTO**, e si indaga come tale |

**Se vale `B`, si guarda CHI SONO:** indici, `eta`, `_deg`, `|psi|`, `peq`, `d`, `cs` — **e cosa
hanno di diverso dagli altri.** **E se sono SEMPRE GLI STESSI**, con il **Jaccard** fra istanti
consecutivi *(il metodo di `Z46`)*.

> **Il MASSIMO non è la distribuzione** — è l'errore che questo mandato esiste per evitare (A3c).
> **Si riportano `p25`, `p50`, `p75`, `p95`, `p99`, `max` a ogni istante, tutti.**

## 3. I TRE CANDIDATI NUMERICI — **da DATARE, non da ipotizzare**

### ① `_cs_lam_degenere` — e la catena è PRECISA, letta dal codice

```python
:3011  if not (_Lam > 1e-30):                      # _Lam = mean(I), cioe' sistema VUOTO
:3012      self._cs_lam_degenere += 1
:3013  _scala = max(_Lam, 1e-30) / (GAMMA_TURBO**2)
:3014  cs_floor = CS_M / (1.0 + sqrt(_I) * sqrt(1.0/_scala))
```

**Se scatta, `_scala = 1e-30` e `sqrt(1/_scala) = 1e15`: `cs_floor` COLLASSA verso zero.** E **`cs`
sta al DENOMINATORE di `tau = d/cs`** *(il ramo `--tau-luce`, attivo in questo run)*, **quindi
`tau` esplode e il termine `−omega/tau` che FRENA `omega_s` sparisce.**

> **Al 2700 il contatore vale `2`: è scattato DUE volte in tutto il run.** **QUANDO?**
> **Se una delle due cade al passo 198, la catena è completa.** Si data dalle **differenze** del
> contatore fra i dieci istanti *(è cumulativo)*.

### ② `_fatt_cs_ultimo` — atteso `~1`, al 2700 vale `142.8`

`sqrt(142.8) ≈ 11.9`: **`cs` dodici volte più piccolo di `CS_M`.** Si riportano **percentili e
max** nei dieci istanti, e **quando attraversa**.

### ③ `NaN` / `inf` / overflow non ancora diventato `inf`

Il registro ha già il caso: **`rho/peq = 0/0 = NaN` finirebbe in `omega_s`, la memoria
persistente**, e **`np.maximum(NaN, 1e-6)` È `NaN`: il pavimento NON protegge.**

**Si conta:** `NaN` e `inf` in `omega_s`, `psi`, `psi_spin`, `peq`, `cs`, `_fatt_cs_ultimo`, **a
ogni istante**. **Zero è l'atteso: se ce n'è anche uno, quello è il reperto.**
**E i valori ESTREMI ma FINITI** *(oltre `1e10`, oltre `1e15`)*: **un overflow che non è ancora
diventato `inf` si vede così.**

## 4. COSA MI FA FERMARE

- **i tre candidati cadono tutti** → **si riporta che l'evento NON è spiegato**, e **NON se ne
  inventa un quarto**. *(Oggi quattro ipotesi pronte sono risultate false, e tre erano di Claude
  web.)*
- **i dieci istanti non bastano a separare l'evento** → **lo si dice e si propone**, ma **non si
  rigioca a caso**: sotto il frame non si scende senza toccare il driver, e il driver non si tocca.
- **una lettura che non si applica** → non si allarga l'altra per farla scattare.

## 5. I PRESIDI
- **MAI il massimo al posto della distribuzione** (A3c);
- **uno snapshot alla volta**, mai tutti in RAM;
- **i contatori sono CUMULATIVI**: si datano con le **differenze**, non coi valori;
- **UN SEME, UNA SCENA**, `--tau-luce` sigillo **6/7**. Nessun verdetto di fisica.
