# TASK HISTORY — **`f` e `median(|f|)`, separati**

**Data** 2026-09-20 · **branch** `fork-su2` · **HEAD** `6973520` · **blob** `775ceab7` · albero
**pulito**.

> **NESSUNA CURA. Il simulatore non si tocca.**

---

## 1. ⚠ I DATI BASTANO: **non si rigenera niente**

**Verificato dal disco, negli snapshot:**

```
psi_spin         (n,2) complex128      <- presenti ENTRAMBI, a un passo di distanza
_psi_spin_prec   (n,2) complex128
_med_f_prec      float   0.3871769...  <- IL `med` USATO, gia' salvato
_med_f_ultimo    float   0.3871769...
```

**Quindi `f` si ricostruisce ESATTAMENTE come lo calcola il codice, e `x = f/med` pure.**
**Il §3 del mandato (rigenerare le finestre) NON si applica.**

**La formula, letta dal sorgente e non ricordata:**

```python
a      = np.angle(_ps[:, 0]) - np.angle(_psp[:, 0])
signed = ((a + 2*np.pi) % (4*np.pi) - 2*np.pi) / DT      # wrapping su 4pi (l'otto)
f      = signed if TEMPO_PROPRIO_ORIENTATO else np.abs(signed)
```

## 2. LE QUATTRO LETTURE, fissate PRIMA

| # | esito | conclusione |
|---|---|---|
| **α** | **`median(|f|)` CROLLA** verso il pavimento o molto sotto il suo valore iniziale | **il METRO si accorcia**: è `A3`, il riferimento costruito sul proprio insieme. **`f` può essere tranquillo** |
| **β** | **`f` CRESCE davvero**, ordini di grandezza nei primi 24 passi | **qualcosa accelera la rotazione della fase**, e la causa è nella dinamica |
| **γ** | **nessuno dei due si muove in modo drammatico** | **l'algebra del §0 è sbagliata**, `x` non vale 224, e `r = √2` ha un'altra spiegazione. **È l'esito più probabile, e chiude una pista falsa** |
| **δ** | **il pavimento scatta più di due volte per passo** | **il contatore al 2700 conta un'altra cosa** *(per chiamata? per nodo?)*, e va chiarito **cosa** misura |

## 3. IL CONTROLLO CHE PUÒ CHIUDERE TUTTO

**`x = f/med` ricalcolato dai dati, contro l'`x` dedotto invertendo `r`** *(`~1.6` al passo 12,
`~4.35` al 18, `~224` al 24)*.

> **Se non coincidono, l'algebra del mandato è sbagliata e si chiude lì.** **È la prima cosa da
> falsificare, e il mandato stesso lo dice.**

## 4. I PRESIDI
- **`f` e `x` sono DUE COSE DIVERSE:** si riportano **separati**, mai il solo rapporto;
- **`|f|` con i percentili** *(p05, p25, p50, p75, p95, max)*, **mai una mediana da sola** (A3c);
- **i contatori sono CUMULATIVI:** il pavimento si data con le **differenze**;
- **uno snapshot alla volta.**

## 5. COSA MI FA FERMARE
- **`x` ricalcolato ≠ `x` dedotto** → l'algebra è sbagliata: **si riporta e si chiude**;
- **nessuna delle quattro letture** → si riporta, **senza inventarne una quinta**.
