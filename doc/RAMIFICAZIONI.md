# REGISTRO DELLE RAMIFICAZIONI — lo **stato** dei fronti, non la cronaca

> Branch `fork-su2`. Creato 2026-09-15. Blob al momento della creazione: **`7d484580`**.
> **Questo e' uno STATO, non un diario.** La cronaca sta nei documenti di `doc/` e in
> `CLAUDECONNECT.md`. Qui c'e' solo: dove siamo, su cosa, e cosa lo chiuderebbe.
>
> **Si aggiorna nello STESSO commit del riscontro che lo cambia** (§5-bis). Un registro aggiornato
> «dopo» e' un registro falso.
>
> **Registro GEMELLO:** questo file tiene i **fronti aperti**. Lo stato **promosso / candidato /
> esperimento / correzione** delle **componenti** (cioe' dei flag) sta in
> **`doc/COMPONENTI_PROMOSSE.md`**, e la regola che lo governa e' **`CLAUDE.md` §10**.
> Non si mescolano: qui c'e' *cosa non sappiamo ancora*, li' c'e' *cosa e' fisica e cosa e' opzione*.

---

## ⚠ LA COSA DA LEGGERE PER PRIMA

> **⚠ QUARTO MARCHIO, il piu' esteso — 2026-09-16, DUE CORREZIONI DI DIFETTO SENZA FLAG.**
> **Ogni misura di questo repo prodotta PRIMA del blob `c57800c1` e' «prodotta con `_xi_rumore`
> EREDITATO alla mitosi e SENZA il fattore `cs^-2` nell'inerzia: misura di un sistema DIVERSO da
> quello corrente».** Vale per gli **8 CSV della campagna a quattro bracci**, per **tutti i sigilli
> del 2026-09-16** e per ogni voce di questo registro che li cita.
> **① `_xi_rumore` non si eredita** (blob `57681b9e`): `xi` e' un campione dell'**AMBIENTE**, non
> una proprieta' del nodo. Prima, padre e figlio avevano rumore **correlato: misurato `+1.0000`**;
> dopo, **`+0.0065` su 258 coppie**.
> **SIGILLO DELLE DUE CORREZIONI: 16/16 PASS** (`csv/_seal_fork/_sigillo_correzioni.txt`).
> **② `inerzia = max(rho*(CS_M/cs)^2, 1e-6)`** (blob `c57800c1`): il fattore che la derivazione
> `inerzia = (d/cs)^2` impone e che **mancava**. Sigillo **M2 PASS**, `max|A-B| = 0.000e+00` con
> nodi **3070 = 3070** dove `cs = CS_M`; **M2b PASS** (col `cs` vivo il fattore morde).
> **⚠ MA L'EFFETTO OGGI E' MINUSCOLO, ed era scritto PRIMA:** il fattore vale **1.0000048** sul
> nodo mediano (`cs_std/cs = 0.0333 %` a 150 passi). **Si e' fatta perche' senza la legge e'
> sbagliata**, non per un effetto misurabile a questa densita'.
> **Le voci di §B e §C NON sono state ri-misurate**: portano questo marchio in aggiunta ai tre
> precedenti.

> **⚠ SECONDO MARCHIO, aggiunto il 2026-09-15 sera — L'OROLOGIO.** Tutte le misure di questo repo
> fino al blob `08784685` sono state prese con il **ritmo SCALARE a 2π**: la doppia copertura
> (**FASE 5, 4π**) **non era attiva** (95.33 % di scarto della guardia, **C11**).
> **Non erano «integrate male»** — `r` era ricalcolato a ogni passo e valido; erano misure di un
> **modello diverso da quello che il flag dichiarava**.
> **Condizione da riverificare col settore 4π in funzione**, e vale **anche per T3 e i suoi quattro
> bracci**, cioè per il divario stesso che stiamo inseguendo.
>
> **✅ TERZO MARCHIO — TOLTO IL 2026-09-16. `tau` SEGUE `cs`, MISURATO.**
> Il ri-sigillo dello STRATO 1 con **`--cs-dinamico`** e il nuovo **SIGILLO 8** da'
> **25/27 PASS + 2 FAIL ATTESI** (S1a/S1b, previsti e committati **prima** in
> `doc/PREDIZIONE_risigillo_strato1.md`). **S8b: rapporto `cs=8 / cs=1` = 7.660686976**
> misurato = atteso — **se `cs` fosse ignorato varrebbe ESATTAMENTE 1.000000000**.
> **Rilanciare il sigillo com'era NON sarebbe bastato:** nei test in-process
> `_cs_nodo_prev` era `None` o **costante**, e un `cs` costante non esercita `tau = d/cs`.
> **⚠ E NON SIGNIFICA che `tau = d/cs` sia distinguibile da `tau ∝ d` nei run veri:** C13
> dice di no. **S8 prova che la LEGGE e' viva; C13 che a queste densita' ha poco da dire.**
> **E sullo stesso ri-sigillo e' uscito un difetto grosso: il sigillo SI SCHIANTAVA da un
> giorno** (`AttributeError` a S2, `_tempo_luce_nodo` mancante in `FintaRete`): **S2..S8 e
> S3b non giravano affatto**, e dal blob `f7051c3` il «23/23» non era **riproducibile**.
>
> **⚠ TERZO MARCHIO, testo STORICO — `cs` COSTANTE (2026-09-15, rilievo di Luca).**
> Il **sigillo 23/23 dello STRATO 1** e le due misure del braccio OFF sono girati **senza
> `--cs-dinamico`**: `cs = CS_M` costante, `_cs_nodo_prev` mai scritta, quindi `tau = d/CS_M`.
> **Il meccanismo del ritardo regge** (S7 misura `r=2/r=1 = 1.9753`, che dipende da `r` non da `cs`);
> **la dipendenza di `tau` da `cs` NON e' mai stata esercitata** — ed e' *proprio* la ragione per cui
> `tau = d/cs` sarebbe piu' principiato di `tau ∝ rho`.
> Vale **doppio** per lo Strato 1: quel sigillo e' del blob `2277e9a0`, **precedente alla cura della
> cache**, quindi anche col flag acceso la cache sarebbe stata scartata a ogni mitosi.
> **I quattro run in partenza sono la PRIMA VOLTA che quella dipendenza gira davvero: non e'
> «rifare la misura meglio», e' MISURARE PER LA PRIMA VOLTA.**
>
> **Quattro chiusure (§B) sono MISURE fatte su un settore ALIASATO** — lo spin gira a ~112
> giri/passo, quindi ogni misura su di esso e' presa su uno sfarfallio.
> **Vanno rifatte quando `omega` rientra nel tetto.** Se questo non resta scritto, fra un mese
> verranno lette come definitive: e' **esattamente** il difetto che `doc/AUDIT_misurato_vs_asserito.md`
> ha gia' trovato nei documenti di questo progetto.

---

# A. CHIUSE PER DIMOSTRAZIONE — algebriche, **non si riaprono**

| # | fronte | riscontro | sostegno |
|---|---|---|---|
| **A1** | **Teorema di inerzia** — lo Strato 0 e' inerte: la connessione costruita dagli stessi stati che trasporta e' uno **specchio**, `<psi_i\|N\|psi_j> = 2<psi_i\|psi_j>` esatto | **1.57e-15** su 200 000 coppie | `csv/_seal_fork/_reperto_inerzia.py`, `CLAUDE.md` §6 |
| **A2** | **Invarianza del Bloch sotto Step 2** — `_phc` e' una **fase globale**, e `nb = psi†σpsi` non la vede | **3.3e-16** | `csv/_seal_fork/_sigillo_step2.py`, `CLAUDECONNECT.md` §926 |
| **A3** | **FDT del solo scuotimento** — il drift di `n → (n+a·g)/\|n+a·g\|` e' `−a²n`, funzione del **solo `n`**, senza i vicini: **un rumore locale non puo' allineare ai vicini** | verificato a **1.28e-07** | `doc/PREDIZIONE_fdt_scuotimento.md` |

> **Sono dimostrazioni**, non misure: valgono a qualunque risoluzione, **anche con lo spin aliasato**.

**Nota su A2, dichiarata perche' il registro non deve contenere numeri senza fonte:** il mandato che
ha generato questo file cita `2.78e-17` e `1.11e-16`. **Non compaiono da nessuna parte nel repo**
(cercati: zero occorrenze), mentre `3.3e-16` compare in 10 punti. Ho scritto **quello documentato**.
Se i due numeri vengono da run di Luca fuori dal repo, vanno committati per entrare qui.

---

# B. CHIUSE PER MISURA — **su un settore ALIASATO ⇒ DA RIFARE**

> **MARCHIO COMUNE, che non si toglie:** *misurate con il settore di spin a **~112 giri/passo**
> (aliasato). **Da rifare se e quando `omega` rientra nel tetto** `2π·cs/λ`.*

| # | fronte | riscontro | cosa lo chiuderebbe **davvero** |
|---|---|---|---|
| **B4** | **Bloch frozen-o-noise** | senza scuotimento `\|<n>\| = 1.000000`; con, `chi = 90.0 ± 39.2` contro il nullo **90.000 ± 39.171** | la stessa misura con `theta < ~1 giro/passo` |
| **B5** | **Kuramoto refutato** | K-frozen **byte-identico** a OFF; K-noise = NO-rumore | idem |
| **B6** | **Esito B del turbo** — con `cs` al **5 %** di `CS_M` lo Step 2 non muove lo spin | `chi` ON 89.9865 / OFF 89.9941 contro 90.000; autocorrelazione piatta su 14 bin | idem, **+ un secondo seme** (§2.7) |
| **B7** | **shake-then-freeze** — la precessione mutua non organizza | `chi` deriva di **−0.33°** in 600 passi da stato casuale | idem |

---

# C. DIAGNOSI CHIUSE (2026-09-15)

| # | cosa e' stato stabilito | numero | sostegno |
|---|---|---|---|
| **C1** | **ESITO (I): nessun bug.** `omega = coppia/inerzia` porta **esattamente** il −1 richiesto | pendenza **−1.056**, `r = −0.981`, n=2781 | `doc/TRACING_omega.md` |
| **C2** | Il −1 e' **cancellato da `√tau`**, con `tau ∝ rho^1.81` | `+1.176 ± 0.019` (n=2195) | `doc/TAU_tempo_luce.md` |
| **C3** | Il residuo di 0.34 e' **TRANSITORIO**, non un termine mancante | `0.979 → 0.010` fra i passi 50 e 400 | `doc/BARRE_ERRORE_pendenze.md` |
| **C4** | **`inerzia = T²`** — chiude il buco dimensionale; esponente `cs^−2` **derivato**, verso confermato | stesso esponente dello Step 2, derivato **prima e indipendentemente** | `doc/INERZIA_tempo_quadro.md` |
| **C5** | **`tau_luce = d/cs` e' PIATTO** ⇒ la sostituzione **rompe** la cancellazione | **+0.097 ± 0.0055**, IC95 [+0.086, +0.108] | `doc/TAU_tempo_luce.md` §7 |
| **C6** | Il rumore **non** guida `omega` | `R_stoc = 0.041`, **sotto** l'errore atteso 0.097 | `doc/TRACING_omega.md` §6.1 |
| **C7** | **La cache `_cs_nodo_prev` veniva scartata a ogni mitosi** ⇒ nel 72 % delle chiamate `tau = d/cs` calcolava `tau = d/CS_M`. **Curata** (il figlio eredita da `src`, sesta voce della stessa convenzione). **NB: e' chiuso il DIFETTO, non un effetto fisico** — vedi D.2/**P** | fallback **71.88 % → 0.00 %**; cache inusabile **24/30 → 0/30**; sigillo **P1-P4 + P1b: 5/5 PASS** | `doc/FIX_cache_cs.md`, `csv/_seal_fork/_sigillo_fix_cache.txt` |
| **C8** | **LA FASE 2 NON SI CHIUDE.** Col tempo-luce cablato la pendenza resta **lontanissima** dall'attesa `-0.69`, e `theta` resta **aliasato** su tutti e sei i run misurati | ON post media **-0.4745**, divario **-0.2155**, SE della media **0.0165** ⇒ **z = 13.1**; `theta` **30.7-44.9 giri/passo** | `doc/FIX_cache_cs.md` §7, `csv/_test_fork/_controllo_semi.txt` |
| **C9** | **`--tau-luce` ha un effetto GRANDE sulla pendenza** (che pero' non basta): il contrasto ON-OFF e' **11 volte** la dispersione fra semi | ON-OFF **-0.3219** contro dispersione fra semi **0.030** | `csv/_test_fork/_rimisura_t3.txt` |
| **C11** | **`_psi_spin_prec` non era esteso alla mitosi ⇒ la guardia ESATTA di `ritmo()` scartava il ramo a **4π**: la **FASE 5 (orologio di doppia copertura) era INERTE in ogni run `--campo-spinoriale` mai girato**. `r` veniva dal ritmo **SCALARE a 2π**, quello storico — **non stale: diverso**. **CURATO**, settima voce della stessa convenzione | guardia 4π fallita nel **95.33 %** (143/150), condizione `len(_psi_spin_prec) != n` in **143/143**; dopo la cura **0.00 %** (0/60) e `len == n` **60/60**; sigillo **S1-S5 + S1b: 6/6 PASS**, `S1` `max\|A-B\| = 0.000e+00` con `n_A = n_B = 2501` | `doc/REPERTO_psi_spin_prec.md`, `csv/_seal_fork/_sigillo_psi_spin_prec.txt` |
| **C12** | **⚠ SECONDO CASO DEL PUNTO FISSO AUTO-NORMALIZZANTE.** `r` è normalizzato sulla **propria mediana** (`x = f/median(f)`, monotona) ⇒ **`median(r) = 1.0` identicamente, con qualunque orologio.** Quindi `S4` — che confrontava le due mediane — **non poteva misurare nulla**, e il suo `z = 0.00` non significa «nessun effetto» | `median(r)` **1.000000** contro **0.999999**, `z = 0.00`, per costruzione. L'unico numero informativo è la **dispersione**: **0.4421 → 0.4257** (−3.7 %), **un seme, nullo non misurato → C10** | `doc/REPERTO_psi_spin_prec.md` §8; il primo caso e' `_tau`/`_dens_rif` in `CLAUDE.md` §9 |
| **C13** | **⚠⚠ IL TEMPO-LUCE NON E' TESTABILE A QUESTA DENSITA'.** `cs` non e' «quasi-costante per caso»: e' **morto per densita'** (`I ~ 0.05` contro soglia `~400`, §6). Quindi **`tau = d/cs` E' `tau ∝ d`**, e tutto il ramo di lavoro su di esso è, in questo regime, **un ramo su `tau ∝ d`**. **Chi legge «tau = d/cs cablato e misurato» NON deve credere che il canale sia stato provato: non lo è.** | **AGGIORNATO a fine run (500 passi, 4 run): `cs_std/cs_medio` = **0.193 / 0.213 / 0.198 / 0.244 %** — **~4-5 volte** sotto la soglia dell'1 %, non 116. **Il rapporto CRESCE COL SISTEMA:** `0.0086 %` al passo 50 (n~80) → `0.096 %` al passo 300 → `~0.21 %` al passo 500, cioè **venti volte in 450 passi**. **La voce regge** (sotto l'1 %: `tau = d/cs` è `tau ∝ d`) **ma il margine è un quarantesimo di come l'avevo scritto**, e la traiettoria è monotona: **il tempo-luce non è «non testabile mai», è «non testabile a 500 passi»** | serve **`cs` VIVO**: alta densita', oppure il **turbo su GAMMA** — che pero' e' un **esperimento**, non fisica (`doc/COMPONENTI_PROMOSSE.md` C1) |
| **C14** | **ESITO (A) sui QUATTRO BRACCI — nessuna firma emerge nemmeno a risoluzione 6.4x migliore.** E **(A) NON è conclusivo**: `theta` resta oltre il giro per passo | `chi` mat **89.778 / 89.876** (OFF) e **90.048 / 90.084** (ON) contro il null 90.000; autocorrelazione max-z **1.81-2.36** su **40 bin** (il null ne dà 2.2-2.5); frazioni ai poli coincidenti entro il terzo decimale in tutti e quattro; **gradiente `theta` 96.37 → 15.08 = 6.39x** | `theta` sotto il tetto `2π·cs/λ`. Solo allora la frase «lo spin non si organizza» diventa dicibile |
| **C15** | **La firma di |<n>| che «pendeva» È CHIUSA come rumore.** Stamattina 1.741 / 1.374 (`z +2.11`, `+1.16`), *sempre dallo stesso lato* | su 4 run: **1.237 / 0.760 / 0.147 / 0.813**, `z` da `+0.81` a `-1.99` — **due sotto e due sopra** il null empirico `0.9213 ± 0.3888`. **Non si riproduce** | — chiusa, e per la ragione giusta: **piu' dati, non una rilettura** |
| **C16** | **`ESITO (I)` CONFERMATO QUATTRO VOLTE, con la barra giusta.** L'esponente di `omega = coppia/inerzia` non si muove fra semi, bracci e **due leggi di `tau` diverse** | `sigma` = **-1.0498 / -1.0592 / -1.0562 / -1.0554** (`r^2` 0.968-0.973, `n` 2458-3019) contro il **-1.056** del tracing | `doc/REFERTO_S_R_FDT.md` §2-bis |
| **C17** | **LA DISPERSIONE DI `r` E' RUMORE: la FASE 5 non ha, a oggi, NESSUN effetto misurabile.** Ne' sulla mediana (non poteva, C12) ne' sulla dispersione (poteva, e non ne ha). La cura C11 resta giusta — un flag inerte al 95.33 % e' un difetto — ma **il suo effetto fisico resta non dimostrato**, come per C7 (voce **P**) | `ON - OFF = +0.0130 +- 0.0274`, **IC95 [-0.074, +0.100]**, **+3.4 %** in relativo (su 2 semi sembrava ~10 %). Controllo C12: `r_mediana` = **1.000000** su 7 run su 8 | `doc/REFERTO_S_R_FDT.md` §3 |
| **C18** | **IL FDT RIFATTO SUL SISTEMA NON CASTRATO (FASE 5 attiva, `--cs-dinamico` acceso): MIGLIORA DI DUE ORDINI E RESTA INSUFFICIENTE DI TRE. NON SI CABLA** | `lambda` **3.47 -> ~26** (x7.5); `tau_smorz` **28.8 -> 3.7-3.8 passi**; `kT/Lam` **~2e7 -> 1.1e6** (OFF) / **1.5e5** (ON); ma `tau_smorz/tau_disordine` resta **3081** (OFF) / **726** (ON). Il migliore e' **1500 volte** sopra la banda in cui il FDT si applica | `doc/REFERTO_S_R_FDT.md` §4 |
| **C19** | **CHIUSO il 2026-09-16: le due convenzioni di `theta` ora girano INSIEME.** `theta_coord = \|omega\|*DT` (coordinata, la convenzione di C8 e dell'attesa `-0.69`) e `theta_prop = \|omega\|*dt_n` (proprio, quella giusta per §9) sono **entrambe** colonne del CSV, col loro rapporto `r_ratio` (che **e' `r`**). **Controllo di identita' cablato e verificato: `pend(prop) - pend(coord) - pend(r) = 4.6e-16`.** **E ne e' uscita una correzione alla CATENA:** da `\|omega\|_eq = \|F\|*sqrt(dt_n*tau/2)` con `dt_n = DT*r` segue `pend(theta_coord) = sigma + tau/2 + r/2` e `pend(theta_prop) = sigma + tau/2 + 3r/2`. **La formula usata finora, `sigma + tau/2`, ASSUMEVA `pend(r) = 0` in entrambe le convenzioni, e non era mai stato verificato.** Ora `t3_b_r` lo misura | **NB: il DIVARIO e' indipendente dalla convenzione** (`divario_prop - divario_coord = 0`, verificato). Gli 8 CSV gia' committati **non hanno** le colonne nuove: `_verdetto_S_R.py` lo dichiara con «(nessun dato)» invece di inventare | `doc/INVENTARIO_strumenti.md` §3-ter |
| **C20** | **PRIMA MISURA DELLA CONSERVAZIONE DI `L_tot = somma(inerzia*\|omega\|)`, mai fatta prima.** **Cresce del ~3.4 % a ogni passo con crescita**, sia PRIMA sia DOPO la correzione `cs^-2`. **La violazione e' MISURATA, non piu' argomentata** — e la correzione **non la chiude a questa densita'**, com'era scritto PRIMA (fattore `1.0000048` sul nodo mediano) | PRE mediana **+0.0340** (media +0.0413, 517 nodi nati); POST mediana **+0.0355** (media +0.0397, 479 nati). **Il confronto fra i due NON e' significativo** (traiettorie caotiche diverse): conta che **entrambi** stiano a ~3.4 % | `csv/_seal_fork/_sigillo_correzioni.txt`, sigillo **M4** |
| **C21** | **TRE CRITERI DI SIGILLO SBAGLIATI IN UN GIORNO, tutti scritti dal MODELLO MENTALE del codice invece che da una MISURA.** `N3b` (soglia assoluta su un comportamento legittimo), `M1b`/`M3` (misurati nel momento sbagliato), `M3c` (confrontati con la coppia sbagliata). **Un criterio scaduto che produce un FAIL FALSO costa PIU' di un sigillo mancante: si porta dietro una DIAGNOSI** | `M3c` stampava **«nodi nati 0»** — un numero **impossibile**, ed e' il modo in cui un criterio sbagliato si denuncia da solo. Misurato: nati **211**, freschi **194**, differenza **17** = esattamente l'ultima nidiata (`freschi[k] == nati[k-1]`) | presidio in `CLAUDE.md` §9 |
| **C22** | **LA CRESCITA DI `L_tot` E' L'INERZIA CHE SI ACCENDE, NON UNA VIOLAZIONE.** Il mandato la attribuiva alla mitosi; **misurato: `L_tot` cresce DI PIU' nei passi SENZA nascite** (da 3 a 7 volte, su 4 run su 4). **E la scomposizione dice perche': DOMINA l'INERZIA.** `d log(L/n) = d log(I) + d log(omega)`: peso di `A` **89.6 / 93.0 / 97.7 / 99.7 %**. `Lam` cresce **x376-1481** in 450 passi mentre `RMS(omega)` fa **x0.98-2.02** — **nel braccio ON `omega` e' PIATTO o in CALO**. **Non c'e' una sorgente che pompa momento angolare: c'e' un campo che si accende** | prova indipendente: **l'inerzia MEDIANA era incollata al floor `1e-6` fino al passo 250-350**, cioe' per meta' run il nodo tipico aveva **una regolarizzazione, non una massa**. E `ramp = min(1, eta/TAU_A)` (`:2321`) arriva al pieno a **~5000 passi**: a 500 il sistema ha vissuto **un decimo** della maturazione | `doc/REFERTO_scomposizione_L.md`. **La domanda «L_tot si conserva?» e' MAL POSTA adesso: si giudica A REGIME** |
| **C24** | **LA CRESCITA DI `L_tot` STA NELLA CODA, NON NEL NODO TIPICO — fatto nuovo, non spiegato.** `L_tot/n` cresce **x110-126** (OFF) e **x6.4-10** (ON), ma il prodotto delle MEDIANE non ci si avvicina: `I` mediana **x2.3-4.1**, `omega` mediano **x0.5-1.9**. Poche unita' con `I*|omega|` grandissimo dominano la somma | ed e' anche il motivo per cui la scomposizione con le MEDIANE **non chiude**: `L/n` e' una MEDIA, e le mediane non si moltiplicano per darla | `doc/REFERTO_scomposizione_L.md` §3, §5.1 |
| **C25** | **⚠ UN FILE NON SI PUO' COMMITTARE PER IL SUO ESSERE CRLF: git lo NORMALIZZA ENTRANDO.** Le tre copie `._sim.py` erano state committate come **prova** del file CRLF (442240 byte, sha1 grezzo `37c31630`); **il blob storato e' `08784685`, 435730 byte, ZERO CR** | **L'unico record superstite del `37c31630` e' il `.motivo.txt` scritto A MANO** — che si e' rivelato non un di piu' ma **l'unica prova possibile**. E le tre copie nel repo sono **identiche al riferimento**: non portano informazione | `doc/REFERTO_scomposizione_L.md` §5.3 |
| **C23** | **IL FATTORE `(CS_M/cs)^2` NON E' ~1 SULLA CODA a 500 passi.** La mediana resta `1.000020` — il nodo tipico non lo sente — ma il **massimo** arriva a **1.019** e il **6-16 % dei nodi** supera l'1 % | `frac>1%`: **0.000 al passo 50**, **0.057-0.161 al passo 500**; `cs_std/cs` da `6-9e-5` a `1.9-2.4e-3` (= 0.19-0.24 %, **coincide con C13**) | `doc/REFERTO_baseline_corretta.md` §2. **NB: avevo scritto «0.000» nella predizione del turbo citando uno smoke a 60 PASSI** — e' il presidio §9 sulle soglie prese dall'istante sbagliato, violato da me che l'ho scritto |
| **C26** | **⚠ `VERSO_CHI` E' UN NO-OP SILENZIOSO SOTTO `CHI_CORE`, che e' acceso in OGNI run.** `:2880-2884`: `if FRAME_DRAG and len(_tw_t): if CHI_CORE ...: ... elif VERSO_CHI ...:` — il ramo di `CHI_CORE` **arriva prima**, e `--chi-core` e' nella config base. **Accendere `--verso-chi` non aggiungerebbe NESSUNA legge** | `FRAME_DRAG = True`, quindi il blocco esterno non protegge: e' proprio l'`elif` a uccidere il ramo | **ESCE dalla lista delle accendibili.** Scritto qui perche' fra un mese non venga riacceso credendo di aver aggiunto qualcosa. `doc/REFERTO_faseA_sigilli.md` §3.1 |
| **C27** | **TRE FLAG VIVONO DENTRO `if VIRIALE:` E NON LO DICHIARANO.** `LS_AZIM` (`:3744`), `ZETA_VIR` (`:3739`) e `OLON_PART` (`:3732`), sotto `if VIRIALE:` (`:3724`). **Senza `--viriale` sarebbero no-op muti** | **⚠ E `ZETA_VIR` e' il caso PEGGIORE: e' condizionato solo A META'.** Ha un secondo uso (`:3115`, `:3152`) dentro `if VERLET:`, che e' acceso: **li' e' vivo anche senza `VIRIALE`**. Meta' dell'effetto sparirebbe in silenzio | `doc/REFERTO_faseA_sigilli.md` §3.2 |
| **C28** | **⚠⚠ 14 FLAG SU 16 NON HANNO ALCUN SIGILLO.** Tier 2 (metrica) e Tier 3 (chiralita'/segno) sono a **ZERO su dieci**. Passa solo `STEP2_OROLOGIO` (**9/10 + 1 FAIL ATTESO**: `S1` contro un blob di QUATTRO cambiamenti fa, con `max\|A-B\| = 0` e **nodi 3164 contro 2924** = mancanza di confronto; **`S2` PASSA** con 2924 = 2924) | **Un flag conta come TESTATO solo se un sigillo lo COMMUTA.** Essere nella `BASE` argv non e' un sigillo: `CAMPO_SPINORIALE`, `SPINORE_VIVO`, `VERLET`, `CHI_CORE`, `DEPARAM_OROLOGIO` compaiono in **nove** sigilli **senza mai esserne il soggetto** | **DECISIONE (Luca): UNO ALLA VOLTA.** Step 2 acceso, poi `TW_SPINORE` sigillato e acceso, test a variabile singola. *«Con ~15 leggi accese un cambiamento non e' attribuibile a una sola»*: **l'assenza di sigilli forza la metodologia migliore** |
| **C10** | **⚠⚠ LA BARRA D'ERRORE USATA FINORA E' TRE VOLTE TROPPO PICCOLA.** Su questo sistema **caotico** la pendenza trasversale cambia da run a run di **0.03 a codice INVARIATO**; la `SE` interna a un singolo run vale **~0.010**. **Il valore sotto ipotesi nulla di un confronto fra due run non e' zero** | dispersione fra semi **0.0302** (ramo pre) / **0.0286** (post) contro `SE` interna **~0.010** | `csv/_test_fork/_controllo_semi.txt` |

---

# D. APERTE — la struttura conta piu' dell'elenco

## D.1 — LA CATENA COLLO-DI-BOTTIGLIA (**un** fronte, non tre)

```
 A. cablare tau = d/cs      ->   B. la FORMA del dissipativo   ->   E. sotto-passi per lo spin
    SIGILLO FALLITO oggi          -omega/tau  (FRENA)                SOLO se dopo A e B resta
    (T2/T3/T4): flag OFF,         -lambda n x (n x B)  (ORIENTA)     un residuo modesto
    gate non ri-timbrato          ^ il "tira verso" mancante, ma
    misurato 129.5 -> 43.6          il coefficiente tornerebbe una
    giri/passo (atteso ~9)          SCELTA => manopola. Nodo aperto.
```

> **Questa catena BLOCCA tutto il resto.** Finche' lo spin e' aliasato **ogni misura su di esso e'
> nulla**, e le quattro voci di §B restano da rifare. **A e' il collo di bottiglia dell'intero
> programma.**

| # | stato | criterio di chiusura |
|---|---|---|
| **A** | `--tau-luce` cablato, **SIGILLO ANCORA FALLITO**. T2 resta da riscrivere (il monkeypatch colpiva il metodo **condiviso**). T3 **rifatto oggi** dopo la cura della cache (C7), e poi **su 3 semi**: ON post medio **-0.4745** contro l'attesa **-0.69**, divario **-0.2155** a **z = 13.1**, e `theta` resta a **30.7-44.9 giri/passo**. *(La lettura intermedia «un sesto recuperato» e' stata **RITIRATA**: era dispersione di run — C10.)* `doc/SIGILLO_tau_luce_FALLITO.md`, `doc/FIX_cache_cs.md` §6 | T1-T5 tutti PASS, **e** `theta` sotto il tetto `2π·cs/λ` | **⚠ MARCATA IL 2026-09-17 — VERDETTO DA RIGIRARE PRIMA DI ESSERE CITATO:** il FAIL è del blob `7d484580` (15 set); da allora **14 commit** e **sette correzioni di legge sigillate**, e `cs_std/cs` è passato da **0.0079 %** a **11 %** — cioè il sigillo fu preso quando `d/cs` era di fatto `∝ d`. **Ma i tre FAIL non hanno la stessa natura: T2 è un difetto del TEST** (monkeypatch sul metodo condiviso) **e il rigiro non può cambiarlo**; T4 dipendeva dalla cache `_cs_nodo_prev`, **poi CURATA** (C7). → `doc/SIGILLO_tau_luce_FALLITO.md` **✅ RIGIRATO il 2026-09-17** (`doc/REFERTO_rigiro_tau_luce.md`): **T4 da FAIL a PASS** (`cs→2cs` ora **`0.500000`** esatto: la cura C7 ha funzionato, `tau = d/cs` **e' una LEGGE**); **T2 FAIL per la STESSA ragione** (difetto del test, non del codice); **T1 da PASS a FAIL** perche' il suo **riferimento e' di 15 commit fa** — **criterio SCADUTO, non regressione**; **T3 FAIL per una RAGIONE DIVERSA: l'effetto ha CAMBIATO SEGNO** (allora ON si allontanava da zero di 0.258, oggi si **avvicina** di 0.456). **L'ostacolo non cade, ma non regge piu' per le ragioni di allora.**
| **B** | **CHIUSA COME NON DERIVABILE** (2026-09-16). Il coefficiente di `−λ n×(n×B)` sarebbe una **scelta** (§3), e ora ci sono **TRE ragioni indipendenti** per cui non si deriva: ① il **FDT** licenzia un `λ` ~10⁴ volte troppo lento (`doc/ANALISI_gilbert_fdt.md`); ② la **FASE 1 LLG** ha mostrato che il punto di cablaggio proposto non produce allineamento e che la dinamica esistente orienta verso `−B` (`doc/FASE1_gilbert_llg.md`, commit `8fa052c`); ③ **NON ESISTE UN BILANCIO da cui farlo uscire** — lo spin e' **accoppiato ma senza grandezza conservata**: il torque `cross(B_i, nb_i)` **non e' azione-reazione** (normalizzazione per `deg_i` diversa fra i due estremi, e la riflessione `z→−z` sui legami omochirali rompe l'antisimmetria **nella struttura**), e nel file **non esiste nessuna funzione di energia totale** (`doc/MAPPA_accoppiamenti_spin.md`). **NB: il terzo argomento e' DIMOSTRATO dalla formula, l'AMPIEZZA della violazione non e' misurata** | riaprirebbe **solo** una derivazione del coefficiente da un principio, cioe' la voce **L**. **Misura che manca e costa poco:** `|Σᵢ cross(Bᵢ,nbᵢ)| / Σᵢ |cross(Bᵢ,nbᵢ)|` — `~1/√n` = rumore di somma, `O(1)` = violazione grande |
| **E** | **non aperto**, e **non va aperto ora**: risolverebbe i giri **senza abbassare `omega`**, cioe' integrerebbe bene una rotazione che **viola il tetto di causalita' del modello**. Il fine non e' risolvere `omega`: e' **farlo rientrare nel tetto** | solo dopo A e B, e solo per un residuo modesto |

## D.2 — CANALI MAI TESTATI (**nessuna misura li esclude**)

| # | fronte | perche' e' aperto | criterio di chiusura |
|---|---|---|---|
| **C** | il fattore **`cs^−2` mancante nell'inerzia** ⇒ `cs` entrerebbe nello spin **via la MASSA**, non via l'orologio | esito **(b)** di `doc/INERZIA_tempo_quadro.md`: il fattore **manca nel codice** (zero occorrenze di `cs` in tutto il percorso che costruisce `Psi`). Le sei misure riguardano **solo** lo Step 2, che e' fase globale | cablarlo con flag+sigilli **e** misurare a densita' dove `cs ≠ CS_M` |
| **I** | **correlazione genealogica** (parentela → spin) | l'albero e' **ricostruibile** (FASE A del bilancio, 100% su 7/7 e poi 4163 coppie). **Misura mai fatta:** `<n_i·n_j>` per **grado di parentela**, non per distanza spaziale | l'osservabile esiste gia' (osservatore sigillato): serve solo farla |
| **R** | **REFUTATA (2026-09-16).** L'anello avrebbe dovuto far cambiare `sigma` al cambiare di `tau`. **Cambiando `tau` di DUE ORDINI DI GRANDEZZA** (pendenza **+1.708** OFF -> **+0.020** ON) **`sigma` NON SI MUOVE: -1.0545 contro -1.0558, differenza 0.0013 — VENTITRE VOLTE sotto la barra di sistema (0.030, C10).** E l'attesa ricalcolata **peggiora** il divario invece di chiuderlo (ON **+0.386**, IC95 [+0.054, +0.718], **esclude lo zero**). **Non cerco un colpevole nuovo (P1)** | chiusa come meccanismo. **Resta NON SPIEGATO il residuo positivo** in entrambi i bracci (**+0.126** OFF, **+0.386** ON) -> voce **R2** |
| **R2** | **NUOVO: il residuo della catena `theta = sigma + tau/2` non e' spiegato, E TENSIONE CON C3.** C3 lo dava **transitorio** (`0.979 -> 0.010` fra i passi 50 e 400); a **500 passi** vale **+0.126** (OFF) e **+0.386** (ON). **⚠ PRIMA DI TOCCARLO va sciolto il §0 del referto: `theta` e' misurato in DUE CONVENZIONI DIVERSE** (`\|omega\|*DT` in `_rimisura_t3.py`, `\|omega\|*dt_n` in MISURA F). Puo' essere tutta li' | rimisurare **entrambe** le convenzioni nello stesso run: costa **una riga** (`g` contiene gia' `om_src` e `dtn`), **zero run nuovi** |
| **S** | **CHIUSA COME RUMORE (2026-09-16), per il criterio scritto prima.** 4 semi per braccio: OFF media **89.872** sd 0.094 **IC95 [89.722, 90.022]**; ON media **90.049** sd 0.034 **IC95 [89.995, 90.103]**. **Entrambi CONTENGONO 90** -> l'ipotesi si RITIRA, non si raffina. E il segno concorde non regge nemmeno come indizio: `OFF s3` vale **89.9999**, il null esatto | chiusa. `doc/REFERTO_S_R_FDT.md` §1 |
| **S2** | **NON SI RIPRODUCE sul sistema corretto, E CAMBIA SEGNO** (2026-09-16, baseline `c57800c1`). Il contrasto `chi` ON-OFF era **+0.1769** e ora vale **-0.146**: il braccio **OFF** si e' spostato da **89.872** (4 semi, sd 0.094) a **90.130** (2 semi), cioe' **+0.26 gradi = 2.7 volte** la sua dispersione fra semi. **E' il cambiamento piu' grande prodotto dalle tre correzioni.** **⚠ E NON si dichiara una firma nell'altro verso:** l'IC95 di OFF esclude 90 ma i due semi concordano a **0.004 gradi**, e con **1 grado di liberta'** una coincidenza fra due punti da' un intervallo **spuriamente stretto** | **>= 4 semi per braccio.** Cio' che e' stabilito e' solo che il `+0.177` **non si riproduce** |
| **P** | **la cura della cache ha un effetto fisico?** — **INDECIDIBILE con 3 semi** | `Delta` per seme: **-0.0445 / +0.0367 / -0.0635** → **segno NON concorde**. `media -0.0238`, `SE 0.0307`, `t = -0.77`. **`IC95 = [-0.156, +0.108]` contiene lo ZERO *e* contiene il `-0.120` dell'ipotesi «~meta'» del mandato.** Nessuna delle due e' esclusa: l'esperimento **non ha potenza** | piu' semi (~12 per una `SE` di 0.015), **oppure** una misura che non passi da un confronto fra traiettorie caotiche |
| **J** | **quantizzazione delle masse**: esistono taglie preferite? | `massa_critica_collasso()` e' una **soglia**, e le soglie **selezionano** | istogramma delle masse, coi **picchi verificati al cambio di binning** (un picco che sparisce non e' un picco) |

| **W** | **`TW_SPINORE`: la legge CODIFICATA non e' quella DICHIARATA** - misurato 2026-09-16 | Il commento dice «pilota il Bloch di `tw/2`» (un **ANGOLO**); il codice (`:2149-2154`) somma `tw/(4pi)` a `omega_new`, che e' una **VELOCITA'** (`theta = |omega|*dt`, `:2210`). L'angolo effettivo e' **1.564e-03 rad/passo** contro i **9.827e-01** dichiarati: **fattore 628.3 = 2pi/DT**. E il termine finisce in **`self.omega_s`** (`:2313`), la **memoria**, mentre il commento di `SYNC_SPINORE` (`:724`) dice che metterlo li' «darebbe accumulo/divergenza». Unico del blocco, **non e' diviso per l'inerzia**. Peso in ampiezza **0.054 %** (equilibrio 3.910e+01 contro mediana di `|omega_s|` 7.239e+04) - **ma l'ampiezza NON decide**: l'asse TW e' **fisso e persistente**, `omega` e' un random walk (l'errore ampiezza-contro-correlazione, §9, da non rifare al contrario). Controllo positivo **debole**: ON/OFF divergono (2849 -> 3047 nodi, +6.9 %) **ma con shape diverse**, quindi «che cambia», non «di quanto» | **DECISO da Luca il 2026-09-16: RESTA SPENTO** (strada 3 delle tre; le altre erano *correggere il cablaggio*, che ne farebbe una **correzione di difetto senza flag**, e *sigillare cio' che fa davvero*). **Il fronte NON si chiude: resta aperto come DIFETTO DI CABLAGGIO**, e il suo criterio di chiusura e' **una decisione di fisica su quale delle due leggi sia quella voluta** — non una misura in piu'. **Presidio in `CLAUDE.md` §9 perche' nessuno lo riaccenda credendo di aggiungere `tw/2`.** **NON e' stato scritto un sigillo**: verrebbe dalla descrizione, non dal codice - il quarto criterio stale in due giorni. `doc/REFERTO_tw_spinore.md` |

## D.3 — SETTORI INTERI MAI INTERROGATI

| # | fronte | nota |
|---|---|---|
| **G** | **metrica / densita'** col rigore dato allo spin | pista indicata da Luca: e' li' che sarebbe avvenuto il **salto del raggio 0.8 → 13.2** mentre le firme di spin restavano piatte. **NON verificato in questo giro**: i due numeri non sono stati ricontrollati dai dati, e vanno confermati prima di usarli |
| **F** | il canale del **SEGNO** (doppia copertura) | **ATTENZIONE, e' un limite strutturale:** e' **`Z2`, abeliano**. Riguarda materia/antimateria e la carica, **NON puo' produrre la struttura non-abeliana del fork.** Aprirlo sperando che dia il non-abeliano sarebbe un errore di categoria |

| **X1** | **`TEMPO_PROPRIO_ORIENTATO`: il principio e' giusto, ma `dt_n < 0` rompe TRE consumatori** | Con `f = signed` (`:1925`) `r` puo' essere **negativo** (`r = x/sqrt(1+x^2)`, `x < 0`), quindi `dt_n = DT*r < 0`. **Enumerati i consumatori dal codice:** (a) **`:2143`** `omega_new = omega_src + dtn_c*(... - omega_src/_tau)` -> con `dtn_c < 0` il termine dissipativo **AMPLIFICA invece di smorzare**; (b) **`:2661`** `alpha = 1 - exp(-dt_n/tau)` -> **`alpha < 0`**, e lo slerp **estrapola via** invece di rilassare verso; (c) **`:2834`** `self.eta += dt_n` -> **l'ETA' DIMINUISCE**, e `eta` pilota `ramp = min(1, eta/TAU_A)`. **PROTETTO invece:** (d) **`:1993`** il rumore OU usa **`np.abs(_dtl)`**. **BENIGNO:** (e) `:2298` la rotazione inverte il verso — ed e' proprio cio' che si vuole. **⚠ E IL FATTO NUOVO:** `--tempo-segno` mette il segno in **`dt_n_s`**, una variabile **separata** (`:2829-2833`), cosi' `eta` e la geometria restano su `dt_n` **non firmato**; `--tempo-proprio-orientato` lo mette **dentro `r`**, quindi **dentro `dt_n` stesso**, e il segno **dilaga** su tutti i consumatori. **Due meccanismi per la stessa idea, con architetture opposte** | **NON e' escluso**: il principio (il tempo proprio orientato distingue materia e antimateria) e' buono. Si chiude quando (a), (b) e (c) sono trattati **esplicitamente** — p.es. col pattern di `--tempo-segno`: segno in una variabile separata, `|dt_n|` dove serve una durata. **Serve anche l'osservabile «frazione di nodi con `r < 0`»**, che oggi varrebbe identicamente zero |
| **X2** | **`ZETA_LOC`: «smorzamento locale» che dipende da una statistica GLOBALE, e ha un PUNTO FISSO** | `med_rho = max(float(np.median(rho)), 1e-9)` (**`:3136`**) e' la mediana su **tutto** il sistema, poi `eccesso = max(rho/med_rho - 1, 0)` e `zeta_loc = ZETA_M/(1+eccesso)`. **Due difetti distinti:** (1) **non-localita'** — lo smorzamento di un arco dipende dalla densita' dell'intero sistema (§4); (2) **P4, ed e' il piu' insidioso:** per il nodo **mediano** `eccesso = 0` **sempre**, a qualunque maturazione, quindi `zeta_loc = ZETA_M` **per costruzione**. **✅ MISURATO il 2026-09-17** (`csv/_test_fork/_analisi7_assiomi.txt`, 4 semi): la frazione di archi con `eccesso` **ESATTAMENTE 0** vale **50.00 %** su **tutti e quattro** i semi, e `zeta_loc` mediano vale **1.0000 = ZETA_M** su tutti e quattro. **Non e' una misura, e' la definizione di mediana** — ed e' esattamente cio' che la voce prevedeva. **⚠ MA IL PARAGONE CON `_tau` NON REGGE PIU', e va corretto qui perche' e' scritto qui:** `_tau` **NON** ha il punto fisso (`CLAUDE.md` §9, voce corretta il 2026-09-17), perche' `_dens_rif` e' la mediana di un **SOTTOINSIEME** (`_dens > 1e-6`, il 78-88 %), mentre `med_rho` di `ZETA_LOC` e' la mediana **dell'insieme intero**. **Due forme che sembravano gemelle non lo sono: una ha il punto fisso, l'altra no, e la differenza e' UN FILTRO.** | **il difetto e' DORMIENTE: `ZETA_LOC` e' OFF di default (`:250`), quindi nessun run committato ne risente.** Si chiude quando il flag viene o corretto (scala **locale**, non `median(rho)` globale) o dichiarato esperimento permanente. **Il test di C12 NON serve piu': l'identita' e' stata verificata direttamente** |
| **X3** | **AUDIT DI LETTURA DELLE LEGGI — registrato** (2026-09-16) | Nove leggi **escluse per DIMOSTRAZIONE** (non per misura), otto **confermate sane come costruzione**, **due motivazioni della tabella proposta CORRETTE** (`SYNC_UPDATE` non accende lo scuotimento; `LS_AZIM` non usa un asse di laboratorio ma un **baricentro globale** e un **indice cablato**). Il codice delle escluse **non si cancella**: e' l'evidenza che spiega i loro sostituti | non e' un fronte da chiudere: e' uno **stato**. Si aggiorna quando una legge cambia categoria. -> `doc/COMPONENTI_PROMOSSE.md` sezione **E** |

| **Y1** | **Il settore U(1) non ha un'osservabile dell'OROLOGIO** | Fra le **256** colonne dell'osservatore **nessuna** contiene `clk`/`orolog`/`fase`/`phc`: si misurano le **conseguenze** di `omega_clk` su spin e segno, **mai `omega_clk`**. Quindi il nullo della campagna Step 2 dice *«cio' che sappiamo misurare non se ne accorge»*, **non** *«l'orologio non e' cambiato»* — ed e' cambiato (**10.8 %** dei nodi con `\|fatt_cs-1\| > 1 %`) | una colonna con **mediana e dispersione di `\|omega_clk\|`**: una riga di scrittura nell'osservatore, **zero fisica**. Finche' manca, ogni verdetto sul settore EM e' indiretto |
| **Y2** | **Due osservabili U(1) hanno il nullo SBAGLIATO o NON VERIFICATO, e una e' scritta nei CSV** | (a) `u1_segno_ov_nullo` scrive **`2/pi = 0.6366`**, ma `canon` viene da `_nb_grav()` — una direzione **diversa** dal Bloch del nodo — quindi il nullo e' **`(2/3)*(2/pi) = 0.42441`** (Monte Carlo 4e6: 0.424474). Col nullo giusto il misurato **0.4254 +- 0.0105** e' **il caso**, non una deviazione. **Il valore sbagliato e' nei CSV gia' scritti e non lo acquisiranno** (§9). (b) `u1_verso_arco_coer` ha nullo **0 NON verificato**: `nb_grav` e' una **media di vicinato**, quindi due nodi adiacenti sono correlati **per costruzione** | (a) correggere la costante nell'osservatore **e annotare** quali CSV portano il vecchio valore; (b) **misurare** il nullo confrontando coppie d'arco con coppie **CASUALI** dello stesso run. Finche' non e' fatto, `u1_verso_arco_coer` **resta muta** |

## D.4 — IGIENE / INFRASTRUTTURA

| # | fronte | stato **verificato oggi** | criterio |
|---|---|---|---|
| **H** | **purezza dei diagnostici**: `calcola_psi()` **scrive** `self.psi` ed e' chiamata da `circolazione_topologica` (~1467), `diagnostica` (~5448), `_picchi_nuovi` (~6184) | **⚠ PIU' STRETTO DI COME E' STATO POSTO.** Il **percorso batch e' GIA' PROTETTO**: snapshot/restore espliciti di `psi`, `_psi_prec`, `_psi_spinor`, `_nb`, `psi_spin`… **e dello stato RNG**, alle righe **6300-6319** (diaglog) e **6357-6419** (condensazione) — verificato dal sorgente. **Tutte le misure di questo programma vengono da li'.** Il rischio resta sul percorso **GUI/video** (`update()`), non protetto. **AGGIUNTO 2026-09-15:** sullo stesso percorso GUI c'e' la **terza via di crescita dei nodi**, `semina()` (`:1600`), che **non** passa da `_eredita_spinore_figli` e quindi **lascia la cache `_cs_nodo_prev` corta** anche dopo la cura (C7). In batch e' inerte: `semina_cont=False` di default, si accende **solo** dalla GUI (`:4610`, `:4753`) | test binario **sul percorso GUI**: run con/senza diagnostiche → byte-identico? **e** contatore `_cs_fallback` a zero con `semina_cont` acceso |
| **K** | **merge in `main`** (fork-su2 come linea unica) | **PIANO SCRITTO, NON ESEGUITO** (2026-09-16, `doc/PIANO_merge_main.md`). **Il fatto che cambia il quadro: `main` NON HA MAI TOCCATO `soliton_simulator.py`** — il suo blob e' quello della merge-base (`194a9456`), i suoi 20 commit sono **tutta documentazione**. Non e' un merge di codice, e' un merge di **racconto**: conflitti **solo** su `CLAUDE.md` e `CLAUDECONNECT.md` (prova a secco con `git merge-tree`). Dei tre presidi: il **rename dello STATO** e' **gia' risolto** (`main` non ha NESSUN file di STATO; quello a rischio e' di `dev-spinoriale`, e solo se si cancella quel branch); l'**AVVISO** e' **chiuso** oggi; il **ri-timbro del gate NON e' risolvibile** finche' A e' aperta. **Trappola operativa vera:** `main` e' estratto in un **worktree separato** (`C:/Users/lpeano/st_main`). **RACCOMANDAZIONE: non adesso** — `main` e' oggi l'unico branch il cui codice coincide con un blob certificato, e portarci `08784685` significherebbe perdere l'ultimo punto fermo mentre A e' aperta | **decisione di Luca.** Il costo-zero che toglie il 90 % del danno: una riga in testa a `CLAUDE.md` **di `main`** che dica «branch FERMO al 2026-09-10, il lavoro vivo e' su `fork-su2`» |
| **M** | **ottimizzazione** | **due cose da NON fare, gia' accertate:** il loop CFL **non e' il collo** (`doc/PROFILAZIONE_costo_run.md`); `_pesi` e' una **ricorsione**, non 16 ricalcoli (`doc/REPERTO_pesi_ricorsione.md`). **Resta il costo, non la cura** | una misura che mostri un collo **diverso** da quelli gia' esclusi | **✅ RICONFERMATO il 2026-09-17** (`doc/REFERTO_calcpsi_tempo1.md`): misurato di nuovo, **stessi numeri** — `calcola_psi` **13.3 %** (era 12.8 %), `stato_crossover` **80.5 %** (era 80.9 %), profondita' di annidamento **2**. **E ha di nuovo fermato una premessa**: un mandato assumeva che le 16 chiamate fossero *«sedici violazioni»* di `calcola_psi`; **sono DUE.**
| **D** | rumore **bianco senza taglio spettrale** (catastrofe UV discreta) | **CABLATO il 2026-09-16 dietro `--rumore-colorato`, FLAG OFF** (blob `08784685` -> `c5e5088c` -> `a467fd9a`), dopo il via libera di Luca sulla predizione CORRETTA. OU con `tau_c = LAM/CS_M` = 0.400 = **40 passi**, `dt_n = DT*r` (non `DT`), **`amp` INVARIATA**, zero coefficienti. **SIGILLO A META': N1.0, N1.0b, N1 (`max\|A-B\| = 0.000e+00`, nodi 3020 = 3020) e N1b PASS; poi SI E' SCHIANTATO** a N7 (`AttributeError: Generator.normal is read-only`): **N2, N3, N6 e N7 NON sono girati**. Corretta la spia (proxy sull'OGGETTO `rng`), da rigirare. **Difetto trovato leggendo il chiamante:** `_passo_spinoriale` riceve `dt_n_s`, che sotto `--tempo-segno` e' **NEGATIVO** per l'antimateria -> `a > 1` -> la ricorsione **sarebbe divergita in silenzio**; da qui la guardia `\|dt_n\|`. **E il criterio di N3b era MIO ed era sbagliato:** «fallback <= 1» in assoluto avrebbe fatto fallire il sigillo per una ragione **legittima** (l'estensione scatta su `semina`/`nuova_massa`, la voce **H**, dove e' CORRETTA: i nodi esistenti conservano `xi`, solo i nuovi ricevono un'estrazione stazionaria). Corretto in **DELTA** sui passi di sola mitosi | **N7 e' quello che decide** (la ricorsione usa `dt_n` per nodo? col `DT` nudo N1..N6 passerebbero IDENTICI — e' il bug dello Strato 1 che solo S7 stano'), poi la campagna `{OFF, ON} x >= 2 semi`. **PREDIZIONE INVARIATA:** `theta` non scende, e se si muove SALE (<= 5 %). Un calo significativo sarebbe un reperto **a mio carico** | `doc/PREDIZIONE_taglio_spettrale.md`, `csv/_seal_fork/_sigillo_rumore_colorato.txt` |
| **N** | `python soliton_simulator.py --help` e' **rotto** (`%` non escapato in una help string) | **preesistente**, verificato sulla copia pre-modifica: non introdotto da noi | l'help si stampa |

| **Z1** | **`inerzia`: la correzione (1) NON e' stata cablata — il gate che la autorizzava aveva misurato UN'ALTRA GRANDEZZA** (2026-09-17) | La forma proposta e' `(rho_sorgente/peq_nodo) * (d_nodo/cs_nodo)^2`. **Ma `_rho_sorgente()` NON restituisce `\|psi\|^2` quando `CAMPO_SPINORIALE` e' ON** — e lo e' in **tutti** i run del fork: restituisce `rho_spin`, la norma del campo **EMESSO**. Il denominatore `peq` insegue invece `rho = 0.5*(I[i]+I[j])` con `I = \|psi\|^2` (`:3115`). **Due campi diversi:** correlazione **0.80** su 4 semi, rapporto da **0.005** a **8.8** fra p05 e p95, massimo **7684**. **GATE A** (commit `31922ff`) aveva misurato `I/peq_nodo`, cioe' col numeratore **giusto** e quindi **un'altra grandezza**. **E togliendo il pavimento `1e-6` senza metterne uno DERIVATO l'inerzia non «puo'» annullarsi: SI ANNULLA** — `min = 0` esatto su **6 nodi (0.045 %)**, dinamica max/min da **5957** a **1.4e+301**. *(La frazione non e' l'argomento: U7b e' stato bloccante allo 0.11 %.)* | **DECISIONE DI LUCA, due strade, nessuna delle quali e' una bonifica:** **(A)** allineare le popolazioni con `(\|psi\|^2/peq_nodo)*(d/cs)^2` — ma l'inerzia **esce dalla FASE 2** (il campo emesso), cioe' **cambio di modello**; **(B)** costruire un `peq_spin`, lo sfondo del campo emesso, che **oggi non esiste** — cioe' **legge nuova**. `csv/_test_fork/_verifica_inerzia1.txt` |
| **Z1-bis** | **⚠ AGGIORNAMENTO di Z1 (2026-09-17): UNA MIA OBIEZIONE CADE, L'ALTRA VA RINOMINATA — e il blocco vero e' un ALTRO** | **(a) CADE**: avevo scritto che togliendo il pavimento `1e-6` l'inerzia **si annulla** (`min = 0` su 6 nodi). Il mandato di ① **NON toglie il pavimento** — lo dichiara e ne da' la ragione (`omega = coppia/inerzia`: inerzia minore = omega **maggiore**). **Contro questa proposta l'obiezione non si applica.** **(b) VA RINOMINATA**: avevo chiamato «errore di POPOLAZIONE (A3)» il fatto che `rho_sorgente` sia `rho_spin` e `peq` insegua `\|psi\|^2`. **Non e' A3**: dopo la proiezione arco→nodo le due grandezze vivono **entrambe sui nodi**, e su questo il mandato ha ragione. E' una questione di **COERENZA DI GRANDEZZA**, piu' debole di come l'avevo scritta. *(Correlazione `rho_sorgente` ↔ `\|psi\|^2` misurata per passo: **1.000 / 1.000 / 0.992 / 0.487 / 0.612 / 0.683** — coincidono all'inizio, poi divergono.)* **(c) IL BLOCCO VERO E' UN ALTRO**, e non lo avevo visto: `_passo_spinoriale` (`:3062`) gira **PRIMA** della calibrazione di `peq` (`:3147`), e ai passi **0 e 1** `peq_nodo` e' **zero su TUTTI i nodi** → `rho/peq` = `0/0` = **NaN**, che finirebbe in **`omega_s`, la memoria persistente**. **E il pavimento non protegge:** `np.maximum(NaN, 1e-6)` **e' NaN** | **decisione di Luca**, tre vie in `doc/REFERTO_inerzia_transitorio.md` §4. **La diagnosi dimensionale del mandato e' invece CONFERMATA e piu' forte del previsto: pavimento attivo sul 100.00 % (non 99.7 %), `inerzia` = `1e-6` ESATTO ad ogni passo, `T^2/inerzia = 1.03e+06`** |
| **Z6** | **IL TRANSITORIO DI ACCENSIONE E' UN BLOCCO STRUTTURALE — ha fermato DUE correzioni** (2026-09-17) | `psi` **non viene calcolato prima del primo `step`**, quindi ai passi 0-1 **ogni grandezza derivata e' zero**: `rho`, `peq`, e ogni **rapporto fra grandezze di stato**. Ha fermato **`peq` alla nascita** (la cura avrebbe scritto `0` al posto di `NaN`) **e ① `inerzia`** (`rho/peq` = `0/0` = NaN in `omega_s`). **Non e' piu' «due passi su sessanta, irrilevante»: e' il muro contro cui si ferma la bonifica** | **decisione di Luca.** La via che sblocca **entrambe**: **calcolare `psi` una volta alla costruzione della scena**, prima del primo `step`. **Ma e' un cambio del percorso di inizializzazione — non una bonifica — e cambia il seme di ogni run esistente.** Le alternative (convenzione «rapporto = 1 senza sfondo» → `inerzia = T^2`; oppure escludere il transitorio con un ramo contato) sono in `doc/REFERTO_inerzia_transitorio.md` §4 |
| **Z7** | **`_fatt_cs_ultimo` e' SCRITTO e MAI LETTO — quarto caso della stessa famiglia** (2026-09-17) | `:2123` `self._fatt_cs_ultimo = _fatt_cs   # per la metrica (solo lettura a valle)`. **A valle non c'e' nessuno:** una sola occorrenza nel codice, ed e' la scrittura. **Quarto caso dopo `_passo_spinoriale` (docstring «ORFANO» ma VIVO), `VERSO_CHI` (cablato ma MUTO) e `spin_locale` (definita e MAI CHIAMATA): lo stato di vita del codice non e' leggibile dal codice** | **conseguenza operativa gia' utile**: togliere `_fatt_cs` dall'inerzia (necessario in ①, o si avrebbe `cs^-4`) **non rompe nessun consumatore**. Si chiude quando ① viene cablata, dichiarando `_fatt_cs_ultimo` come diagnostico |
| **Z1-ter** | **✅ Z1 CHIUSA: ① CABLATA** (2026-09-17, sigillo Y 11/11) | La forma cablata e' `max((rho_sorgente/peq_nodo)*(d/cs)^2, 1e-6)`. **Il blocco si e' sciolto con A6, non con una rete:** l'inerzia si valuta sullo stato **precedente**, e a `:3137` `peq`/`cs`/`d` sono **gia'** quelli del passo prima (aggiornati a `:3222`/`:3213`/`:3318`). **Il pavimento `1e-6` passa dal 100.00 % allo 0.3457 %** senza essere toccato. La questione della **coerenza di grandezza** (`rho_spin` contro `\|psi\|^2`) **resta dichiarata e NON risolta**: la correzione e' stata cablata **con quella riserva scritta**, non perche' sia caduta | **resta aperto** il solo punto della coerenza di grandezza: si chiuderebbe con un `peq_spin` (lo sfondo del campo emesso), che **oggi non esiste** ed e' una **legge nuova**, non una bonifica. -> `doc/COMPONENTI_PROMOSSE.md` sezione **G** |
| **Z9** | **IL KERNEL NON HA MAI FINITO DI ACCENDERSI: `ramp` matura in ~5526 passi, i run sono 300-500** (2026-09-17) | `_pesi()` costruisce `base = exp(-d/lam) * ramp[i] * ramp[j]` con `ramp = min(1, eta/TAU_A)`. `eta` e' il **tempo proprio** e cresce di **~0.009 per passo**, `TAU_A = 50` -> **per `ramp = 1` servono ~5526 passi**. Misurato: `ramp` mediano **0.0002** al passo 1, **0.0106** al 60, **0.0217** al 120; a 500 passi vale **~0.09**. E poiche' `base ∝ ramp[i]·ramp[j]`, **il peso d'arco tipico e' ~1 % di quello maturo**. **NON E' UN DIFETTO — `ramp` e' una LEGGE** (la maturazione dell'arco col tempo proprio), non una regolarizzazione. **Ma e' una condizione di REGIME mai dichiarata.** *(E' anche la vera ragione per cui `psi = 0` alla costruzione della scena: non manca l'inizializzazione, `calcola_psi()` E' gia' chiamato via `_registra_concorrenza`. Controprova: forzando `eta = TAU_A`, `max\|psi\| = 8.02`.)* | **LE CONSEGUENZE NON SONO STATE MISURATE**, e dire *«quindi le misure sono sbagliate»* sarebbe l'errore di ampiezza-contro-correlazione gia' catalogato in §9. **Cio' che e' stabilito e' il regime, non il suo effetto.** Le vie (accorciare `TAU_A` per il ramp, disaccoppiare `ramp` dall'eta', o accettare il transitorio continuando a contarlo) sono **decisioni di regime**, e due su tre toccano una legge. -> `doc/REFERTO_psi_zero_ramp.md` | **✅ RIMISURATA sul blob `a8f1b2f4` il 2026-09-17** (`doc/REFERTO_rimisura_Z9.md`): **SOSTANZIALMENTE INTATTA.** Scena ORIGINALE (l'unica confrontabile): `ramp` mediano **0.0002 / 0.0102 / 0.0204** ai passi 1/60/120 contro **0.0002 / 0.0106 / 0.0217**; `eta` **0.00849/passo** contro ~0.009; **`ramp = 1` a ~5887** contro ~5526. Scena del BATCH (il numero valido oggi): **~5960**. **⚠ Lo scarto e' +6.5 % / +7.9 %, nella direzione «piu' lenta» — ma e' UN SOLO SEME, e P3 vieta di chiamarlo un effetto: la dispersione di `ramp` fra semi NON e' mai stata misurata.** **Il fatto non cambia: il kernel matura in ~5900 passi, i run sono 300-500, a 500 passi `ramp` sta sotto il 10 %.** **⚠ LA TESTIMONIANZA DI LUCA, registrata CON LA SUA QUALIFICA (2026-09-17):** *«quel parametro era stato settato cosi' in sessioni precedenti, **prima di creare il repository**, per non far esplodere tutto»*. **E' una TESTIMONIANZA RIFERITA, non un riscontro: precedente al branch, NON verificabile da git.** **Cosa git dice, verificato dal disco:** il primo commit che **aggiunge** `soliton_simulator.py` e' **`670310f`** (2026-08-28 — **non `ca02af0`**, che e' del 13 settembre ed e' «PEZZO 2 peso sin chi»); **`670310f^` NON ha il file**; **`TAU_A = 50` e' gia' li'** (riga 130), accanto al `2.0` marcato «canonico»; e **`git log -S "_TAU_A_REGIME = 50.0"` trova UN SOLO commit, quello.** **Nessun commit ha mai cambiato quel valore: la decisione e' PRECEDENTE AL REPOSITORY.** **E' un'ASSENZA DI PROVA, non una prova di assenza** — la testimonianza resta tale finche' un esperimento non la conferma o la smentisce. **ESPERIMENTO ESEGUITO il 2026-09-17** (`--tau-a 2.0`, flag OFF di default, S3 byte-identico committato PRIMA del run): **NON DIVERGE** — S1 5/5 PASS, zero NaN, `\|nb\|=1`, `_taup_cfl_max` 0.563 contro 0.564. **La testimonianza non e' confermata nella forma letterale. MA i segni di stress sono grossi:** `psi` max **x91** (0.064 -> 5.83), `d0` **x17.4**, `phivel` **x22.7**, **nodi finali -32 %** (2577 -> 1754). **«Regge» significa solo «l'aritmetica non produce NaN».** **E `Z9` sarebbe risolta:** `ramp` mediano da **0.0148 a 0.470**, maturita' a **~255 passi** invece di ~6000-8000 — **dentro la durata dei run**. **TERZA lettura: regge con stress. NON promosso, default invariato**, e `TAU_A=2.0` con `G_PH=3e-3` **non e' ne' il canonico ne' il deterministico: e' una TERZA combinazione mai validata**. **E l'esperimento ha mosso ENTRAMBE le leggi di `Z10`:** parte dello stress puo' venire dalla **memoria spinoriale**, non dalla maturazione — **distinguerle richiede la separazione che `Z10` chiede.** -> `doc/REFERTO_esperimento_tau_a.md`
| **Z9-bis** | **`TAU_A` GOVERNA DUE LEGGI, e la cura proposta ne tocca una terza gia' bocciata** (2026-09-17) | Mappa dal disco: `:2429` `ramp = min(1, eta/TAU_A)` (**maturazione del kernel**, il difetto Z9) e `:2226`/`:2229` `_tau = TAU_A*max(dens/dens_rif, 0.05)` (**vita media della MEMORIA SPINORIALE**, il rilassamento di `omega_s`). **Due tempi che non hanno nulla in comune se non il nome.** ⚠ **E la sostituzione `TAU_A -> tempo-luce` su `_tau` ESISTE GIA', dietro `--tau-luce` (`:2218-2222`), il cui SIGILLO E' FALLITO** (`doc/SIGILLO_tau_luce_FALLITO.md`, voce **A**, *«il collo di bottiglia del programma»*, la ragione per cui il gate resta indietro). Cablare `TAU_A = LAM/cs` farebbe **senza flag e senza sigillo** cio' che `--tau-luce` fa esplicitamente e **non ha superato il sigillo**. ⚠ **E `LAM` non e' «di stato»:** e' `0.8` a `:146`, spostabile con `--lam`. La formula corretta e' **«nessun parametro NUOVO»**, non «zero parametri». ⚠ **E il `git blame` NON conferma la «compensazione scaduta»:** entrambe le righe vengono da `670310fc`, messaggio generico | **decisione di regime, tre vie** in `doc/REFERTO_tau_a_due_leggi.md` §6. La piu' pulita: **separare le due leggi** (una scala distinta per la maturazione, `TAU_A` dov'e') — **ma separare significa stabilire che oggi due leggi condividono un numero per accidente, ed e' una decisione.** **Z9 RESTA APERTA e intatta:** la diagnosi sul `ramp` e' giusta, e' il VEICOLO della cura che non regge |
| **Z10** | **`TAU_A` E' UN SOLO NUMERO PER DUE LEGGI FISICHE DISTINTE — difetto di suo, indipendente da Z9 e da `--tau-luce`** (2026-09-17) | `:2429` `ramp = min(1, eta/TAU_A)` e' la **MATURAZIONE DEL KERNEL** (*quando un insieme di punti diventa un oggetto*); `:2226`/`:2229` `_tau = TAU_A*max(dens/dens_rif, 0.05)` e' la **VITA MEDIA DELLA MEMORIA SPINORIALE** (*quanto a lungo un nodo ricorda la propria rotazione*). **Non hanno ragione di coincidere.** Anche se `TAU_A` restasse un numero, **dovrebbero essere DUE numeri**: la loro coincidenza e' **accidentale, non derivata**. *(Il `git blame` conferma l'accidente: entrambe le righe vengono dallo stesso commit `670310fc`, messaggio generico.)* **Conseguenza operativa gia' vista:** qualunque modifica a `TAU_A` tocca **due meccanismi**, e il secondo regge `omega_eq ∝ sqrt(tau)` e il plateau di `\|omega_s\|` | **separare le due scale** — una per la maturazione, una per la memoria. **NON e' un cablaggio da fare al volo:** stabilire che due leggi finora unite sono indipendenti e' una **decisione di regime**. Si chiude quando la separazione e' decisa e sigillata. -> `doc/REFERTO_tau_a_due_leggi.md` |
| **Z11** | **RIGIRO DEI SIGILLI STORICI — lavoro PREVISTO, non ancora fatto** (2026-09-17) | Il rigiro di `--tau-luce` ha mostrato che **un sigillo preso su un sistema poi cambiato puo' dire il falso in ENTRAMBI i versi**: **T4 e' passato da FAIL a PASS** (il difetto che lo causava e' stato curato) e **T1 da PASS a FAIL** (il suo riferimento e' di 15 commit fa). **Lo stesso criterio va applicato a TUTTI i sigilli storici**: Strato 1 **25/27**, Step 2 **9/10**, rumore colorato **a meta'**, e i sigilli di questa bonifica quando il blob cambiera' ancora | **DA FARE A BONIFICA FINITA**, non ora (**registrato come lavoro previsto**, mandato del 2026-09-17). Criterio: per ogni sigillo storico, **il blob del riferimento e' ancora quello giusto?** e **la condizione fisica su cui il criterio poggiava e' ancora vera?** |
| **Z12** | **`_pesi()` gira SEDICI volte per passo, a cavallo della scrittura di `_cs_nodo_prev`: A6 sarebbe violato in modo PERMANENTE** (2026-09-17) | Misurato: a ogni passo `_pesi()` e' chiamata **16 volte — 9 PRIMA** che la cache sia aggiornata (`:3213`) **e 7 DOPO**. Se il `ramp` usasse `_tempo_luce_nodo`, **9 chiamate leggerebbero il `cs` del passo precedente (A6 ok) e 7 quello corrente (A6 violato)**: **il 44 %, a ogni passo, per sempre.** E il valore della maturazione dipenderebbe da **quale** delle sedici chiamate la calcola — un dettaglio di implementazione, non la fisica. **E come TERZO LETTORE della stessa cache, `_pesi()` legge il fallback al 7.8189 %** contro **0.0000 %** di `_tempo_luce_nodo` e **3.0303 %** di `_passo_spinoriale`: **tre lettori, tre frazioni.** *(I 19 fallback sono tutti al passo 0: la frazione alta viene dal NUMERO di chiamate, ed e' esattamente il punto di A8b — la frazione di un fallback non e' una proprieta' della cache, e' una proprieta' del CONSUMATORE.)* | **una decisione su una riga:** si aggiunge uno **snapshot per-passo** del tempo-luce nodale (calcolato una volta all'inizio di `step`, letto da tutte e sedici le chiamate)? Risolverebbe A6 **e** l'ordine **e** il costo. **Ma e' stato nuovo, e A8b impone di estenderlo ai CINQUE punti di crescita e di contarlo** — il prezzo gia' pagato due volte (`_cs_nodo_prev` **71.88 %**, `_psi_spin_prec` **95.33 %**). -> `doc/REFERTO_ramp_terzo_lettore.md` | **⚠ RIMISURATO il 2026-09-17: `R6` REGGE.** `_pesi()` gira ora **12 volte per passo** (era 16) — **ma ANCORA A CAVALLO**: **7 PRIMA** della scrittura di `_cs_nodo_prev` e **5 DOPO** (erano 9 e 7). **Valutare li' il `ramp` violerebbe A6 sul 42 % delle chiamate** (era 44 %). **E il quadro per chiamante spiega perche' il TEMPO 2 non poteva bastare:** `stato_crossover` **1354**, `step` **123**, `calcola_psi` **37** — **il TEMPO 2 agiva su `calcola_psi`, che e' il 2.4 % delle chiamate; l'89 % viene da `stato_crossover`** attraverso `massa_critica_adattiva` (la ricorsione indiretta della voce **M**). Il fallback di `_pesi()` come terzo lettore e' ora **1.19 %** (era 7.82 %).
| **Z13** | **`calcola_psi()` ricalcola i pesi in TUTTE le chiamate (100 %), e in DUE punti del passo produce LETTURE MISTE `t`/`t+1`** (2026-09-17) | `calcola_psi(self, w=None)` accetta `w`, **ma nessuno dei ~19 chiamanti lo passa**: `_calcpsi_w_none = 134 su 134`. **Dentro il passo sono TRE PUNTI NEL SORGENTE ma DUE A RUNTIME:** `:3006` e `:3118` **girano**, `:3037` e' il ramo **`elif`** di un `if`/`elif` **escluso da `REPULS_LEGGE = True`** (il commento lo chiama *«vecchia repulsione a parametro, fallback»*) — **ha lo stesso difetto ma nessun sigillo puo' testarlo**. Il difetto e' **flagrante**: a `:3006-3007` `psi_forces = self.calcola_psi()` (che ricalcola i pesi) viene moltiplicato per `self._mat(w)`, **il `w` di `step`** — **due insiemi di pesi diversi nella stessa espressione**, una riga sotto il commento (`:2960`) che lo vieta: *«Non ricalcolare psi in punti diversi del passo: quello introdurrebbe letture miste t/t+1»*. **Entrambi i rami sono presi perche' `SYNC_UPDATE` e' FALSE** in tutti i run del fork; **con `SYNC_UPDATE` ON il difetto non esiste** | **TEMPO 2 realizzabile, DUE righe:** passare `w`, che e' **gia' presente** a `:3007`. **Portata ridimensionata:** elimina **2** ricalcoli su 16 (**13 %**), non sedici — gli altri **13** vengono da `stato_crossover` (voce **M**) e sono un fronte diverso. **Sull'effetto NON c'e' previsione:** se i pesi coincidono il difetto e' **teorico**, ed e' il sigillo **Q4** a dirlo. Contatori gia' cablati e **byte-identici** (Q1 `0.000e+00`). -> `doc/REFERTO_calcpsi_tempo1.md` | **⚠ TEMPO 2 CABLATO il 2026-09-17 (2 chiamanti), sigilli propri 7/7 — MA Q8 FALLISCE: `Y5` dell'inerzia si ROMPE.** `Q4` ha risposto alla domanda aperta: i pesi ricalcolati **NON coincidevano**, il difetto era **reale** (nodi 1669 vs 1850). `Q5`: ricalcoli dentro il passo **-> 0**. **Ma il fallback dello sfondo dell'inerzia passa da «cessa all'invocazione 8» a «scatta in 57 passi su 60, fino all'ultima», e il pavimento risale da 0.35 % a 0.85 %.** Due letture possibili, e **si distinguono con UNA misura non ancora fatta**: quale condizione cade, `peq_nodo <= 0` o `rho_sorgente <= 0`. **Il blob attuale ha `Y5` ROSSO.** -> `doc/REFERTO_calcpsi_tempo2.md` **✅ CHIUSA il 2026-09-17: il TEMPO 2 E' CORRETTO.** La regressione che `Q8` segnalava era **un criterio scaduto**, non un danno: il TEMPO 2 ha **tolto uno sfasamento** (`Z18`), e `Y5` era scritto sul comportamento sfasato. **`Y5` RISCRITTO** (`csv/_seal_fork/_sigillo_Y5_riscritto.py`, **4/4 PASS**: grado max **2**, eta 17 volte minore, **ogni nodo UNA sola invocazione**), **vecchio `Y5` dichiarato SCADUTO — l'ottavo.** **Rigiro completo: V1 8/8, V2-V5 8/8, V6-V10 12/12, Y 10/10, Q0/Q3-Q8 7/7, Y5 4/4.** ⚠ **E il rigiro ha trovato il NONO criterio scaduto: `Q1`** — riferimento `69ee5403`, **prima del TEMPO 2**, che cambia le traiettorie per costruzione; `Q1b` avrebbe dato un **falso PASS** (`0.000e+00` su **0 array**) e **`Q1a` l'ha intercettato**. **Non riscritto: serve un mandato proprio.**
| **Z14** | **IL TERZO RAMO DI `calcola_psi` (`elif` sotto `REPULS_LEGGE`): DICHIARATO, non corretto** (2026-09-17) | `:3054` ha **lo stesso difetto di letture miste** di `:3013` (`calcola_psi()` ricalcola i pesi, la riga sotto usa `self._mat(w)`), **ma e' escluso da `REPULS_LEGGE = True`** (default). **NON e' codice morto** — a differenza di `spin_locale`, che nessun flag poteva accendere: **questo si accende spegnendo `REPULS_LEGGE`**, e rimuoverlo renderebbe quella configurazione **rotta, non diversa**. Non essendo eseguito, **nessun sigillo puo' verificarne la correzione**: cablarlo darebbe una verifica di **FORMA** spacciata per una di **COMPORTAMENTO** (la classe di `VERSO_CHI`) | **non e' un fronte da chiudere, e' un AVVISO PERMANENTE:** *«se qualcuno spegne `REPULS_LEGGE`, il difetto delle letture miste torna»*. Scritto **nel codice** accanto al ramo e qui. **REGOLA GENERALE che ne discende: un ramo sotto flag non si corregge e non si cancella — SI DICHIARA, perche' il difetto e' LATENTE, non assente.** |
| **Z15** | **14 `.pkl` su 36 non portano il BLOB del codice che li ha prodotti, e 1 non e' ricostruibile affatto** (2026-09-17) | Recupero retroattivo dalle fonti nel repo (blocco JSON dei `.cond.csv` + colonna `blob` dei `.vuoto.csv`): **21 RICOSTRUITI, 14 PARZIALI, 1 NON RICOSTRUIBILE**. I 14 parziali mancano **tutti della stessa cosa** — il blob — perche' sono **anteriori al cablaggio delle colonne P6**: si sa **cosa** e' stato lanciato, **non su quale codice**, e il blob e' cambiato **14 volte in tre giorni**. **`_stf_seed.pkl` non ha nemmeno il `.cond.csv`:** il file c'e', ma **come riproducibilita' e' gia' perso** | **non e' un fronte da chiudere con una misura: e' un DEBITO.** I 14 parziali diventerebbero ricostruibili solo **rilanciandoli** sul blob attuale — cioe' **rifacendo i dati**, non recuperandoli. **Da qui in avanti il debito non cresce**: la regola in `CLAUDE.md` (*«un `.pkl` senza il suo comando non e' un dato»*) impone nome, comando, blob, seme e passi **nello stesso commit** del run. -> `doc/INVENTARIO_strumenti.md` §5 |
| **Z16** | **`Y5` ROSSO: la causa e' `rho_sorgente <= 0`, NON `peq`. E nessuna delle due porte ipotizzate** (2026-09-17) | Strumentazione **byte-inerte** (`0.000e+00`, nodi 2025 = 2025) che separa le porte: **PORTA A (lunghezza) MAI**; **PORTA B (valore di `peq`) solo fino all'invocazione 8** — il transitorio — **e il suo conteggio (2392) e' ESATTAMENTE il fallback totale del blob PRE-TEMPO 2: la componente `peq` e' INVARIATA.** **La causa permanente e' la TERZA condizione di `_ok_n`: `rho_sorgente <= 0`, fino all'invocazione 66 su 2019 nodi-invocazione.** `rho_sorgente` e' `rho_spin`, il campo **EMESSO**: **il TEMPO 2 ha cambiato `psi` (Q4: 1669 nodi contro 1850) e ha prodotto nodi in cui il campo emesso SI ANNULLA**, dove prima non accadeva. Tutti i 3215 nodi in fallback **hanno archi** (0 senza) | **decisione di Luca, quattro vie** in `doc/REFERTO_Y5_due_porte.md` §4: rimisurare `Y5`; trattare `rho_sorgente <= 0` come caso a se'; revertire il TEMPO 2 (**ma Q4 ha dimostrato che quel difetto era REALE**); oppure — indipendente da `Y5` — far leggere all'inerzia **`_peq_t`** invece di `self.peq`. **NON RIPARATO: il mandato diceva di misurare.** | **✅ DECISO il 2026-09-17 (`doc/REFERTO_rho_zero_tempo2.md`): IL TEMPO 2 LI HA CREATI.** Confronto dei due blob con sonda **ESTERNA identica** (il vecchio non ha i contatori): **VECCHIO `9dfd91c4` = ZERO** nodi con `rho <= 0` in 66 invocazioni; **ATTUALE = 26 invocazioni, 488 nodi**. **Il sospetto `Z9` e' SMENTITO da tre misure:** (a) sono **ZERO ESATTO** (488/488), **zero denormali** — la maturazione lenta lascerebbe valori piccoli, non nulli; (b) il `ramp` dei loro **vicini** (0.0039742) e' quello di **tutti** (0.0038639); (c) `ramp` e' **identico** nei due blob. **CHI SONO: grado esattamente 2 e eta' 14 volte minore -> i FIGLI DELLA MITOSI**, al loro primo passo. **⚠ IL MECCANISMO NON E' NOTO e non lo invento:** l'ipotesi ovvia (`calcola_psi` ricalcolava i pesi e popolava `psi_spin` per i nodi nuovi) **non e' verificata**, e confligge con la verifica preliminare del TEMPO 2 (topologia invariata al 100 % **dentro** `step`, perche' la mitosi avviene **fuori**).
| **Z17** | **A6 nell'inerzia e' garantito dall'ORDINE, non dalla STRUTTURA** (2026-09-17, rilievo del guardiano verificato) | `_peq_t = self.peq.copy()` esiste (`:2986`) e **l'inerzia non lo usa**: legge `self.peq` (`:2161`). **Verificato dal codice: oggi e' EQUIVALENTE** — fra `:2986` e la chiamata a `:3217` nulla modifica `self.peq`, e la calibrazione (`:3302`) e la **diffusione** (`:3320`) vengono **dopo**. **Quindi il sospetto di violazione di A5 (leggere `peq` gia' diffuso di questo passo) e' FALSO**, e sostituire `self.peq` con `_peq_t` **non cambierebbe un bit**. **MA la garanzia viene dall'ORDINE:** se qualcuno spostasse `_passo_spinoriale` dopo `:3320`, **A6 si romperebbe IN SILENZIO**. ⚠ **E il parallelo con `_psi_spin_prec` (95.33 %) e `_cs_nodo_prev` (71.88 %) NON regge:** quelle erano **cache INERTI**, qui lo snapshot e' **EQUIVALENTE** — sono due cose diverse | **irrobustimento, non correzione:** far leggere `_peq_t` renderebbe A6 vero **per costruzione**. **Oggi sarebbe byte-identico**, quindi si decide a freddo. ⚠ `_peq_t` e' **locale a `step`**: non e' raggiungibile da `_passo_spinoriale` senza passarlo |
| **Z18** | **LO SFASAMENTO `eta`: per mesi il nodo appena nato ha pesato `~2e-4` invece di ZERO — e il TEMPO 2 lo ha TOLTO, non introdotto** (2026-09-17) | `:3018` `w = self._pesi(); self.eta += dt_n`: **`_pesi()` usa `eta` VECCHIA, poi `eta` viene incrementata.** Un figlio della mitosi ha `eta = 0` -> `ramp = min(1, eta/TAU_A)` = **ZERO ESATTO** -> **entrambi i suoi archi hanno peso zero** (`base ∝ ramp[i]·ramp[j]`) -> `psi = 0` -> `rho_sorgente = 0`. **MISURATO: `ramp` prima dell'incremento = 0 esatto su 31/31 (100 %); dopo = `2e-4`, mai zero (0/31); 62 archi di neonati su 62 con peso ESATTAMENTE zero.** **E nel blob VECCHIO `calcola_psi()` (`:3006`, `:3118`) RICALCOLAVA `_pesi()` DOPO l'incremento (`:2979`)**, quindi il neonato riceveva un peso minuscolo **ma non nullo** — confermato dalla misura indipendente: sul blob vecchio i nodi con `rho <= 0` erano **ZERO**. **QUINDI IL PESO ZERO E' L'INTENTO DICHIARATO** (CLAUDE.md §9 e A7b: *«un nodo appena nato non pesa ancora»*), **e il `2e-4` di prima era una PERDITA DI SINCRONIA, non una scelta di legge** | **`Y5` e' un CRITERIO SCADUTO — l'ottavo di questo giro — e va riscritto, MA CON UN MANDATO PROPRIO**: riscrivere un criterio e' la cosa su cui questo repo ha gia' sbagliato otto volte. **Il codice NON si tocca.** **E resta il fatto da non perdere: per tutto il tempo in cui quel codice e' girato, il neonato ha pesato `2e-4` invece di 0, e nessuno lo sapeva.** -> `doc/REFERTO_sfasamento_eta.md` |
| **Z19** | **QUARTA VOLTA: una grandezza letta in un momento del passo diverso da quello che il codice dichiara** (2026-09-17) | La famiglia, ora con quattro membri: **`_cs_nodo_prev`** (cache scartata a ogni mitosi, fallback **71.88 %**, `cs = CS_M` costante) · **`_psi_spin_prec`** (non estesa alla mitosi, **95.33 %**, FASE 5 inerte **per mesi**) · **i due `theta`** (C19, due convenzioni per lo stesso nome, numeri incomparabili) · **lo sfasamento `eta`** (`_pesi()` valutato a un `eta` diverso da quello del passo). **E' esattamente cio' che A6 chiede di escludere, qui col segno opposto:** non uno stato troppo nuovo, ma **due letture della stessa grandezza a istanti diversi** | **non e' un fronte da chiudere: e' un PATTERN da cercare.** La scansione sistematica — *quali altre grandezze sono lette in piu' punti del passo, e con quale stato?* — **non e' mai stata fatta**, e le quattro trovate finora sono emerse **tutte per caso**, indagando altro. Va registrata come lavoro previsto accanto a **Z11** (rigiro dei sigilli storici) |
| **Z20** | **UN DRIVER CHE FORZA UN FLAG IN TUTTI I BRACCI RENDE L'A/B NON ATTRIBUIBILE — e NESSUN messaggio lo dice** (2026-09-17, rilievo di Luca) | `csv/_test_fork/_esperimento_spin_feedback.py`: `--tau-a 2.0` era nella **lista comune** di `gira()`, quindi arrivava **anche al braccio di riferimento**, che doveva girare a `TAU_A = 50`. Il "riferimento" era un **secondo braccio sperimentale**: confrontare `TAU_A = 2.0` con se stesso e chiamarlo effetto. **Stessa famiglia del default ribaltato** (CLAUDE.md par.9): in entrambi i casi i **rami di controllo diventano duplicati del ramo di prova**, e il test **continua a PASSARE**. **Il driver NON era committato** (`??` in `git status`) benche' `d3874fd` si intitoli *«Committato PRIMA del run»*: par.5 non rispettato per il driver | **L'esperimento `TAU_A = 2.0` di ieri NON e' contaminato — verificato DAI DATI**, non dal comando: il blocco `# RUN_PARAMS` del CSV da' `tau_a_over = null` + `REGIME = deterministico` -> **`TAU_A = 50`** nel braccio di riferimento. **Controprova indipendente: 2577 nodi esatti** sia nei CSV di ieri sia nel braccio `rif` rigirato oggi. **Il `-32 %` REGGE.** Scansionati tutti i 34 driver: **8 hanno piu' di un braccio, nessun altro porta il difetto** — **col limite dichiarato** che la scansione guarda il disco di OGGI e il file era gia' corretto (il mio primo rilevatore automatico trovava **zero collisioni, compreso il file col bug**: *un rilevatore che non trova il caso che lo ha generato non e' un presidio*). **PRESIDIO: un A/B si certifica dai DATI dei bracci, non dal codice del driver.** **Lacuna P6 aperta: `# RUN_PARAMS` non contiene ne' `TAU_A` ne' `G_PH` ne' il `blob`** — `TAU_A` e' solo *deducibile*, il `blob` **nemmeno quello**. -> `doc/REFERTO_driver_gira.md` |
| **Z21** | **`SPIN_FEEDBACK` con `TAU_A = 2.0`: ESITO MISTO, e non si sceglie una delle quattro letture** (2026-09-17) | **Base verificata** (Z20). `E4` **[GATE] PASSA**: feedback applicato in **124/126** chiamate (98.4 %), `lift_corto` 2 = transitorio, `mask_vuota` 0 — **nessuna traccia della famiglia `_psi_spin_prec`**. **Ma il numero va letto: rapporto |feedback|/|coppia| MEDIO = 2.706** (A3c, mediane dello stesso passo; il max 115 e' un passo solo). **Un termine chiamato *feedback* che vale quasi TRE VOLTE la coppia su cui retroagisce non e' una correzione: e' il motore.** `E1` **[BLOCCANTE] 4/4**: 0 NaN, `|nb|-1` 2.2e-16, `min d0` 0.05, `_taup_cfl_max` 0.553 < 1 | **`E2`: la perdita di nodi si riduce da -31.9 % a -28.9 %** (1754 -> 1833, **+4.5 %**) — **direzione attesa, ampiezza NON stabilita: UN SEME, e il nullo di questa differenza non e' misurato** (il nullo caotico noto sul conteggio nodi vale ~1.4 %, ma su altra configurazione, e `SPIN_FEEDBACK` non e' una perturbazione a 1e-16). **`E3`: gli indicatori di STRESS PEGGIORANO** — `psi` da x91 a **x112**, `d0` da x17.5 a **x27**; `omega_s` e `phivel` invece **calano**: il quadro **non e' monotono**. `ramp` piatto (0.4704 vs 0.4661) = controllo negativo interno. **VERDETTO: un'ipotesi che guadagna 3 punti sul conteggio nodi mentre peggiora di 21 punti il picco di `d0` NON ha «retto»: ha spostato il problema.** **Per decidere servono, in ordine: (1) un SIGILLO per `SPIN_FEEDBACK` (oggi non certificato), (2) >= 4 semi su `E2` (P3), (3) `Z10`.** **Nessuna promozione, nessun cablaggio.** -> `doc/REFERTO_esperimento_spin_feedback.md` |
| **Z22** | **REGOLA 9 — `par.5-quinquies` ESISTEVA, ed e' stato violato lo stesso. Una regola che non impedisce il ripetersi di un difetto non e' un presidio: e' una testimonianza** (2026-09-17, criterio posto da Luca) | La regola *«il codice di una misura dev'essere recuperabile PER COSTRUZIONE»* e' scritta dal **2026-09-16**. Il **2026-09-17** `_esperimento_spin_feedback.py` ha prodotto **un run intero** risultando `??` in `git status`, e **nulla lo ha segnalato** — mentre il commit `d3874fd` si intitolava *«Committato PRIMA del run»* (conteneva il simulatore e le previsioni, non il driver). **Il criterio e' secco: se il difetto si ripete DOPO che la regola esiste, la regola ha fallito**, e va sostituita da un MECCANISMO — non rafforzata, non ripetuta piu' in grande, non spostata piu' in alto. **E' la stessa forma gia' vista con l'encoding cp1252** (sette volte, la settima allo script che contava le occorrenze): *«una nota che non impedisce il ripetersi non e' un presidio»* | **MECCANISMO COSTRUITO E PROVATO: `csv/_presidio.py`, funzione `avvia(__file__)`.** Fa quattro cose in una riga: (a) forza `stdout`/`stderr` a **utf-8**; (b) calcola lo **sha1 dei BYTE GREZZI** dello script e lo confronta con HEAD **estraendo il blob con `git cat-file`**, mai con `git diff` o `git hash-object` (trappola CRLF, par.5-quinquies); (c) **STAMPA il timbro in testa all'output** — cosi' un referto prodotto da uno script non tracciato **lo dichiara da solo**, dentro il file che qualcuno leggera' fra sei mesi, invece che nella memoria di chi ha lanciato il comando; (d) se lo script e' un **`_sigillo_*.py`** e non e' pulito, **RIFIUTA DI GIRARE** (`exit 2`). **La distinzione e' voluta:** un diagnostico da codice non committato produce un numero **da dichiarare** — rifiutare renderebbe impossibile il lavoro esplorativo e il divieto verrebbe aggirato; un **sigillo** da codice non committato produce un timbro **privo di valore per definizione** (par.2.6: il gate certifica un BLOB), e li' rifiutare non costa nulla. **I quattro rami sono stati ESERCITATI, non dedotti** (pulito / modificato / non tracciato / sigillo respinto con `rc = 2` e la riga proibita **non** stampata): *un ramo mai visto scattare e' un comportamento sconosciuto* (P5). **E il timbro e' agganciato al presidio ENCODING di proposito**, perche' quello e' gia' in **74 script su 74**: una riga che nessuno dimentica gia' arriva dove serve. **COSA IL MECCANISMO NON FA, e va detto: non IMPEDISCE di girare uno script non tracciato — lo fa DICHIARARE.** **RESTA DA FARE, e serve un mandato proprio:** (1) sostituire il blocco encoding con `_presidio.avvia(__file__)` nei 74 script (tocca ogni strumento: va fatto in un commit dedicato, non dentro un esperimento); (2) **la parte strutturale vera — scrivere `blob` e `SOLITON_DRIVER` dentro il blocco `# RUN_PARAMS` del CSV**, perche' e' **nei DATI** che la certificazione si e' salvata oggi, non in stdout: li' un `driver: null` sarebbe **l'allarme stesso**, e permanente. Oggi `# RUN_PARAMS` **non contiene ne' `blob` ne' `TAU_A` ne' `G_PH`** | -> `doc/REFERTO_driver_gira.md`, `csv/_presidio.py` |
| **Z23** | **`SPIN_FEEDBACK` NON E' ANTISIMMETRICO: `sum(out) != 0`, e la causa e' la divisione per il grado. E il `2.706` non e' un fatto sul feedback** (2026-09-17) | **(a) IL RAPPORTO NON E' UNA COSTANTE.** `|feedback|/|coppia|` su 64 invocazioni a `TAU_A = 2.0`: media **2086**, mediana **1.83**, p25/p75 **1.11 / 9.51**, min/max **0.70 / 54844** — **4.9 ORDINI di escursione**, e negli **ultimi 10 passi mediana 0.748**, cioe' **il feedback e' piu' PICCOLO della coppia a regime** (rapporto < 1 nel **21.9 %** dei passi). **Il `2.706` era una MEDIA su una popolazione NON STAZIONARIA: UNDICESIMO caso di A3c, e cade sul mio stesso rimedio** — avevo sostituito *«due max di passi diversi»* con *«media dei rapporti per-passo»*, e la media resta sbagliata perche' A3c chiede la **stessa popolazione**, e una popolazione che si muove di cinque ordini **non e' una popolazione**. **La frase «non e' una correzione, e' il motore» NON REGGE:** era vera del transitorio e falsa del regime. **(b) L'AMPIEZZA VIENE DALLA COPPIA:** da 0 a 63 passi `|coppia|` cresce **x2.9 MILIONI** (2.1e-8 -> 6.0e-2), `|feedback|` solo **x37**. `|imag(ov)|` mediana **0.035** (max 0.852, dentro `[0,1]`), `w/grado` **0.0073**, `|feedback|` **0.0228** — **sotto** il nullo stimato **prima** (~0.1). **Il feedback e' ORDINARIO; e' la coppia che parte sei ordini sotto il suo regime** | **(c) ⚠ IL FATTO PIU' GROSSO, e non era fra le domande: `F3` FALLISCE GIA' ADESSO.** `|sum(out)|/max|out|` = mediana **1.112**, MAX **8.441** (l'errore macchina sarebbe 1e-14). **CAUSA ALGEBRICA, dimostrata e verificata sul numero esatto:** `out[a] -= f/g_a` e `out[b] += f/g_b` hanno **denominatori DIVERSI**, quindi la somma vale `f*(1/g_b - 1/g_a)`, **zero solo se i gradi sono uguali**. Caso minimo (stella `[3,1,1,1]`, `f=1`): previsto `3*(1-1/3) = 2.000000`, **misurato 2.000000**; con un denominatore **simmetrico** sull'arco la somma torna **-2.2e-16**. **Il docstring dice *«coppia ANTISIMMETRICA ai nodi»* e *«la divisione per il grado non introduce una manopola»*: la seconda frase e' VERA ed e' la RAGIONE per cui la prima e' FALSA.** Con `sum(out) != 0` il termine **inietta coppia NETTA**, pilotata dalla **disomogeneita' dei GRADI** — una proprieta' della topologia, non della fisica dello spin: e' il **cricchetto** che A7 esclude. **QUINTO membro della famiglia «un flag che non fa cio' che dichiara»** dopo `_passo_spinoriale`, `VERSO_CHI`, `spin_locale`, `TW_SPINORE` — **e qui non e' il commento a essere stale: e' la PROPRIETA' dichiarata a non esserci.** **(d) PREMESSA DEL MANDATO CORRETTA:** il feedback vive nel settore della **FASE** (`coppia += _fb` a `:3129`, poi `/ M_PH = 1.0`, una **COSTANTE**), **non** in quello dello SPIN: la correzione dell'inerzia di sei ordini sta in `_passo_spinoriale` e **non tocca questa catena**. **(e) FRAGILITA' ANNOTATA:** `_cm` misura la coppia **accumulata fin li'**; il twist di `:3154` viene **dopo**. Con `TW_SPINORE` spento non manca nulla, ma accendendolo `_cm` misurerebbe una coppia **parziale** in silenzio. **DECISIONE A LUCA:** `E4b` da riscrivere; se si accetta `F3`, il sigillo **fallisce sul punto che definisce la forma del termine**; e ripensare il divisore sarebbe una **LEGGE NUOVA** (par.10), non una riparazione. **`SPIN_FEEDBACK` resta OFF.** -> `doc/REFERTO_2706.md`, sonda `csv/_test_fork/_sonda_2706.py` |
| **Z24** | **IL DENOMINATORE PER GRADO: la misura NON distingue (A) da (B), ma (B) cade per DIMOSTRAZIONE. E lo stesso schema e' in ALMENO QUATTRO PUNTI** (2026-09-17) | **(B) «l'inerzia vera e' gia' a valle» NON REGGE, verificato dal sorgente:** il feedback ha **una sola** chiamata (`:3113`), finisce in `coppia`, e `coppia` va a `:3197` -> `delta_phivel = ... / **M_PH**` con **`M_PH = 1.0` COSTANTE GLOBALE** (`:206`). L'**unica** `/inerzia` del file (`:2283`) divide **`correzione`** (un 3-vettore in `_passo_spinoriale`), **non `coppia`**: **`omega_s` non vede mai il feedback.** Due catene separate — FASE contro SPIN. **Non c'e' nessuna doppia divisione da togliere**, e togliere `/grado` non toglie una normalizzazione di due: toglie **l'unica**. *(Esclusione per DIMOSTRAZIONE, non per misura: CLAUDE.md, «si misura per promuovere, si dimostra per escludere».)* **E IL CRITERIO CAMBIA: in un settore a massa UNIFORME `d/dt sum(phivel) = sum(coppia)/M_PH`, quindi `sum(out) = 0` E' LA CONSERVAZIONE, non un criterio di forma.** L'avvertenza *«`sum(coppia)` non e' fisica perche' `I` varia per nodo»* vale per lo SPIN, non per questo settore. **`L_tot` NON e' lo strumento giusto** — e non e' stato calcolato: il simulatore **non espone l'array dell'inerzia**, e metterci `I = 1` avrebbe dato un `sum(|omega|)` **travestito da L_tot** (il fallback silenzioso che P5 vieta) | **LA MISURA** (3 varianti, stessa scena, `TAU_A = 2.0`, 60 passi, **UN SEME**): **`G1`** `|sum(out)|/max|out|` mediana **attuale 1.112e+00**, **senza 6.475e-16**, **simm 8.540e-16** — **quindici ordini di grandezza**: il difetto e' confermato ED e' il denominatore, **ma G1 NON DISCRIMINA, entrambe le cure lo passano.** **Deriva di `sum(phivel)`:** `sum|d_step|` **298.1 / 293.5 / 243.2**, `|deriva|/scala` **1.066 / 1.038 / 0.924** — `simm` migliore su tutto, **MA `n finale` = 548 / 564 / 554: le tre traiettorie DIVERGONO**, quindi confronto **fra sistemi diversi**, un seme, nullo non misurato (il nullo caotico noto sul conteggio nodi e' ~1.4 %, qui `n` differisce del **2.9 %**). **-> LA MISURA NON DISTINGUE, e lo si dice invece di scegliere** (§4 del mandato). **⚠ E L'«ALTERNATIVA» DEL MANDATO — *accumulare nudo e dividere per il grado DOPO, sul totale del nodo* — E' ALGEBRICAMENTE IDENTICA AL CODICE ATTUALE**, perche' `grado[k]` e' lo stesso su tutti gli archi di `k` e **si raccoglie**: `Sum(+-f/g_k) = (Sum +-f)/g_k`. Verificato: **`max|attuale - alternativa| = 5.551e-17`**, e `|sum|/max` **7.119e-01 in ENTRAMBE** contro **2.365e-16** della forma simmetrica. **L'unico modo di avere `sum(out) = 0` e' un denominatore SIMMETRICO SULL'ARCO, o nessuno.** **⚠⚠ E IL DIFETTO ERA GIA' DIMOSTRATO NEL REPO, su un termine GEMELLO:** `doc/MAPPA_accoppiamenti_spin.md` (88-96) dice gia' che `(w/deg_i)` contro `(w/deg_j)` *«sono opposti SOLO se `deg_i == deg_j`»* e che *«`SOMMA_i L_i` non e' conservata, per costruzione»*, **col suo NB ONESTO: «quanto non lo sia non e' misurato»**. **La mia `1.112` e' la PRIMA misura di AMPIEZZA di un difetto gia' dimostrato in forma** — **P1 mancato da me**, ed e' il presidio *«ri-interroga le misure vecchie»*. **FRONTE NUOVO, NON APERTO QUI:** lo schema *«dividi per il grado del nodo»* e' in **almeno quattro punti** — `:1367-1368` (feedback), `:2082` (`B`), `:2294` (`_otw`), `:3154` (twist) — **e per il punto precedente «dentro» e «dopo» sono la STESSA COSA**, quindi la domanda **non riguarda solo `SPIN_FEEDBACK`**. **DA DECIDERE (Luca):** (1) se si accettano l'indifferenza della misura e l'esclusione di (B), **resta (A)**, che §1 del mandato pre-registra per il caso indifferente; (2) **il denominatore simmetrico NON ha oggi una derivazione** — `w` e' il candidato del mandato ma `flusso = w*imag(ov)`, quindi **dividere per `w` lo CANCELLA** e il peso sparisce dalla legge; `(g_i+g_j)/2`, `min`, `max`, `sqrt(g_i g_j)` sono **quattro scelte arbitrarie (A1)**; (3) il pregio smentito sta in **UN** posto, il **docstring** `:1336-1341`, ed e' `G9` (parte della cura, non di questa misura). **`SPIN_FEEDBACK` resta OFF.** -> `doc/REFERTO_denominatore.md` |
| **Z25** | **✅ CHIUSA — IL DENOMINATORE PER GRADO ERA UN ERRORE, ed e' stato TOLTO. Sigillo 12/12. E `G6` dice che LA FASE E' CUCITA** (2026-09-17) | **E' UN ERRORE, NON UNA SCELTA, e va letto cosi'.** `out[i] -= f/grado[i]` e `out[j] += f/grado[j]` hanno **denominatori DIVERSI**: la somma vale `f*(1/g_j - 1/g_i)`, **zero solo se i gradi coincidono**, e su un grafo disomogeneo non coincidono mai. Misurato **`|sum(out)|/max|out|` mediana 1.112, MAX 8.441** contro un errore macchina di **1e-16**. Il termine **iniettava coppia NETTA**, col verso pilotato dalla **disomogeneita' dei GRADI** — una proprieta' della **topologia**, non della fisica dello spin: il **cricchetto** che A7 esclude. **`imag(<psi_i|psi_j>)` e' antisimmetrico PER COSTRUZIONE: la legge era giusta, era la divisione ad averla rotta** | **CURA: nessun denominatore.** `out[i] -= flusso ; out[j] += flusso`. **PERCHE' NESSUNO e non uno simmetrico, misurato su 4 varianti (`eaa402b`):** `nudo`, `(g_i+g_j)/2` e `g_i+g_j-2w` portano `G1` **tutte e tre** all'epsilon (**6.5e-16 / 1.1e-15 / 7.9e-16**) — **la correttezza NON discrimina**; e la domanda che giustificherebbe un denominatore d'arco — *«|out| cresce col grado?»* — **NON HA RISOLUZIONE su questo grafo**: **~~il ~77 % dei nodi ha grado ESATTAMENTE 2~~ -> RITIRATO il 2026-09-18, errore di POPOLAZIONE mio: sono il 19.85 %, la distribuzione e' BIMODALE (mediana 119) e LA MISURA AVEVA RISOLUZIONE -- vedi Z27** e le mediane per grado sono **NON MONOTONE**. **E la pendenza del fit era un ARTEFATTO** (dava `+0.03` per la forma attuale mentre le mediane calavano: dominata da una coda di poche decine di nodi — lo strumento e' stato corretto perche' riportasse **entrambi**). Restano **A1 e par.3: nessun denominatore ha ZERO SCELTE.** **E l'ampiezza NON esplode:** `|out|` mediano **0.0400 -> 0.0223**, `max|out|` **0.816 -> 0.590** — e' **piu' piccolo** di prima. **SIGILLO `csv/_seal_fork/_sigillo_denominatore.py`: 12/12 PASS** (blob `11cf103f`): **`G1` [BLOCCANTE] mediana 6.475e-16, MAX 2.752e-15 su 64 invocazioni** (quindici ordini); `G2` **al limite VERO `grado pesato = 1`** `max|A-B| = 0.000e+00` su shape uguali — **NON il limite del mandato, che avrebbe dato un FAIL FALSO** (con `grado = g` la vecchia da' `±f/g` e la nuova `±f`) **ed era dichiarato PRIMA** in `doc/PREVISIONI_qualitative.md` (`f3f1ab0`); `G3` `0.40874`; `G5` `0.000e+00`; `G7` lift corto **2/66 = il transitorio**; `G8` 0 NaN, CFL **0.400**; `G9` docstring riscritto **con i numeri che lo hanno smentito**. **⭐ `G6` — LA CUCITURA DI FASE, CHE NON AVEVA MAI FATTO NESSUNO: `|delta imag(ov)|/|imag(ov)| = 0.0319` e CAMBI DI SEGNO `0.0035` contro un NULLO di `0.50`. LA FASE E' CUCITA:** `imag(ov)` e' **continuo** fra passi consecutivi, quindi il termine e' una **corrente orientata vera**, non rumore di gauge travestito — ed e' la proprieta' che rende significativo tutto il resto. *(Senza il nullo 0.5 quel `0.0035` non direbbe niente.)* **`G4` RIPORTATO, NON GIUDICATO:** deriva di `sum(phivel)` **-118.14** contro **-70.41** pre-cura, **ma le due traiettorie DIVERGONO** — confronto fra sistemi diversi, un seme, nullo non misurato. **COSA LA CURA NON FA:** `out[k]` resta una **SOMMA su termini che crescono col grado** — se sia un difetto **non e' stato misurabile**: fronte aperto. **E LO STESSO SCHEMA E' IN ALMENO TRE ALTRI PUNTI** (`:2082` `B`, `:2294` `_otw`, `:3154` twist), **non toccati e non misurati** — gia' dimostrati in forma in `doc/MAPPA_accoppiamenti_spin.md`: e' **Z24**. **CATEGORIA D (par.10): correzione di difetto, NESSUN FLAG. `SPIN_FEEDBACK` resta OFF di default** — la cura ripara il termine, **non lo promuove**. -> `doc/REFERTO_denominatore.md`, `csv/_seal_fork/_sigillo_denominatore.txt` |
| **Z26** | **A QUATTRO SEMI `SPIN_FEEDBACK` NON PRODUCE EFFETTO MISURABILE. Il `+3.0 punti` era UN SEME, e cade anche il «costo» su `d0`** (2026-09-18) | **P6 prima di tutto:** 8 run su 8 conformi, `TAU_A = 2.0` e `SPIN_FEEDBACK` letti **dal blocco `# RUN_PARAMS`** di ogni CSV, non dal comando. Disegno **APPAIATO** per seme (5, 11, 17, 23), barra = **dispersione FRA SEMI** delle differenze, mai la SE interna. **Criterio scritto PRIMA e non prorogato** (`4d4db99`): `t(0.025,3) = 3.182`, effetto dichiarato solo se l'IC95 appaiato **non** contiene lo zero | **δn per seme: `+31 / 0 / +3 / −59` — IL SEGNO NON E' NEMMENO CONCORDE**, la firma gia' catalogata in **C10**. **media −6.25, SD fra semi 37.84, IC95 ±60.2 -> CONTIENE LO ZERO.** Recupero medio **−0.34 %** del braccio OFF: non solo non recupera, la media e' **negativa**. **δ`max|d0|`** media +14.55, IC95 **±162.7**; **δ`max|psi|`** media +0.887, IC95 **±3.482**. **I TRE NULLI NON SI SCRIVONO ALLO STESSO MODO:** su **δn** la risoluzione e' il **3.2 %** (IC95 60.2 su 1865 nodi) -> *«il feedback non sposta il conteggio nodi di piu' di ~66 nodi»*, e **il nullo DICE qualcosa**; su **`d0`** la barra e' **11 volte** il valore e su **`psi`** **3.9 volte** -> **«NON MISURATO»**, non «nessun effetto». **COSA CADE:** il `+3.0 punti` era `1754 -> 1833` su **un seme** (**+79 nodi**), e **+79 sta FUORI dall'IC95** della misura a quattro. **E cade anche il «costo» di +21 punti su `d0`**, anch'esso un seme: qui `d0` fa `−7.5 / +160.2 / −79.2 / −15.3` — due semi migliorano, uno peggiora di 4.9 volte. **Il mio verdetto precedente *«ha spostato il problema»* era costruito su DUE numeri di un seme solo, e nessuno dei due sopravvive.** **LETTURA PRE-REGISTRATA CHE SI APPLICA (la seconda delle quattro): l'ipotesi «il −32 % dipendeva dalla trasmissione staccata» CADE** — riattaccare il feedback, e riattaccarlo **riparato**, non lo recupera. **Il fronte si sposta su `Z10`** (`TAU_A` e' ANCHE la vita media, `:2265`: a 2.0 la memoria di spin muore 25 volte piu' in fretta — un numero per due leggi). **⭐ CONTROLLO GRATIS NON PROGRAMMATO: il braccio OFF del seme 5 da' `1754` nodi, ESATTAMENTE come nell'esperimento PRE-CURA** (`1347246`): **la cura e' inerte a flag spento su un run batch VERO di 120 passi**, non solo nello stub del sigillo — il rito par.2.1 confermato sul campo. *(Onesta': un conteggio identico dopo 120 passi caotici e' una condizione **necessaria e stringente** — basta `1e-16` per farlo divergere — **ma non e' una dimostrazione di byte-identita'**.)* **COSA RESTA VERO DEL GIRO: il valore non e' nell'A/B, e' nella BONIFICA** — il cricchetto e' tolto (**1.112 -> 6.5e-16**), `G6` dice che **la fase e' CUCITA**, e `SPIN_FEEDBACK` **ora ha un sigillo** (12/12) che prima non aveva. **Un cricchetto e' un difetto anche se il termine non produce effetti misurabili.** **LIMITI:** 4 semi sono il MINIMO (con 8 la risoluzione su δn passerebbe da 3.2 % a ~2 %: **un effetto vero ma ≤ 1 % resterebbe invisibile a questo disegno**); 120 passi sono ~1/50 della maturazione di `ramp`; una sola scena. **`SPIN_FEEDBACK` RESTA OFF DI DEFAULT.** -> `doc/REFERTO_semi_spin_feedback.md` |
| **Z27** | **`Z24` MISURATA: i tre punti NON sono lo stesso schema. UNO e' un cricchetto ATTIVO, uno e' LATENTE, uno NON si cura col denominatore. E una mia conclusione precedente e' RITIRATA** (2026-09-18) | *(righe del mandato PRE-CURA e shiftate: sul blob attuale `:2116`, `:2328`, `:3188`, trovate per NOME — par.0)* **PUNTO 1 `B`:** `np.add.at` ha **il segno PIU' su ENTRAMBI** -> **non e' uno scambio**, e' una **media pesata** dei Bloch dei vicini: `sum(B)` non ha ragione di essere zero. L'oggetto vero e' `correzione = cross(B, nb)`, **residuo 2.266** (MAX 5.512). **MA LA CURA DI Z25 NON SI APPLICA, e la misura lo dimostra: DUE ROTTURE INDIPENDENTI** — divisione **senza** refl **2.622**, refl **senza** divisione **6.507**, denominatore d'ARCO **3.060**, e **solo togliendo ENTRAMBE** si arriva a **1.379e-15**. **Togliere solo il denominatore PEGGIORA** (2.266 -> 2.622). La seconda rottura e' `refl` (asse `sigma_x` per i legami fra chiralita' uguali): toglierla **e' una LEGGE NUOVA**, non una bonifica. *(MAPPA_accoppiamenti_spin.md le dichiarava entrambe in FORMA, col suo NB ONESTO «quanto non lo sia non e' misurato»: ora e' misurato.)* **PUNTO 2 `_otw`:** idem **segno PIU' su entrambi**, e non e' nemmeno una coppia — e' un **incremento di omega** aggiunto direttamente. **`TW_SPINORE = False`** -> **LATENTE: si dichiara, non si corregge.** | **⚠ PUNTO 3 `twist_nodo` — IL RITROVAMENTO: `+twn` / `−twn` = SCAMBIO, poi divisione per il grado DEL NODO. E' ESATTAMENTE `Z25`, ed e' ATTIVO: `FRAME_DRAG = True` di default (`:685`), quindi GIRA IN OGNI RUN MAI FATTO.** Residuo **6.756** (MAX 8.483); **il controllo NUDO da' `0.000e+00` ESATTO** -> **la divisione e' l'INTERA causa.** E finisce in `coppia` -> `delta_phivel / M_PH` con `M_PH` **uniforme**, quindi **qui `sum = 0` E' la conservazione**. **CURA NON ESEGUITA: il mandato dice STOP dopo la misura.** **⚠⚠ E UNA MIA CONCLUSIONE PRECEDENTE E' RITIRATA:** avevo scritto (in `Z25`, nel docstring del simulatore, nelle PREVISIONI, in `9.39`) che *«il ~77 % dei nodi ha grado 2, quindi la domanda |out| cresce col grado NON HA RISOLUZIONE»*. **ERRORE DI POPOLAZIONE — quello che A3 chiama per nome, fatto sul mio stesso conteggio:** il `1018` era su **5410** nodi-istanza e l'ho diviso per **1318**, la somma delle SOLE colonne stampate (gradi 2..9). **Distribuzione VERA: 549 nodi, 22044 archi, grado MEDIA 80.3 MEDIANA 119 MAX 122; grado == 2 -> 19.85 %, grado >= 100 -> 65.57 %. E' BIMODALE**, e i bin che avevo guardato coprivano il **21 %** dei nodi. **LA MISURA AVEVA RISOLUZIONE — un fattore 60 — e io ho guardato dove non ce n'era.** **RIFATTA SUL RANGE GIUSTO** (`|out|` a `g=2` contro `g>=100`): **attuale (pre-cura) 0.0489 -> 0.0483, rapporto 0.989, pendenza −0.003 = INTENSIVA**; **nudo 0.0017 -> 0.0613, rapporto 36.2, pendenza +0.878 = ESTENSIVA**; media 18.9 (+0.719); linea 16.9 (+0.691). **IL VECCHIO `/grado` RENDEVA `out` INTENSIVA DAVVERO — cio' che il docstring dichiarava — E NESSUNA DELLE TRE CURE LO FA.** **IL COMPROMESSO, algebrico: `sum(out) = 0` richiede un denominatore SIMMETRICO SULL'ARCO; l'indipendenza dal grado richiede il denominatore DEL NODO CHE RICEVE. NON POSSONO VALERE INSIEME.** Il vecchio codice sceglieva la seconda **rompendo** la prima, e non lo diceva. **La cura resta giusta su cio' che ripara** (un cricchetto e' una violazione di A7, non una scelta di modello) **ma il costo NON era «non misurabile»: e' misurato ed e' grande** — ai neonati `|out|` scende di un **fattore 29**. **DUE DECISIONI A LUCA:** (1) curare il punto 3 sapendo del compromesso; (2) se riconsiderare `nudo` per `SPIN_FEEDBACK`, dove `linea` e' la **meno estensiva** (16.9 contro 36.2) — **riportato, non proposto**. **E IL LIMITE DI par.3 DEL MANDATO E' CHIUSO, NON APERTO:** la topologia a grado variabile **c'e' gia'** (bimodale, 2 contro 119), la domanda **era** misurabile ed **e' misurata**. -> `doc/REFERTO_Z24.md` |
| **Z28** | **✅ CHIUSA PRIMA DI NASCERE — il limite «serve una topologia a grado basso o variabile» era un ERRORE DEL MANDATO, costruito su una MIA conclusione sbagliata. E gli ERRORI DI POPOLAZIONE sono QUATTRO in due giorni** (2026-09-18) | Il mandato di `Z24` chiedeva di registrare come **APERTO** un fronte: *«la domanda se `out[k]` che cresce col grado sia un difetto non e' misurabile su questo grafo — serve una topologia a grado basso o variabile, e non e' questo giro»*. **Quel fronte non esiste**, e la ragione e' che il mandato aveva ereditato la mia conclusione sbagliata sul «77 % a grado 2». **La topologia a grado variabile C'E' GIA': e' BIMODALE** — `grado == 2` **19.85 %**, `grado >= 100` **65.57 %**, mediana **119**, un'escursione di **un fattore 60**. **La domanda ERA misurabile, ED E' MISURATA** (`Z27`): il vecchio `/grado` era **INTENSIVO** (pendenza −0.003), `nudo` e' **ESTENSIVO** (+0.878). **Il limite si CHIUDE, non si apre**, e va registrato **come errore del mandato**, non come fronte | **⚠ UNA PRECISAZIONE, perche' la risposta di Luca puo' essere letta male:** *«il ~77 % di grado 2 e il grado medio ~121 non sono in contraddizione, sono due statistiche diverse su una coda pesante»* — **e' vero in generale, ma NON salva il mio numero**: il `77 %` era **aritmeticamente sbagliato** a prescindere (avevo diviso 1018 per **1318**, la somma delle sole colonne stampate, invece che per **5410**). Il valore vero e' **19.85 %**. **Le due cose vanno tenute separate: (a) una frazione alta a grado basso PUO' coesistere con una media alta; (b) la MIA frazione era comunque errata.** | **LA FAMIGLIA — QUARTO ERRORE DI POPOLAZIONE IN DUE GIORNI, e dopo A8 e' la regola che ne ha trovati di piu':** (1) `rho_arco` contro `median(I_nodi)` -> **8830**; (2) **media-di-mediane 19 contro 4089** della popolazione unita (`V5b`); (3) **`A3c`**, un RAPPORTO confrontato con un MASSIMO di passi diversi; (4) **questo**, `1018/1318` invece di `1018/5410`. **E il quarto e' il piu' istruttivo perche' il denominatore sbagliato ERA VISIBILE NELLA MIA STESSA TABELLA**: le colonne stampate erano `g=2..9` e io le ho sommate come se fossero il totale. **PRESIDIO: quando si calcola una frazione, il DENOMINATORE va preso dalla popolazione, MAI dalla somma di cio' che si e' scelto di stampare.** -> `doc/REFERTO_Z24.md`, `Z27` |
| **Z29** | **✅ `Z24` CHIUSA — dei tre punti UNO era un cricchetto e ora e' CURATO, gli altri DUE non lo erano. Sigillo 8/8** (2026-09-18) | **PUNTO 3 `twist_nodo` / `FRAME_DRAG` — CURATO.** Tolta `/ grado[k]`, **nessun denominatore** (decisione di Luca: e' l'unica forma esatta che non richiede **nessuna** scelta — A1, par.3). Commento riscritto: **due affermazioni smentite**, *«mediato sugli archi del nodo»* (era la divisione che rompeva l'antisimmetria) e *«emerge nella scala giusta ~0.2 della coppia principale senza aggiustamenti»* (quella scala veniva **proprio** dalla divisione: era un aggiustamento non dichiarato). **PUNTO 1 `B`/`correzione` — NON CURATO, con la ragione: DUE rotture indipendenti**, e togliere solo il denominatore **PEGGIORA** (2.266 -> 2.622); la seconda e' `refl`, e toglierla **e' una legge nuova** (par.10). **PUNTO 2 `_otw` — NON CURATO: LATENTE** (`TW_SPINORE = False`) e comunque **non e' uno scambio** (segno `+` su entrambi). **«Dimostrato in forma» puo' essere sbagliato in ENTRAMBE le direzioni: due dei tre non erano cricchetti, ed e' un RISULTATO** | **SIGILLO `csv/_seal_fork/_sigillo_twist_nodo.py`: 8/8 PASS** (blob byte `da216e56`): **`H1` [BLOCCANTE] `|sum|/max|.|` mediana e MAX = `0.000e+00` su 66/66 invocazioni**, era **6.756 / 8.483** — **zero ESATTO**, non «all'epsilon», perche' `twn` e' sommato e sottratto senza passare per una divisione; `H2` al limite **grado topologico = 1** `0.000e+00` su shape uguali; `H3` `1.7922`; `H3b` `sum(nuova) = 2.2e-16` contro `sum(vecchia) = 2.2218`; `H5` 0 NaN, **CFL 0.400**. **⚠ `H4` — IL RISCHIO ERA SCRITTO PRIMA E LA PREVISIONE ERA GIUSTA:** qui `grado` e' il **CONTEGGIO** (media ~80, mediana 119), **non** il grado pesato di `Z25`, quindi la cura **MOLTIPLICA** il termine: `|twist_nodo|` mediano **0.231 -> 27.5 (fattore 119)**, `max` **0.5 -> 34 (fattore 68)** — previsto *«~10^2»* in `52823d4`. **E il sistema REGGE COMUNQUE** (`max|phivel|` 14.065 contro 14.345 pre-cura): si applica la **PRIMA** delle tre letture fissate prima. *(`n` finale 548 -> 461 e `max|omega_s|` 34778 -> 12124: **riportati, NON attribuiti** — traiettorie divergenti, un seme, nessuna barra.)* **⚠ E `H6` HA PRODOTTO UN FAIL FALSO — DODICESIMO CRITERIO SCADUTO, committato prima di essere corretto** (`1d41a6c`, par.5): cercava le frasi smentite nel sorgente e le trovava **perche' il commento nuovo LE CITA come ritirate**, che e' la cosa giusta da fare. **Il criterio non distingueva AFFERMATO da CITATO COME RITIRATO.** Riscritto (`marcatore di ritiro` + posizione): **puo' ancora fallire** se l'affermazione ricomparisse **fuori** dal ritiro. **Un FAIL falso costa piu' di un sigillo mancante.** -> `csv/_seal_fork/_sigillo_twist_nodo.txt` |
| **Z30** | **✅ CHIUSA il 2026-09-18 — INDIFFERENTE, quindi `nudo` per A1** *(decisione di Luca)*. **La misura ha tolto la posta in gioco:** il criterio dell'estensivita' **NON DISCRIMINA**, perche' la forma del denominatore **non arriva all'osservabile** — `phivel` fa `x4.891` con la forma INTENSIVA e `x4.853` con quella ESTENSIVA (**0.8 %**), e il confronto e' fra i **due estremi disponibili** (un fattore **37** sul termine). **Fra due forme entrambe esatte e indistinguibili a valle, decide A1: `nudo` non richiede NESSUNA scelta, `linea` ne richiede una.** **Resta `nudo` su ENTRAMBI i punti** (`Z25` e `Z27`), come stabilito. *(I numeri `36.2` contro `16.9` restano veri **sul termine**: non erano sbagliati, erano **sulla grandezza sbagliata per decidere**.)* ~~APERTA — `SPIN_FEEDBACK`: `nudo` o `linea`? Il dato c'e', la decisione e' rinviata di proposito~~ (2026-09-18) | Coi valori veri (`Z27`) le tre forme esatte non sono equivalenti sull'**estensivita'**: rapporto `|out|` fra `g>=100` e `g=2` -> **`nudo` 36.2** (pendenza +0.878), **`media` 18.9** (+0.719), **`linea` (`g_i+g_j-2w`) 16.9** (+0.691). **`linea` e' la MENO estensiva**, e ha anche l'ampiezza piu' contenuta. Il pre-cura era **INTENSIVO** (0.989, pendenza −0.003) ma **rompeva la conservazione**: e' il compromesso algebrico di `Z27` | **NON SI TOCCA ORA, e la ragione e' metodologica:** la cura di `Z25` e' **committata e sigillata 12/12**, e cambiarla significherebbe rifare il sigillo **su un criterio diverso da quello con cui la scelta era stata fatta**. **Si decide INSIEME al punto 3, con lo STESSO criterio** — invece che una scelta oggi e una domani con criteri diversi. **La coerenza fra i punti vale piu' che ottimizzarne uno solo** (decisione di Luca, 2026-09-18). **✅ MISURATO IL 2026-09-18, E CAMBIA LA DOMANDA** (`doc/REFERTO_estensivita.md`): **(a)** la premessa *«grado alto = regione densa»* **E' UN FATTO** — `corr(grado, rho) = +0.79`, rapporto fra le mode **637x**; **(b)** la **compensazione dell'inerzia ESISTE ED E' GRANDE, ma vive nel settore dello SPIN**: contrasto `rho/peq` **x295** verso il bulk e **`omega_s` ~130 VOLTE PIU' PICCOLO** nel bulk — il segno e' quello che l'argomento di Luca prevede; **(c) ⚠ MA LA SCELTA DEL DENOMINATORE NON SI VEDE A VALLE: `phivel` fa `x4.891` PRIMA e `x4.853` DOPO — 0.8 % di differenza**, e il confronto e' fra i **due estremi disponibili** (termine **intensivo 0.989** contro **estensivo 36.2**, un fattore **37** sul termine). **Il bias di grado di `phivel` PRECEDE la cura, SOPRAVVIVE alla cura, e NON e' il denominatore a produrlo** — `coppia` contiene altri termini che lo dominano, coerente con l'A/B a quattro semi che non mostrava effetto. **CONSEGUENZA SU Z30: il criterio «quale forma e' meno estensiva» NON DISCRIMINA**, perche' l'osservabile non lo vede. Avevo portato a Luca una scelta fra `36.2` e `16.9` come se fosse la posta in gioco: **quei numeri sono sul TERMINE, e a valle valgono 0.8 %.** **Restano A1 e par.3 — `nudo` non richiede nessuna scelta — ma ora non e' solo un principio: e' una misura che dice che l'alternativa NON COMPRA NIENTE.** **La speculazione di Luca REGGE, con una precisazione: la compensazione e' reale e misurata, ma agisce sul settore dello SPIN, non su quello in cui il feedback entra. Il feedback non ha bisogno di essere normalizzato — non perche' l'inerzia lo compensi, ma perche' la sua forma non arriva all'osservabile.** **DOMANDA NUOVA CHE NE NASCE, non misurata: il bias di `phivel` (`x4.9`) e' esso stesso un difetto? Da dove viene?** | **(vecchia formulazione, superata)** ~~NON E' MISURATO SE L'ESTENSIVITA' SIA UN DIFETTO~~ Si sa **quanto** i termini crescono col grado; **non si sa se crescere col grado sia sbagliato.** In un modello relazionale un nodo piu' connesso partecipa a piu' relazioni: **potrebbe essere la fisica.** Se si risponde a QUESTO, la scelta fra `nudo` e `linea` diventa una **conseguenza** invece che una preferenza. **E' la domanda a monte, aperta a Claude web quanto a Luca.** **CRITERIO DI CHIUSURA:** una decisione esplicita fra *«esattezza senza scelte»* (`nudo` su entrambi, lo stato attuale) e *«esattezza + minima estensivita'»* (`linea` su entrambi), presa **con un criterio dichiarato prima** e applicata **a tutti e due i punti insieme**. **Ora che `Z24` e' chiusa (`Z29`), la decisione e' SBLOCCATA.** -> `Z27`, `doc/REFERTO_Z24.md` |
| **Z31** | **⚠ QUATTRO SIGILLI NON SONO PIU' RI-GIRABILI: il loro termine di paragone viveva in una CARTELLA TEMPORANEA, e il riavvio l'ha cancellata** (2026-09-18) | **UNA sola causa per tutti e quattro.** `_sigillo_inerzia`, `_sigillo_calcpsi_T1`, `_sigillo_calcpsi_T2` e `_sigillo_rimozione5` caricano una copia PRE del simulatore da `SC = os.environ.get("SCRATCH", HERE)` — cioe' dallo **scratchpad di sessione**, che **non e' nel repo**. Dopo il riavvio quella cartella non esiste piu'. **ESITO DEL RIGIRO (2026-09-18, blob `aa84755b`):** `_sigillo_inerzia` **Y6 FAIL FALSO** (legge un PRE vuoto, conta `0` usi di `rho_sorgente * _fatt_cs` e fallisce **su codice corretto**); `_sigillo_calcpsi_T1` **`[run pre FALLITO rc=2]` -> 0/1 FAIL**; `_sigillo_calcpsi_T2` gira in **modalita' ridotta** (1 solo verdetto: `Q3` e `Q6` dipendono dal PRE); **`_sigillo_rimozione5` SI SCHIANTA** con `FileNotFoundError` — **la modalita' peggiore**, gia' catalogata col caso dello Strato 1: *«NON FALLIVA: SI SCHIANTAVA»* | **E' `par.5-quinquies` APPLICATO AI RIFERIMENTI DEI SIGILLI, e il riavvio l'ha appena DIMOSTRATO invece che argomentato: un sigillo il cui termine di paragone sta in una cartella temporanea NON E' RI-GIRABILE, e un sigillo che non si puo' rigirare NON PROTEGGE NULLA.** **⚠ E IL DIFETTO NON E' SOLO L'ASSENZA DEL FILE: e' che tre di essi DEGRADANO IN SILENZIO** — `pre_src = "" if not os.path.exists(PRE)` trasforma un riferimento mancante in **un confronto contro il vuoto**, che poi produce un FAIL su codice sano. **Un riferimento mancante deve RIFIUTARE, non degradare.** **MECCANISMO PROPOSTO (Regola 9 — e dichiaro che finche' non e' cablato resta una PROPOSTA, non un presidio):** il PRE **non si conserva, si DERIVA dal repo** — `git cat-file -p <commit>:soliton_simulator.py` scritto in **BINARIO** (mai `git checkout`, mai `git hash-object`: trappola CRLF C18), tramite un helper in `csv/_presidio.py` che **(a)** prende il commit/blob come **costante dichiarata nel sigillo**, **(b)** estrae in una cache locale, **(c)** **ESCE con errore** se quel commit non e' nel repo, invece di restituire stringa vuota. Cosi' il riferimento e' **pinnato da un hash — l'unica identita' che non mente (par.2.6)** — e non costa 500 KB per sigillo. *(La convenzione ESISTE gia' nel repo: `csv/_seal_fork/_old_sim_pre_*.py` sono committate. Questi quattro sigilli semplicemente non l'hanno seguita.)* **NON RIPARATI IN QUESTO GIRO per ordine del mandato: e' un fronte a se'.** **NESSUNO DEI QUATTRO E' UNA REGRESSIONE FISICA** |
| **Z32** | **`Z9` RIMISURATA SUL BLOB ATTUALE: INTATTA (+2.8 %). E il meccanismo che poteva cambiarla e' BLOCCATO da una normalizzazione** (2026-09-18, blob `72acd6aa`) | **Il rilievo di Luca era corretto:** il numero di `Z9` era del blob `a8f1b2f4`, **prima** di `Z25`, `Z24/Z27` e dei due default `SPINORE_VIVO = True` e `SPIN_FEEDBACK = True`. E il meccanismo che descriveva **esiste tutto**: `eta += dt_n`, `dt_n = DT*r`, `r = ritmo()` dipende da `psi`, e il feedback ora gira e cambia `psi`. **PASSO 0, stampato e non assunto:** il feedback **GIRA** in questa misura, e `TAU_A = 50` come nello storico *(le mie misure recenti erano a `2.0`, e `ramp = min(1, eta/TAU_A)`: confrontarle sarebbe stato **A3c puro**)*. ⚠ **E lo script misura la config nuova SOLO perche' il default e' stato promosso** — la sua lista di flag **non contiene `SPIN_FEEDBACK`**: e' la classe di difetto di `STEP2_OROLOGIO`, stavolta **stampata** | **IL NUMERO — scena (A), l'unico confronto lecito collo storico:** `ramp` a 1/60/120 = **0.0002 / 0.010232 / 0.021151** contro **0.0002 / 0.0106 / 0.0217**; crescita di `eta` **0.0088** contro ~0.009; **`ramp = 1` a `5680` passi contro `~5526` -> +2.8 %.** **Scena (B), il sistema che gira davvero: `ramp = 1` a `6049` passi** — e **e' questo il numero da citare d'ora in poi**. *(La previsione ex ante, nel task history `93308af`, era «invariato entro un fattore ~2»: **e' entro il 3 %**.)* **⭐ E IL PERCHE' NON SI E' MOSSO — P4, verificato non dedotto:** `ritmo()` (`:2043-2049`) fa `med = max(median(|f|), 1e-9)`, `x = f/med`, `r = x/sqrt(1+x^2)+1e-6` **monotona**, e restituisce `1 + TAU_LOC*(r/r_unit - 1)`. Con `TEMPO_PROPRIO_ORIENTATO = False` (default) **`f = |signed| >= 0`**, quindi **`median(x) = 1` esatto e la mediana del risultato e' `1.0`**. **Misurato: `median(r) = 1.0` a meno di `~1.4e-4` in 245 chiamate su 246** *(l'unica eccezione e' il primo passo, `f` identicamente nullo, dove `med` cade sul pavimento `1e-9`)*. **=> `median(dt_n) = DT` esattamente: l'incremento MEDIANO di `eta` e' PINNATO PER COSTRUZIONE, e il meccanismo e' bloccato al PRIMO ORDINE.** E' **C12 — ma la condizione andava VERIFICATA**: con `--tempo-proprio-orientato` l'ancoraggio **cadrebbe**. **Cio' che resta libero e' la FORMA della distribuzione di `r` e la POPOLAZIONE, e insieme valgono +2.8 %.** **⚠ DUE CORREZIONI AI MIEI STESSI CRITERI, IN DUE GIRI CONSECUTIVI:** (1) avevo scritto il criterio P4 col **MASSIMO**, che dava `1.000e+00` e da solo direbbe *«l'ancoraggio non c'e'»* — **il massimo descriveva la coda, non la popolazione**, stessa forma di `A3c` e della pendenza di `Z27`; (2) corretto in **frazione**, ho messo la soglia a `1e-6` e ho etichettato **116 chiamate come «transitorio degenere»** — **sono `0.999857…0.999919`, cioe' `1.0` a quattro cifre: la soglia era troppo stretta e l'etichetta sbagliata.** **`R6` REGGE:** `_pesi()` 12 per passo (rif. 16), **861 PRIMA / 624 DOPO** la scrittura della cache -> gira **ancora a cavallo**; per chiamante `stato_crossover` 1354, `step` 123, `calcola_psi` 8; **fallback su `_cs_nodo_prev` 1.2121 %** — **piccolo ma NON nullo, e NON interpretato**: serve il confronto col valore della rimisura precedente. **CONSEGUENZA: `Z9` non cambia nessuna conclusione, e NON precludeva `Z30`** — e ora si puo' dirlo con un numero misurato sul blob attuale invece che su uno di due generazioni fa. -> `doc/REFERTO_Z9_rimisura_2026-09-18.md` |
| **Z33** | **LA MEDIANA IN `ritmo()` E' ENTRAMBE: normalizzazione dimensionale E gauge del tempo. `Z9` NON SI CURA DA QUI (A4)** (2026-09-18, blob `72acd6aa`) | **`2.1`** `f = |Delta_angolo/DT|`, **dimensione `1/T`**, per nodo. **Due rami**, e la scala differisce di **~100x**: scalare 2pi `median|f| = 3.04`, **spinoriale 4pi (quello delle campagne) `0.0280`**. **`2.2` TRE PROVE INDIPENDENTI, e danno DUE risposte:** **(a) dimensionale -> NORMALIZZAZIONE** — `f` ha unita', quindi la divisione **serve** all'adimensionalita' e **non e' eliminabile**; **(b) strutturale -> GAUGE** — il ritorno e' `1 + TAU_LOC*(r/r_unit - 1)`, una **deviazione da un riferimento**, e il docstring dice testualmente *«ancorata alla mediana globale come GAUGE»* (`:2013`); **(c) di consumo -> IL GAUGE CONTA** — `r` non e' MAI usato come rapporto fra nodi: `dt_n = DT*r`, `eta += dt_n`, `alpha = 1-exp(-dt_n/tau)`, `delta_phivel = dt_n_s*(...)` sono **tutti usi in VALORE ASSOLUTO**, e **nessuna soglia usa `r` direttamente**. **`2.3`** `dt_n`/`dt_e` alimentano `eta` (cioe' `Z9` stessa), `alpha` dello Strato 1, `delta_phivel`, `_rep`, il termostato: **cambiare `r_unit` riscala TUTTI i tempi propri locali** | **VERDETTO: E' ENTRAMBE** — la **quarta lettura**, aggiunta al mandato **prima** di misurare (task history `bb058c0`): *«una normalizzazione che si e' portata dietro un gauge»*. **La 2.2 come alternativa secca era MAL POSTA.** **=> `Z9` NON si cura togliendo la mediana:** toglierla rompe l'adimensionalita', sostituirla **cambia il gauge del tempo**, cioe' l'unita' di misura di `eta`, `alpha`, `delta_phivel`, `_rep` e del termostato. **E' A4, e va deciso come tale, non come una bonifica.** **`2.4`: LA TERZA LETTURA NON SI APPLICA AL RAMO VERO.** `median(|f|)` per quarti sul 4pi: **0.0254 -> 0.0201 -> 0.0228 -> 0.0358, cioe' x1.4 in 120 passi — QUASI PIATTA**, il caso che avevo scritto come peggiore (*«una costante mascherata da statistica»*). Sul ramo 2pi variava x8.4, ma **quel ramo nessuno lo gira**. **⚠ E UN REPERTO CHE NON ERA NELLA LISTA: IL GAUGE E' DEGENERE 1 PASSO SU 31.** `median(|f|)` vale **ZERO ESATTO in 4 invocazioni su 126 (3.2 %)** sul ramo 4pi — **e accade anche a `n = 451`**, cioe' **piu' della meta' dei nodi ha variazione di fase nulla in quel passo**. Li' `med` cade sul **pavimento `1e-9`**, `x` esplode, e **`r` diventa BINARIO**: `sqrt(2)` per i non nulli, `1.414e-06` per gli altri. **Il pavimento, messo come protezione, DIVENTA IL PARAMETRO FISICO** — stessa famiglia del `1e-6` sull'inerzia e del `0.05` su `_tau`. **Difetto INDIPENDENTE dall'ancoraggio, mai cercato, in nessuna voce.** **CIO' CHE RESTA LIBERO:** `median(r) = 1.000000000` per identita', ma la **dispersione p95-p05 vale 1.267 su una mediana di 1.0** — `r` spazia su un intervallo **largo quanto il suo stesso valore centrale** — **ed e' PIATTA** (per quarti 1.216 -> 1.302). **E' il CENTRO a essere fisso, non la fisica**: l'ancoraggio toglie che il *nodo tipico* acceleri **rispetto a se stesso**, non la differenza **fra** nodi. **SE SI VOLESSE CURARE:** serve una scala **locale, derivata, di dimensione `1/T`**. Candidato **`cs/d`** (l'inverso del tempo-luce del nodo) — **riportato, NON proposto**: sceglierlo significa **scegliere un gauge nuovo**. *(E l'architettura lo ammette gia': il ramo `TEMPO_SEGNO` a `:2027` ritorna `1 + (twn/deg)/PHI_CRIT`, **locale e normalizzato da una costante derivata** — e' una LEGGE diversa, non un rimpiazzo, ma e' il pattern di `cs_floor`/`Lam`.)* **⚠ E IL PRIMO GIRO DELLA SONDA AVEVA MISURATO IL RAMO SBAGLIATO** (`CAMPO_SPINORIALE = False`, 126/126 sullo scalare): stessa classe di `doc/REPERTO_cs_dinamico_spento.md`. Rifatto sul ramo vero. **NESSUN CABLAGGIO.** -> `doc/REFERTO_mediana_ritmo.md` |
| **Z34** | **`Z33` MISURATA: NON e' un difetto del GAUGE, e' un difetto di ORDINE/SNAPSHOT. TRANSITORIO, 4 passi su 126, tutti prima del passo 8** (2026-09-18, blob `72acd6aa`) | **(A) SONO NEL PERCORSO FISICO:** `126/126` invocazioni da **`step()`**, nessun diagnostico; le 4 degeneri **alimentano `dt_n` e quindi `eta`**. *(La lettura «artefatto della sonda» e' ESCLUSA.)* **(B) MA `f` NON HA MEDIANA PICCOLA: E' IDENTICAMENTE NULLO** in **3 casi su 4** (il quarto ha `max|f| = 3.55e-13`, **zero numerico**). **La terza alternativa della 2.1 e' esclusa, e con essa cade l'idea che il difetto sia nella mediana: non c'e' una statistica che collassa, NON C'E' VARIAZIONE DA MISURARE. Il pavimento `1e-9` NON C'ENTRA.** **DUE CAUSE, entrambe confermate:** **(1) SNAPSHOT CONFRONTATO CON SE' STESSO (3/4)** — `_psi_spin_prec` si aggiorna a `:3099-3101`, cioe' **DOPO** `ritmo()`, quindi `a = angle(psi) - angle(psi) = 0` per ogni nodo (`snap == True`); **(2) LUNGHEZZE DISALLINEATE (passo 6)** — `len(psi_spin) = 440` ma `len(_psi_spin_prec) = 80` dopo l'iniezione delle masse: il guard di `:2038` fallisce, si cade sul ramo 2pi, e li' anche `_psi_prec` e' corto -> scatta il **ramo di sicurezza `:2028`** che ritorna `np.ones(n)`. **TERZO MEMBRO della famiglia `C11`/`C7`** (snapshot/cache non estesi a una crescita di `n`) | **(C) TRANSITORIO, senza ambiguita':** passi **0, 1, 6, 7** — **ZERO dopo il passo 20**. **NON coincidono con le mitosi** (nati = 0 in tutti e quattro): coincidono con le **due discontinuita' della popolazione**, l'avvio e l'iniezione delle tre masse (`n: 80 -> 440`). **(D) LA DILATAZIONE SPARISCE DAVVERO: `std(r) = 0 ESATTO`** nei degeneri (contro `0.4146` nei sani), `p95-p05 = 0` esatto. **⚠ MA IL MANDATO AVEVA IL SEGNO SBAGLIATO:** prevedeva `r -> ±1` per `x -> inf`; **misurato `fraz r>1.4 = 0.0000` e `fraz r<1e-5 = 0.49`**. Se `f` e' **identicamente nullo** allora `x = 0`, non infinito, e `r -> 1.414e-06`: **IL COLLASSO E' VERSO IL BASSO.** I quattro si dividono in due coppie: **passi 0 e 6 -> `r = 1.0` esatto** (il ramo di sicurezza), **passi 1 e 7 -> `r ~ 1.414e-06`**, cioe' **`dt_n = DT * 1.4e-06`: IL TEMPO PROPRIO SI FERMA PER TUTTI.** *(Previsto nel task history `aff0df6` PRIMA di misurare; il mandato prevedeva l'opposto.)* **VERDETTO: la SECONDA lettura — «transitorio, ma `r` degenera -> difetto CONFINATO, cura OPZIONALE».** **E la seconda meta' della QUINTA lettura, quella che avevo aggiunto io, e' quella giusta:** *«se `f` e' identicamente nullo e la causa e' lo snapshot, e' un difetto di ORDINE, non di gauge, e la cura non c'entra con la mediana»*. **`Z33` va spostata FUORI dalla famiglia A3 in cui l'avevo messa.** **IL par.4 DEL MANDATO CADE, e con un numero: `Z9` NON va rimisurata.** `eta` non cresce in **2 passi su 126 = 1.6 %**, tutti nel transitorio iniziale: **impatto <= ~2 %, dentro il `+2.8 %` gia' misurato e dentro il `+6.5 %` fra le due scene.** **SE SI CURASSE — e non e' il gauge, quindi NON ricade nella decisione del par.3:** (1) una guardia che **rilevi e DICHIARI** `psi_spin == _psi_spin_prec` (A8); (2) **estendere lo snapshot con la popolazione**, la stessa cura di `C7`/`C11`. **Riportate, non proposte.** **LIMITE:** i 4 eventi sono legati a **due discontinuita' specifiche di QUESTA scena** — con piu' iniezioni sarebbero di piu'. **Il `3.2 %` e' della scena, non del sistema.** -> `doc/REFERTO_Z33_gauge_degenere.md` |
| **Z35** | **NESSUNA delle due vie cura `Z33`: la (1) E' IL DIFETTO, la (2) NON CURA e INVENTEREBBE UN PASSATO. La cura e' il CONTATORE, ed e' dentro** (2026-09-18, blob byte `94b6cc29`) | **VIA (1) — spostare l'aggiornamento prima del consumo: VERIFICATO DAL SORGENTE, SAREBBE IL DIFETTO.** `psi_spin` e' assegnato in **un solo punto** (`:2693`, dentro `calcola_psi()`); `ritmo()` consuma a `:3124`, **all'inizio di `step()`**, e lo snapshot si aggiorna a `:3132`, **dopo**. Quindi **il consumo legge gia' uno stato `t-1`: L'ORDINE ATTUALE RISPETTA A6.** Spostarlo confronterebbe `psi_spin` **con se' stesso** -> `f = 0` **per costruzione, sempre**. **VIA (2) — estendere lo snapshot: NON CURA IL CASO CHE DOVREBBE CURARE.** `semina()` (`:1812`) non estende `_psi_spin_prec` (voce **H**); `_eredita_spinore_figli` (`:1351`) lo estende **solo per la MITOSI** (cura `C11`), ed e' **giustificato** — il figlio nasce dal padre. **Ma i nodi di `nuova_massa` NON HANNO UN GENITORE.** **IL CONTO CHE DECIDE: all'iniezione `n: 80 -> 440`, i nodi nuovi sono 360 = 81.8 % della popolazione.** Estenderli col **valore corrente** (l'unica scelta che non inventa un moto) darebbe loro `f = 0` **esatto**, e **la mediana resterebbe ZERO**. Estenderli con altro significa **inventare una fase precedente**: **A1** (un numero scelto) + **A7b** (una storia che non c'e') | **LA CURA E' IL CONTATORE, GIA' DENTRO E BYTE-INERTE** (`800fb24`): `_ritmo_sicurezza`, `_ritmo_guard4pi_ko`, `_ritmo_snap_identico`, `_ritmo_f_tutto_nullo`, `_ritmo_f_mediana_nulla`, `_ritmo_med_sul_pavimento`. **Byte-identita' verificata** contro `aa84755b` estratto in binario: `max|A-B| = 0.000e+00` su `psi`/`phivel`/`eta`/`d`/`d0`/`omega_s`/`_nb`, **shape identiche (443 = 443)**. **Da oggi la degenerazione SI DICHIARA invece di essere invisibile** — l'unica cosa che i tre casi della famiglia (`_cs_nodo_prev` 71.88 %, `_psi_spin_prec` 95.33 %, questo) **non avevano**. **⚠ E I CONTATORI CORREGGONO UNA MIA RICOSTRUZIONE: `_ritmo_guard4pi_ko = 0`, il guard 4pi NON fallisce mai.** Avevo scritto che al passo dell'iniezione *«il guard di `:2038` fallisce, si cade sul 2pi»*: **falso** — il ramo di sicurezza `:2028` viene **prima**. **La sonda ricostruiva i rami nell'ordine sbagliato; il contatore, che sta nel codice vero, e' AUTORITATIVO.** E il **quarto caso non era «escluso»**: `_ritmo_f_mediana_nulla = 1` esiste davvero (valori a `3.5e-13`, rumore numerico, **ma il contatore li distingue**). **I QUATTRO CASI SONO LEGITTIMI, uno per uno:** al passo 0 **non esiste un prima**; all'iniezione i 360 nodi nuovi **non hanno un passato**; negli altri due **il campo davvero non si e' mosso**. **Il codice fa la cosa giusta: mancava solo che lo dicesse.** **`Z9` NON va rimisurata:** `eta` non cresce in **2 passi su 126 = 1.6 %**, impatto **<= ~2 %**, dentro il `+2.8 %` gia' misurato. -> `doc/REFERTO_Z33_due_vie.md` |
| **Z36** | **⚠ APERTA, MA RI-LETTA il 2026-09-18 (`Z37`): il `64.7 %` e' UN RAPPORTO SU UNA GRANDEZZA MINUSCOLA.** Misurato: gli stati consecutivi hanno **overlap > 0.99 nel 100 % dei casi** e il livello di `a` e' **3.94e-04 rad/passo**. Quindi *«il tempo proprio salta del 65 %»* significa che una fase che avanza di `4e-04` per passo **varia di ~2.8e-04** — **in valore ASSOLUTO meno del salto del feedback (1.1e-03)**. **E' la stessa forma dell'errore del `2.706`: un rapporto grande perche' il denominatore e' piccolo.** La frase *«non e' cucito, venti volte peggio»* **resta vera SUL RAPPORTO, ma va qualificata: NON e' «il tempo proprio e' rumore».** *(E non e' rumore numerico: `4e-04` e' dodici ordini sopra la precisione di `angle`.)* **LA DOMANDA SI AFFINA: non «la fase e' confrontabile?» — LO E', l'overlap e' 1 — ma «un avanzamento di fase di `4e-04` per passo, che fluttua del 65 %, e' l'orologio che vogliamo?»** E si ricollega a **`Z9`**: con un avanzamento cosi' piccolo, **la maturazione e' lenta PER COSTRUZIONE**. *(testo precedente, che resta vero sul rapporto: «`K7`: IL TEMPO PROPRIO NON E' CUCITO. `f` salta del 64.7 % per passo, VENTI VOLTE il feedback», 2026-09-18)* | Misurata **`G6` applicata a `_psi_spin_prec`**, che **non aveva mai fatto nessuno su questo oggetto**: | **`f = |signed|`, LA GRANDEZZA CHE IL CODICE USA: `|delta f| / |f| = 0.647`.** Il confronto: **`imag(ov)` del feedback dava `0.032`** (`Z25`/`G6`). **VENTI VOLTE PEGGIO.** E `signed` (col segno) **cambia segno nel 34.2 % dei nodi** fra passi consecutivi, contro un **nullo di 0.50**: **parzialmente cucito, ma molto piu' vicino al rumore che alla cucitura** *(il feedback dava `0.0035`)*. **DUE PRECISAZIONI CHE IMPEDISCONO DI SOVRAINTERPRETARE:** (a) il codice prende **`|signed|`**, quindi i cambi di segno **sono gia' scartati** — il `0.933` sul segnato e' **gonfiato da quelli**, e il numero che conta e' **`0.647`**; (b) **ma proprio per questo `--tempo-proprio-orientato` alimenterebbe una grandezza NON CUCITA: e' un avviso per QUEL flag**, non per il default. **PERCHE' E' PIU' GRANDE DI `Z33`: il tempo proprio e' costruito su una grandezza che non e' cucita, e `Z33` — 4 passi su 126 con `f` nullo — e' UN CASO PARTICOLARE di un problema che c'e' in TUTTI i passi.** **CRITERIO DI CHIUSURA:** stabilire se un tempo proprio con `|delta f|/|f| ~ 0.65` sia **fisico** (il campo oscilla davvero cosi') o **rumore di gauge** (la fase non e' confrontabile fra passi). **Serve una misura che distingua le due**, e non l'ho. **LIMITE:** il confronto e' **nodo per nodo sui primi `min(len)` indici** — i nuovi si appendono in coda, quindi gli indici bassi restano gli stessi nodi: **approssimazione dichiarata**. Un seme, 126 passi, una scena. -> `doc/REFERTO_Z33_due_vie.md` par.4 |
| **Z37** | **LA CUCITURA DELLO SNAPSHOT FALLISCE SU ENTRAMBI I FRONTI, e si DIMOSTRA perche'. NON CABLATA** (2026-09-18, blob `f8f46683`) | **(A) IL CANDIDATO DEL MANDATO E' ESCLUSO:** `|psi_spin[:,0]| / |psi_spin|` ha **mediana 0.999881**, `p01` **0.976**, **MIN 0.899377**, e la **frazione sotto `1e-1`/`1e-2`/`1e-3`/`1e-6`/`1e-9` e' ZERO su tutte**. **La prima componente non passa MAI vicino a zero: `angle` su di essa NON e' mal definito.** Lo spinore **non ruota** fra le componenti: resta quasi interamente sulla prima. **(C) GLI STATI CONSECUTIVI SONO QUASI IDENTICI:** `|<psi_prec|psi>|` normalizzato ha **mediana 1**, `p05` **0.999957**, e la **frazione > 0.99 e' 1.0000** — il **100 %** delle coppie. **(B) NESSUNA CORRELAZIONE:** `corr(log comp0, log salto) = **-0.0017**` su 52149 nodi-coppia, e **tutti** stanno nella fascia `comp0 > 1e-1`: **non esiste nemmeno una fascia a componente piccola da confrontare.** Il candidato cade **due volte** — per assenza della causa e per assenza di correlazione | **(D) LA CURA FALLISCE SU ENTRAMBI I FRONTI:** **LIVELLO** `median|a|` da **3.94e-04** a **7.03e-06** (rapporto **0.0179**: toglie il **98 % del segnale**), **e il SALTO NON SCENDE, PEGGIORA**: da **0.712** a **0.786**. **Non e' nemmeno un compromesso «meno segnale ma piu' stabile»: e' peggio su entrambi gli assi.** **⚠ E SI DIMOSTRA PERCHE', dalla STRUTTURA dell'oggetto e non da un seme:** poiche' la componente 0 e' il **99.99 %** dello spinore, `overlap ~ conj(psp[:,0])*ps[:,0]`, quindi **`angle(overlap) ~ a_originale`** e **`a_curato = a_or - angle(overlap) ~ 0` piu' CANCELLAZIONE CATASTROFICA** — ed e' per questo che il salto relativo **AUMENTA**. **La cura sottrae `a` a se' stessa.** *(Il conto nel task history `02ac909`, scritto PRIMA, era giusto nella conclusione e incompleto nella ragione: avevo detto «per rotazione rigida»; la misura dice che **basta che una componente domini**.)* **E PERCHE' NEL LIFT FUNZIONA E QUI NO:** nel lift le due componenti sono **entrambe vive** e la fase globale e' **gauge**; qui **una componente ne porta il 99.99 %** e la sua fase **E' IL SEGNALE**. **Lo stesso codice, su oggetti diversi, fa cose opposte.** **⚠ E `L1` DA SOLO SAREBBE STATO UN FALSO PASS se il salto fosse sceso: il sigillo del mandato guardava UN NUMERO SOLO.** La **coppia salto+livello** era necessaria, ed era nel task history. **NON CABLATA**, `psi_spin` non toccato, `f` non ridefinito, gauge non toccato, `Z9` non rimisurata *(il par.5 la prevede DOPO la cura, e la cura non c'e')*. **LIMITE:** la dominanza al 99.99 % **potrebbe essere di questa configurazione** — `_psi_spinor` nasce da `exp(i*phi)` sull'asse 0 (`:2657`): **in un sistema dove lo spinore ruotasse davvero fra le componenti, il candidato del mandato tornerebbe in gioco.** -> `doc/REFERTO_Z36_cucitura.md` |
| **Z38** | **IL §1 DEL MANDATO GAUGE E' CHIUSO: il gauge attuale sta NELLA MATERIA, e la mia OBIEZIONE e' REFUTATA. NESSUNA CURA CABLATA** (2026-09-18, blob `f8f46683`, 1 seme, 120 passi, ramo **4pi** su 124/126) | **(B) IL NODO MEDIANO DI `f` HA `cs/CS_M = 0.801`**, contro **0.8265** di tutta la popolazione: **il gauge attuale e' ancorato dove l'orologio metrico va all'80 % del vuoto**, cioe' **`(cs/CS_M)^2 = 0.64`** nel fattore di `STEP2`. **I due riferimenti NON coincidono: la lettura «cura cosmetica» NON scatta.** **MA VA DETTO ANCHE IL CONTRARIO, ed e' nella stessa tabella:** i nodi a `x ~ 1` **non si distinguono dalla popolazione** (`0.801` contro `0.8265`; `rho/peq` `1.272` contro `1.354`). **Il nodo mediano di `f` e' semplicemente un nodo TIPICO: non e' che il gauge sia finito in un posto strano, e' che TUTTA la popolazione sta nella materia** | **(C) ⚠ LA MIA OBIEZIONE E' REFUTATA DALLA MISURA.** Avevo scritto PRIMA (task history `0e571b4`) che `cs/CS_M` **non e' ancorato al vuoto ma a `mean(I)`** e che avrebbe avuto **un punto fisso della famiglia C12**. Misurato: **`median(I)/mean(I)` vale `0.145 / 0.653 / 0.462`** a tre istanti — **un fattore 4.5: il nodo TIPICO non e' inchiodato**, e `cs/CS_M` mediano si muove `1 -> 0.871 -> 0.823 -> 0.841`. **DOVE AVEVO RAGIONE:** la scala **e'** `mean(I)`, una statistica sulla propria popolazione — e' nel codice. **DOVE AVEVO TORTO, ed e' tutta la differenza:** `median(|f|)` inchioda **`median(x) = 1` ESATTAMENTE, per identita', sempre**; `mean(I)` inchioda solo la MEDIA, e **su una distribuzione asimmetrica la media non e' il nodo tipico**. **E' la stessa asimmetria che il 2026-09-17 fece cadere il punto fisso di `_tau`** — li' per un filtro, qui per la skewness. **I due ancoraggi NON sono della stessa specie, e questo e' un argomento A FAVORE del mandato, che avevo scritto per metterlo in dubbio.** **(D) ⚠ IL PUNTO CHE NON DECIDO DA SOLO:** il criterio che avevo FISSATO (*«fuori da O(1) di piu' di 3 ordini -> non cablabile»*) **NON SCATTA: sono 1.7 ordini** (mediana `f*d/CS_M = 0.0188`) — **e non lo sposto a posteriori.** **Ma il numero che conta l'ho trovato dopo: la frazione nella banda `[0.1,10]` passa da `87.1 %` a `15.1 %`**, e poiche' nessun nodo supera `x = 10`, **l'`84.9 %` finirebbe sotto `r_norm = 0.141`, cioe' nel DECIMO INFERIORE della dilatazione** (oggi <= `12.9 %`). **Il tetto NON si apre** (`1.358` contro `sqrt(2) = 1.414`) **e il pavimento non si abbassa: e' la POPOLAZIONE che scivola in fondo.** **DUE LETTURE LEGITTIME, e la misura NON le separa:** *(1)* **difetto** — il ginocchio sta a `x = 1`, col tipico a `0.019` il bottleneck lavora nel tratto LINEARE e **la saturazione non morde per nessuno**; *(2)* **fisica** — *«il nodo tipico ha un orologio 53 volte piu' lento del vuoto»* **e' cio' che una dilatazione gravitazionale deve dire**, e con `median(|f|)` era **invisibile per costruzione**. **NON E' UN «ALLINEAMENTO DI GAUGE»: E' UN CAMBIO DI SIGNIFICATO DI `r`**, da *«ritmo relativo al nodo tipico»* a *«ritmo relativo al vuoto»*. **CRITERIO DI CHIUSURA: serve una regola, scritta prima, che dica quale delle due letture vale. Non ce l'ho, e non me la invento adesso: DECIDE LUCA.** **E sul §2: i due candidati sono numericamente quasi indistinguibili** (`0.0244` contro `0.0188`, banda `17.9 %` contro `15.1 %`): **la scelta e' di PRINCIPIO, e il principio dice `CS_M/d_nodo`** — l'unico dei due **senza nessuna statistica di popolazione al suo interno**, e la stessa ancora gia' certificata in sezione A. **⚠ RISERVA NON MISURATA: `d_nodo` cade su `LAM` per i nodi isolati (`:3041`) e senza archi (`:3043`) — il mandato VIETA `LAM`, e per quella via rientrerebbe. E' un ramo `else` su percorso fisico: per P5 va CONTATO prima di cablare.** -> `doc/REFERTO_gauge_vuoto.md` |
| **Z39** | **⚠ UN FATTO STABILE DI CLAUDE.md E' CADUTO: `cs` E' VIVO. `cs_std/cs = 17.6 %`, non `0.0086-0.24 %`** (2026-09-18, blob `f8f46683`, stessa sonda di `Z38`) | **MISURATO:** `cs/CS_M` ha **mediana 0.8265**, `p05` **0.528**, **min 0.283**, e **`cs_std/cs` per passo = 17.6 %** (min `16.6 %`, max `19.4 %`). **La frazione con `cs/CS_M > 0.99` e' solo il 6.3 %.** CLAUDE.md par.9 dice come **fatto stabile** *«`cs_std/cs` fra 0.0086 % e 0.24 %, sempre sotto l'1 % … `tau = d/cs` E' `tau ∝ d` … il braccio ON non testa il tempo-luce»*: **e' superato di un fattore ~2000 rispetto allo 0.0086 % e ~73 rispetto allo 0.24 %, ed e' SOPRA la soglia dell'1 % di DICIASSETTE volte** | **E SI SA PERCHE', DAL CODICE E NON DA UNA CONGETTURA:** la cura di `cs_floor` del 2026-09-16 (categoria D, nessun flag) ha sostituito la scala **ASSOLUTA** `1/GAMMA^2 = 400` con la scala **RELAZIONALE** `_Lam = mean(I)` (`:2914`, `:2921`). Con `400`, `I ~ 1e-7` dava `sqrt(I/400) ~ 1e-5`: **`cs` era inchiodato a `CS_M`.** Con `mean(I)`, il rapporto `I/mean(I)` e' **O(1) PER COSTRUZIONE, a QUALUNQUE densita'**. **`cs` non e' diventato vivo perche' il sistema e' maturato: e' diventato vivo perche' la SCALA e' diventata relazionale.** **⚠ E COSA NON DICE:** non dice che `tau = d/cs` sia **fisicamente distinguibile** da `tau ∝ d` nei run veri — quello richiede il CONFRONTO, non la dispersione. Dice che **la premessa che lo escludeva e' caduta**: la soglia dell'1 % era il criterio scritto, e **oggi e' superata**. **CRITERIO DI CHIUSURA: rifare il confronto `tau = d/cs` contro `tau ∝ d` su >= 4 semi, ora che `cs` ha varianza.** **E TOCCA IL FRONTE `A`:** il ramo `--tau-luce` era giudicato *«un ramo su `tau ∝ d` travestito»* — **quel giudizio va rifatto.** **LIMITE: un seme, 120 passi, una scena**, e il precedente del 2026-09-15 insegna che quel rapporto **cresce col tempo**: va dichiarato con l'istante. -> `doc/REFERTO_gauge_vuoto.md` par.1 |
| **Z40** | **⚠ `A2` E' VIOLATO DA `Lam = mean(I)`, E LA VIOLAZIONE E' LA RAGIONE PER CUI IL PEZZO FUNZIONA** (2026-09-18; la tensione esisteva in `doc/ASSIOMI.md` APERTO #2, **oggi ha un numero**) | `_cs_nodo` (`:2914`, `:2921`) costruisce la scala di `cs` come **`mean(I)`, una media sulla PROPRIA popolazione** — **esattamente la scorciatoia globale che `A2` vieta**. **E FUNZIONA:** `cs_std/cs` passa da **0.0086 %** (scala assoluta `1/GAMMA^2 = 400`) a **17.6 %** (`Z39`), cioe' **`cs` passa da INCHIODATO a VIVO**, fattore **~2050**. **Non e' una violazione tollerata: e' una violazione PRODUTTIVA**, ed e' la piu' scomoda del documento | **LE DUE VIE SONO INCOMPATIBILI, e nessuna e' gratis:** *(a)* **`A2` ammette le medie globali DERIVATE** — ma allora deve **dichiarare cosa distingue `mean(I)` da una scorciatoia**, e oggi quella distinzione **non esiste scritta**; *(b)* **`cs_floor` si rifa' con `peq`**, lo sfondo diffuso **locale** (Legge I, `:265`) — ma **`peq` nasce `NaN` e va calibrato** (`:1972`), e il ramo `peq` -> `rho` e' gia' cablato in due punti perche' ai primi passi non c'e': **si ricadrebbe su `rho`, cioe' su una grandezza diversa**. **CRITERIO DI CHIUSURA: si sceglie (a) o (b) e lo si SCRIVE — una tensione dichiarata e non decisa non e' un fronte, e' un alibi.** **NON si chiude «tanto funziona»: e' precisamente l'argomento che `A2` esiste per rifiutare.** -> `doc/ASSIOMI.md` A10/APERTO #2 |
| **Z41** | **⚠ `median(|f|)` FA TRE MESTIERI, NON DUE: E' ANCHE IL ROMPI-ANELLO. NESSUN RIFERIMENTO ASSOLUTO E' CABLABILE OGGI** (2026-09-18, blob `f8f46683`, 1 seme, 120 passi) | **IL NUMERO CHIESTO C'E':** passando da `median(|f|)` a `cs_nodo/d_nodo`, **`x` si moltiplica per `0.01503`** (a `CS_M/d_nodo`: `0.01228`) — il nodo tipico va da `x = 1` a `x ~ 0.015`, e poiche' **`TAU_LOC = 1.0` (`:251`, nessuno smorzamento)** **`r` andrebbe da `1.000` a `~0.021`**, cioe' **`dt_n = DT*r` scenderebbe di ~47 volte** — **e non e' un riscalamento costante: il fattore varia di 4 volte fra passi** (`std/med 0.70`). **`CS_M/LAM` E' SQUALIFICATO SENZA GIRARE NIENTE:** `LAM` e' **fisso** (`:146`, riassegnato solo da CLI), dunque `CS_M/LAM = 2.5` **e' un numero**, varianza nel tempo **0 esatta**. **E IL DATO PIU' INFORMATIVO NON E' QUELLO CERCATO: il riferimento ATTUALE e' SEI VOLTE PIU' VOLATILE dei candidati** — `std/med` **0.6234** contro **0.0974**/**0.0986**: **oggi `r` e' misurato contro un metro che oscilla del 62 % da un passo all'altro, e quell'oscillazione e' divisa via per costruzione.** **VINCOLO 3 (A3): solo l'ATTUALE e' pinnato**, `median(x) = 1` **a dieci cifre** (identita'); i candidati fluttuano di un fattore 3. **E la RISERVA del giro scorso e' CHIUSA: nodi isolati (`d_nodo -> LAM`, il ramo vietato) ZERO su 124 invocazioni** (P5: contato) | **⚠ MA LA STRADA SI CHIUDE, E NON PER UNA SCELTA FRA CANDIDATI: PER UNA RETROAZIONE.** **Verificato dal sorgente:** `:3389` `self.phi = _phi_t + dt_n_s*self.phivel + ...`, e `dt_n = DT*r` (`:2972`), mentre **`f = Δangle/DT`** — **diviso per il tempo di COORDINATA**. Quindi al prim'ordine **`f ∝ r`**, e si chiude l'anello `r giu' -> dt_n giu' -> fase avanza meno -> f giu' -> x giu' -> r giu'`. **OGGI NON SI CHIUDE PERCHE' `x = f/median(|f|)`: se tutti gli `f` si riscalano di λ, `x` NON cambia — GUADAGNO ESATTAMENTE 1, PER COSTRUZIONE.** Con un riferimento **assoluto** `R`, nel tratto lineare `r ~ sqrt(2)*x`, il guadagno vale **`sqrt(2)*x_misurato = 0.021-0.035`**, cioe' **<< 1**: **`x -> 0` di ~50 volte per passo, L'OROLOGIO SI FERMEREBBE** e `r` cadrebbe sul pavimento. **E un `R` scelto perche' il guadagno valga 1 sarebbe un FILO DI RASOIO e un NUMERO TARATO (A1).** **-> `median(|f|)` NON e' solo normalizzazione e gauge: e' il ROMPI-ANELLO.** *(Nel verdetto su `e342ae8` avevo scritto «la mediana e' ENTRAMBE». **Sono TRE**.)* **IL RIFERIMENTO DERIVABILE PERO' ESISTE: `cs_nodo/d_nodo`** — passa A1 (**nessuna costante: tutto stato**), A3, A6 (**`_tempo_luce_nodo` legge gia' `_cs_nodo_prev`, cioe' `cs` di t-1**) e **A10** *(vedi sotto)*; unica tensione **A2**, gia' registrata in **`Z40`**. **⚠ E SU A10 IL VINCOLO 5 DEL MANDATO VA ROVESCIATO:** `omega_clk = coerenza * r` (`:2458`) **poi** `* (cs/CS_M)^2` (`:2502`). Con `r = f*d/cs` la catena e' *(coerenza) x (ritmo PROPRIO del nodo, nessun riferimento esterno) x (dilatazione vs vuoto)* -> **UN SOLO ponte verso il vuoto**; con `r = f*d/CS_M` sarebbero **DUE confronti col vuoto moltiplicati** — **il doppio conteggio che A10 vieta.** **E questo e' un riscontro su A10 stesso: l'assioma NON dice se il «ponte» sia l'ANCORA o la GRANDEZZA che attraversa, e le due letture danno candidati OPPOSTI. Va disambiguato.** **CRITERIO DI CHIUSURA — due strade, entrambe DECISIONI DI LUCA:** *(1)* **si taglia l'anello alla radice** misurando `f` in tempo **proprio** (`f = Δangle/dt_n`), che e' **la regola permanente di CLAUDE.md par.9** sul tic locale — **ma cambia la definizione di `f`, che Luca aveva escluso in `Z36`**; *(2)* **si tiene `median(|f|)`** e si dichiara che **il difetto A10 resta aperto per una ragione ORA NOTA**, non per distrazione. **Non ho un criterio scritto prima che scelga: la differenza e' di FISICA.** **⚠ E UNA MISURA L'HO SBAGLIATA E LA RITIRO:** la regressione parziale `E3` (`-0.92`, sd `0.41`) e' **degenere per costruzione** — dentro un passo **`r` e' funzione DETERMINISTICA di `f`**, quindi a `f` fissato `r` non ha varianza residua: e' collinearita', non fisica. **E1 (`+0.476`) ed E2 (il nullo, `+0.473`) COINCIDONO: la correlazione grezza e' tutta autocorrelazione.** **La domanda non e' decidibile trasversalmente: la decide il CODICE.** **NON CABLATO NIENTE, nessun numero nel simulatore.** -> `doc/REFERTO_frequenza_riferimento.md` |
| **Z42** | **✅ CURATO E SIGILLATO 10/10 — L'ANELLO ISTANTANEO DI `ritmo()` E' ROTTO. `med` si legge dal passo PRECEDENTE** (2026-09-18, blob **`f8f46683` -> `a1ae5090`**, categoria D: nessun flag) | **IL DIFETTO ERA ESATTO, NON APPROSSIMATO:** `med = median(|f|)` era calcolato **da `f`** e usato **su `f`**, nello stesso istante — **A6** (`f` e `r` si determinavano a vicenda dentro il passo) e **A3** (`median(x) = 1` per identita') **nella stessa riga**. **Misurato PRIMA della cura: `max|median(x) - 1| = 0.000e+00` su 122 passi** *(esclusi i 2 degeneri di `Z33`)*: **non era «quasi» un punto fisso, lo era A MACCHINA.** **DOPO:** `p05 0.3260`, `mediana 0.9759`, `p95 2.9968`, **`max|med-1| = 12.493`**. **LA CURA NON CAMBIA IL GAUGE:** il riferimento resta `median(|f|)`, cambia **QUANDO lo si legge** | **⚠ IL PRESIDIO CHE REGGE TUTTO, e nessuno l'aveva nominato: `ritmo()` NON SCRIVE LO SNAPSHOT.** Il metodo ha **TRE call-site** e **DUE SONO DIAGNOSTICI** (`:3124` la fisica, `:6263` `_diag_completa`, `:6921`): se lo snapshot avanzasse dentro `ritmo()`, **ogni chiamata diagnostica farebbe avanzare lo stato fisico** — par.2.3 violato, e sarebbe **il QUINTO difetto di questa famiglia**. Quindi **`ritmo()` LEGGE e REGISTRA, `step()` PROMUOVE** a `:3129-3131`, accanto a `_psi_prec` e `_psi_spin_prec`. **La contaminazione e' chiusa PER COSTRUZIONE: `step()` chiama `ritmo()` PRIMA di promuovere, quindi il valore promosso e' sempre quello FISICO.** **⚠ E IL MANDATO CITAVA `Z33` COL SEGNO ROVESCIATO:** diceva *«`Z33` e' nata dall'aggiornamento DOPO il consumo»*; **`680d069` ha misurato il contrario** — promuovere **dopo** e' cio' che **fa valere A6**; prima confronterebbe lo stato con se' stesso, `f = 0` sempre. **Il pattern di `:3129-3131` e' quello DA SEGUIRE.** **UNO SCALARE, NON L'ARRAY: A8b CHIUSA PER COSTRUZIONE** — `med` non ha lunghezza, quindi l'estensione a mitosi/`semina`/`nuova_massa` **non serve**; e' esattamente cio' che manco' a `_cs_nodo_prev` (**71.88 %**) e `_psi_spin_prec` (**95.33 %**). **IL PRIMO PASSO riusa la convenzione gia' presente** (`:2021-2026`, `np.ones(n)`, contata), **e il fallback NON e' `median(|f|)` corrente: sarebbe il difetto stesso.** **⚠ E UN PRESIDIO CHE LA MISURA HA IMPOSTO: non si promuove un `med` sul PAVIMENTO `1e-9`** — non e' una misura, e' la protezione da divisione per zero, e promuoverlo renderebbe **la regolarizzazione il gauge del passo dopo**. **Quanto costerebbe, misurato PRIMA: il rapporto `med_t/med_(t-1)` ha `max = 4.81e+07`, ed e' ESATTAMENTE il passo che segue un gauge degenere.** **Senza quel ramo `Z33` non sarebbe sparita: si sarebbe ROVESCIATA**, da «tutti sul pavimento» a «tutti in saturazione». **Misurato dopo: sul pavimento 2, NON promosso 2 — ha sparato 2 volte su 2.** **SIGILLO 10/10** (`csv/_seal_fork/_sigillo_anello.py`): **`P1` e' una BYTE-IDENTITA' VERA** — `n: 444 CONTRO 444`, 7 array su 7 **shape uguali**, `max|A-B| = 0.000e+00` — **e valida anche il wrapper: se avessi ricostruito `f` male, sarebbe fallito.** `P7`: **il tetto NON si e' spostato** (`1.4142130` contro `1.41419`). **⚠ E IL SIGILLO E' FALLITO AL PRIMO GIRO PER UN DIFETTO MIO, committato PRIMA di aggiustare (`c818208`):** estraeva il blob con **`HEAD:soliton_simulator.py`** mentre `HEAD` era gia' il commit della cura — **ho confrontato il codice curato CON SE STESSO**, e `P2` dava **`max|A-B| = 0.000e+00` con SHAPE UGUALI**: **la trappola gia' catalogata, col segno ROVESCIATO** *(la' lo zero era mancanza di confronto, qui identita' perfetta per la ragione sbagliata)*. **La riga delle shape stampata PER PRIMA l'ha reso leggibile in un colpo d'occhio.** E' un difetto di **STRUTTURA**: un riferimento ancorato a `HEAD` **si sposta col lavoro** — la stessa ragione per cui il gate e' ancorato al **BLOB** (par.2.6). -> `doc/REFERTO_anello_istantaneo.md` |
| **Z43** | **⚠ APERTA — ED E' UNA DECISIONE SULLA DEFINIZIONE DEL TEMPO, NON UNA QUESTIONE TECNICA: spetta a Luca** *(qualifica aggiunta il 2026-09-18 su richiesta del mandato)*. **⚠ E NON E' UN SINTOMO DI `Z9`, contrariamente a quanto la lettura ④ ipotizzava: `Z44` lo esclude.** I passi degeneri sono **13 su 124**, e solo **2** hanno `median(|f|) = 0`; il rapporto `med_t/med_(t-1)` ha **`p05 0.482` e `p95 2.097`** — **percentili su 123 passi, che due valori estremi non possono spostare** — e **l'11.4 % dei passi sta fuori da `[0.5, 2]`**. **Curare `Z9` toglierebbe i 13 passi degeneri, NON l'oscillazione del gauge negli altri 111.** *(LIMITE DICHIARATO: non ho ricalcolato quei percentili escludendo esplicitamente i passi degeneri — l'argomento e' di ROBUSTEZZA, non una misura dedicata.)* **`Z33` risale a `Z9`; `Z43` NO.** — IL METRO RESTA BALLERINO: rotto l'anello, la dispersione del gauge PASSA DENTRO `r`** (2026-09-18, blob `a1ae5090`) | **PREVISTA PRIMA DI MISURARE** (task history `fd198ba` §1.6) **e PRIMA DI CABLARE** (previsione 5, `631ff15`), **e confermata PIU' FORTE della stima**: avevo previsto `p05 ~0.53 / p95 ~2.10` dalla sonda sulla traiettoria VECCHIA; **misurato sul codice curato: `p05 0.326`, `p95 2.997`** — **un fattore ~9 fra il 5o e il 95o percentile.** **La causa e' nota e misurata: `median(|f|)` oscilla del 62 % fra passi** (`std/med 0.6234`), e prima quella oscillazione **veniva divisa via per costruzione** dall'anello stesso | **E SI VEDE NEL CODOMINIO: il pavimento assoluto `1.414e-06` ORA SI TOCCA** (`min` da `1.293e-04` a **`1.41421e-06`**), mentre **il tetto NON si e' spostato** (`r_unit` e la forma intatti). **La popolazione arriva ai bordi.** **NON E' UN'OBIEZIONE ALLA CURA:** A6 non e' negoziabile, e **un punto fisso ESATTO e' peggio di un metro mobile**. **Ma e' un fatto nuovo e va deciso.** **CRITERIO DI CHIUSURA:** stabilire se un gauge che oscilla del **62 % fra passi** sia **il metro che vogliamo**. **E' la stessa domanda di `Z36`, spostata di un livello** — li' *«un avanzamento di `4e-04` per passo che fluttua del 65 % e' l'orologio che vogliamo?»*, qui *«un METRO che fluttua del 62 % e' il riferimento che vogliamo?»*. **⚠ E le strade note sono gia' state chiuse:** un riferimento **assoluto** non e' cablabile (**`Z41`**: `median(|f|)` e' anche il rompi-anello, guadagno `0.021-0.035` -> l'orologio si ferma) e la **cucitura** toglie il 98 % del segnale (**`Z37`**). **Resta la via che `Z41` ha nominato e che Luca non ha ancora deciso: `f` misurato in tempo PROPRIO (`f = Δangle/dt_n`), che taglierebbe l'anello alla radice e renderebbe un riferimento assoluto possibile — ma cambia la definizione di `f`.** **LIMITE: un seme, 120 passi, una scena.** -> `doc/REFERTO_anello_istantaneo.md` par.4 |
| **Z44** | **✅ CHIUSA — `f = 0` NON E' FISICA: E' IL TRANSITORIO DI NASCITA. E «META' DEI NODI» NON ESISTE** (2026-09-18, blob `a1ae5090` **invariato**, nessuna cura) | **LA PREMESSA CADE PRIMA DELLE LETTURE: la frazione VERA di nodi con `f = 0` e' `0.4 %`** (mediana sui passi degeneri, cioe' **UNO o DUE nodi su ~450**), e i passi degeneri sono **13 su 124**. **Non esiste nessun regime «a meta'»**: ci sono **due passi di NASCITA** — il **passo 0** (100 %, non esiste un prima) e il **passo 5** (**94.09 %**, subito dopo l'iniezione di 360 nodi senza genitore) — e **undici passi con 1-2 nodi**. *(«Meta'» era ARITMETICA della mediana, non una misura: il mandato stesso chiedeva di non confonderle.)* | **LA CATENA SI CHIUDE TUTTA, e `③` e `④` REGGONO INSIEME — sono LO STESSO FATTO A DUE LIVELLI, non due spiegazioni in competizione** *(previsto nel task history `0d9abac` prima di misurare)*: `nodo APPENA NATO -> eta ~ 0 -> ramp ~ 0 -> pesi del kernel ~ 0 -> psi_spin = **0 ESATTO** -> `angle(0) = 0` **CONVENZIONE numpy** -> `f = 0` -> gauge degenere`. **③ REGGE NELLA FORMA PIU' FORTE:** `|psi_spin|` dei nodi fermi e' **`0.0000e+00` anche nel MASSIMO**, in **12 passi su 13** *(unica eccezione il passo 32: `1.1e-06`, comunque **600 volte** sotto la mediana della popolazione)*. **`f = 0` non dice «il tempo e' fermo»: dice «la fase non e' definita». Non e' una misura, e' un valore di ritorno.** **④ REGGE:** `ramp` dei fermi fino a **66 volte** sotto la popolazione (`eta` 0.0141 contro 0.9317 al passo 117), **e lo scarto CRESCE col tempo** (11 -> 66): la popolazione matura, loro sono appena nati. **① NON regge — e il suo NO e' informativo: Jaccard ~ 0** fra passi degeneri consecutivi: **non sono gli stessi nodi, quindi NON e' congelamento ma un TRANSITORIO DI NASCITA**. **② NON regge**, `|delta psi_spin| = 0.0000e+00`: **ma non e' un'ipotesi concorrente, e' la CONSEGUENZA di ③** — se `psi_spin` e' zero adesso ed era zero prima, la differenza e' zero. **-> `Z33` RISALE A `Z9`: e' il transitorio di maturazione del kernel sui nodi nuovi, non un difetto del tempo.** -> `doc/REFERTO_chi_non_ruota.md` |
| **Z45** | **✅ CHIUSA — L'IPOTESI MATERIA/ANTIMATERIA CADE. L'universo e' BILANCIATO, e il bilanciamento NON viene dalla mitosi antichirale** (2026-09-18, blob `a1ae5090`) | **⑤a — I TRE RAMI, CONTATI e non dedotti (A8):** `semina`/`nuova_massa` (`:1850`, `rng.choice([-1,1])`) **4 chiamate, 440 nodi**; mitosi **UGUALE** (`:3945`) **126 chiamate, 19 nodi**; **Schwinger OPPOSTO** (`:4066`) **5 chiamate, 6 nodi** *(contato dal suo UNICO marcatore, `_eredita_spinore_figli(.., segno=-1)`)*. **Frazione `+1`: `0.4375` (n=80) -> `0.5066` -> `0.5054` (n=459)**, `min 0.4375 / mediana 0.5056 / max 0.5078`. **NON e' un universo di sola materia: e' bilanciato entro lo 0.8 %.** **E il ramo Schwinger GIRA davvero, ma e' l'1.3 % della popolazione.** **⚠ IL BILANCIAMENTO VIENE DA `rng.choice([-1,1])` ALLA NASCITA, che fa il 94.6 % dei nodi — NON dalla mitosi antichirale, che ne fa l'1.3 %** *(il mandato lo attribuiva alla Schwinger; la correzione era nel task history `0d9abac`, scritta PRIMA di misurare)* | **⑤b — MISTO: L'IPOTESI CADE, e cade sui due soli passi che hanno POTENZA STATISTICA.** **Passo 0: scarto `+0.000000` ESATTO** — e non poteva che esserlo, **tutti** i nodi sono fermi, quindi la popolazione dei fermi **E'** la popolazione: **il valore sotto ipotesi nulla ha intercettato una TAUTOLOGIA**. **Passo 5: scarto `+0.0125` su 414 nodi**, cioe' nulla. **Gli altri undici passi hanno UNO o DUE nodi: con `n = 1` la frazione DEVE valere 0 o 1, quindi gli scarti `±0.49` sono ARITMETICA, non segnale** — e sono **bilanciati** (sui sei passi a `n = 1`: **tre `+1` e tre `-1`**). **⚠ E IL CANALE ESISTE DAVVERO, l'avevo mancato e l'ho dichiarato PRIMA di misurare:** `CALORE_VETTORIALE = True` (`:270`) e `scuoti_vuoto` (`:538-540`) fa `phivel[:n] += rng.normal(0,1)*ampiezza * perc_chi` **a ogni passo** — ma e' **un SEGNO su un rumore SIMMETRICO**: la marginale di un `+1` e di un `-1` e' **identica**, e puo' agire **solo attraverso le correlazioni**. **La misura conferma che sul singolo nodo non agisce.** *(Verificati e SPENTI: `OROLOGIO_SEGNO`, `TEMPO_SEGNO`, `CHI_BASC`, `CHI_DA_SPINORE`, `COMPAT_CHI`, `SPIN_POSITIVI`. Accesi: `CALORE_VETTORIALE`, `CHI_CORE`, `TORS_4PI`.)* **L'indizio numerico era una COINCIDENZA, ed e' ESCLUSO.** -> `doc/REFERTO_chi_non_ruota.md` par.3 |
| **Z46** | **⚠ IL 93 % DEI NODI NON INVECCHIA, SONO SEMPRE GLI STESSI, E SONO LE TRE MASSE. E IL GAUGE DEL TEMPO E' LA COSTANTE `1e-9`** (2026-09-18, blob `a1ae5090` **invariato**, run CONTINUO a 1200 passi, seed 900, **`--tau-luce` NON CERTIFICATO** e `--chi-basc` attivo, **un seme**) | **NON E' «META'»: e' il `92.7 %` e NON CALA** (`1116` su `1204`; frazione `0.9308` -> `0.9269`). **E non e' «mediana bassa»: `p05 = p25 = MEDIANA = p75` sono IDENTICI a ogni istante** — **tre quarti della popolazione allo stesso identico valore** — mentre **solo il `p95` si muove**, da `0.886` a `13.03`. **SONO SEMPRE GLI STESSI: `Jaccard = 1.0000` su TUTTE le transizioni** (`120->130->470->800->840`), **primo contro ultimo `1.0000`, `|B\A| = 0` ovunque.** **IL FALSIFICATORE DELLA MITOSI E' ESCLUSO IN MODO TOTALE: 4 nodi nuovi in 1200 passi, e ZERO di loro e' fermo** *(eta' anagrafica ESATTA, non stimata: i nodi si appendono in coda, quindi l'indice e' l'ordine di nascita)*. **NON e' il transitorio di nascita di `Z44`** | **⚠ CHI SONO, ed e' il dato che cambia la natura della voce:** i fermi hanno **grado `371 / 371 / 371`** (p25/mediana/p75, cioe' quasi tutti lo stesso) e **raggio mediano `8.004` = `sep`**: **SONO LE TRE MASSE SEMINATE**. I `87` mobili hanno **grado `9`** e **raggio `2.5`**: sono **la regione centrale**. **Un fattore 41 sul grado.** *(Conferma indipendente dal tracking del batch: `TRACK accr=1116`, lo stesso numero.)* **⚠ E LA CAUSA NON E' `eta` NE' `ramp`: E' A MONTE.** Il loro `r` vale **`1.414213e-06`**, cioe' **`r/r_floor = 1.0000` su 4 intervalli su 4**, e **anche il p95 e' al pavimento** — **il PAVIMENTO ASSOLUTO di `ritmo()`**, non «lento». **PREVISTO PRIMA DI APRIRE I `.pkl`** (task history `49c0c28`): dalla sola crescita di `eta` avevo ricavato `rapporto = 1.0025`; **misurato `1.0000`.** **⚠ E IL ROVESCIAMENTO: `median(|f|) = 1.000000e-09` A TUTTI GLI ISTANTI — il PAVIMENTO `max(median, 1e-9)` E' ATTIVO**, perche' **il 49-61 % dei nodi ha `f` ESATTAMENTE zero**. **Il gauge del tempo proprio non e' piu' una statistica del sistema: E' IL NUMERO `1e-9`** — alla lettera cio' che CLAUDE.md par.9 avverte (*«alle scale simulabili la regolarizzazione diventa il parametro fisico»*). **E il sistema si SEPARA IN DUE POPOLAZIONI CHE NON SI PARLANO: il 93 % a `x = 0` (pavimento, `eta` congelata, `psi_spin` PIATTO a `6e-06`) e il 7 % a `x = 10^7-10^8` (saturazione, `eta` a `15.9`, `psi_spin` x27). NON C'E' PIU' NESSUNO IN MEZZO.** **⚠ COSA NON DICE: non so PERCHE' i nodi a grado 371 abbiano `|psi_spin|` MILLE VOLTE piu' debole di quelli a grado 9. E' il fatto piu' strano della misura e NON HO UNA SPIEGAZIONE MISURATA — e non la invento.** **E `Z44` NON e' smentita: NON SI TRASPORTA.** La' i nodi con `f = 0` erano **1-2 su 450** ed erano **neonati**; qui sono il **53-61 %** e sono **le masse**: **due popolazioni diverse, e vanno citate entrambe con la loro scena (A3c).** **NESSUNA CURA, nessun cablaggio, nessun run nuovo. Il testo riscritto di `Z9` e' una PROPOSTA nel referto, non e' cablato.** -> `doc/REFERTO_chi_non_invecchia.md` |
| **Z47** | **⚠ PROGETTO DI LUNGO PERIODO — NON INIZIATO. GEOMETRIA RELAZIONALE SENZA EMBEDDING: `pos` e' l'unico SFONDO rimasto, e nessuno lo aveva scritto** (registrato 2026-09-18, blob `a1ae5090`, **sola documentazione, nessun codice toccato**) | **IL FATTO, VERIFICATO RIGA PER RIGA DAL DISCO — e i punti VIVI sono QUATTRO, non tre:** `:1958` `_allaccia` usa **`cKDTree(self.pos)`** e **decide LA TOPOLOGIA**; `:4230`/`:4267` `memoria_hebbiana_moto` usa `v = pos[j] - pos[i]` per **`dirarc` e `grad_tw`**, che **scrivono `mem_mot` e `_nb`**; `:4425` `v_rel` da' **`dir_radiale`/`dir_laterale`** (gravita', frame-drag); `:1498-1499` `chiralita_core_locale` usa una **sfera EUCLIDEA di raggio `r`** *(e `--chi-core` e' nella config in uso)*. **⚠ E UN QUARTO che il mandato dava per condizionale: `:3409-3410`, il centro di massa pesato `cmv` e `r_cm` del Kuramoto — e' sotto `if K_SYNC != 0.0`, MA `K_SYNC = 1.0` di DEFAULT (`:199`): E' VIVO.** *(Davvero morti: `:4338` sotto `LS_AZIM = False` (`:624`) e `:4199` sotto `L_CONSERVA = False`, marcato «ERRATA, NON usare» (`:549`).)* **E `pos` INSEGUE `d`** (`rilassa_disegno`, `EMB_IT = 3`, `:1004`/`:4153`): **c'e' un ANELLO `d -> pos (approssimato, 3D) -> topologia + direzioni -> d`** | **TRE CONSEGUENZE:** **(1) LA DIMENSIONE 3 E' FISICAMENTE RILEVANTE** — `_allaccia` cerca per **RAGGIO**, non per `k` vicini, e `N_vicini ~ densita'*rc^D`: **in 6D il grado esploderebbe** *(con `k`-NN sarebbe stata indifferente: non lo e')*; **(2) VIOLA A5** — due nodi si allacciano perche' **vicini NEL DISEGNO**, anche se sul grafo sono lontanissimi: **un legame puo' nascere fra punti che non si sono MAI parlati**; **(3) un grafo arbitrario NON si rappresenta esattamente in 3D** *(gia' a cinque nodi le distanze sono sovradeterminate)*: **l'errore dell'embedding RIENTRA nella fisica**. **-> IL MODELLO NON E' BACKGROUND-INDEPENDENT: e' relazionale nella DINAMICA** *(`psi`, `cs`, `d`, `phi`, le forze e il tempo proprio non leggono MAI `pos`)* **ma LA TOPOLOGIA e LE DIREZIONI passano da un embedding.** **IL PROGETTO (idea di Luca): la geometria si ricostruisce dalle sole `d`** — *un nodo non da' spazio; due danno UNA DIREZIONE; tre un PIANO e un ANGOLO; quattro un VOLUME e con esso la CHIRALITA', il segno di `(v1 x v2)·v3`* — **e tre distanze FISSANO un triangolo (teorema del coseno): gli angoli fra gli archi di un nodo si ricostruiscono dalle sole `d`, senza coordinate.** **LA FORMA:** un **RIFERIMENTO LOCALE** per nodo dagli angoli fra i suoi archi; le direzioni **espresse in quel riferimento**, mai in coordinate globali; una **CONNESSIONE** per confrontare riferimenti vicini *(**il precedente esiste gia': `_spinor_lift` fa la cucitura di fase per lo spinore, misurata a `0.35 %` di salti — e' TRASPORTO PARALLELO; servirebbe l'analogo per la GEOMETRIA**)*; e **l'allaccio dalla TOPOLOGIA** (vicini-dei-vicini) invece che dal KD-tree. **COSA SI OTTERREBBE:** background-independence **VERA**, nessuna dimensione scelta *(e la dimensionalita' potrebbe EMERGERE, misurabile come **dimensione spettrale** del grafo, `N(k) ~ k^D` con un random walk: **non e' nel codice**)*, e **A5 rispettata** sulla nascita degli archi | **⚠ IL COSTO, e va scritto perche' DECIDE:** non e' una modifica, e' la **riscrittura di QUATTRO settori** (`_allaccia`, `memoria_hebbiana_moto`, `chiralita_core_locale`, **e il `cmv` del Kuramoto**); **la CONNESSIONE non esiste e va COSTRUITA — e ogni scelta li' dentro e' una LEGGE, da DERIVARE (A1), non da prendere**; **due nodi vicini hanno riferimenti diversi e possono essere INCOMPATIBILI: quella E' LA CURVATURA** — **non si ottiene uno spazio globale, si ottiene una connessione locale** *(ed e' esattamente cio' che serve, ma va capito PRIMA di scrivere)*; **⚠ SAREBBE UN SISTEMA NUOVO, non quello attuale corretto: TUTTO CIO' CHE E' STATO MISURATO NON SI TRASPORTA — sigilli, predizioni, confronti: DA RIFARE.** **Stima: settimane, non ore.** **⚠ CRITERIO DI AVVIO — E NON E' UN'OPINIONE: «QUANTO MENTE L'EMBEDDING?»** Si misura **`|pos_i - pos_j| / d_ij`** per arco *(mediana, p05, p95)* **E la STABILITA' DELL'ORIENTAMENTO fra passi** *(l'angolo fra `dirarc(t)` e `dirarc(t-1)`)*. **⚠ E la seconda conta PIU' della prima: le direzioni sono NORMALIZZATE (`v/L`), quindi l'errore sulla LUNGHEZZA SI CANCELLA e resta quello sull'ORIENTAMENTO — ed e' quello che entra in `grad_tw` e in `_nb`.** **LE DUE LETTURE:** *errore piccolo e orientamento stabile* -> **il guadagno e' TEORICO: si dichiara il limite e NON si riscrive niente**; *errore grande o orientamento che sobbalza* -> **la motivazione e' MISURATA e il progetto diventa prioritario** *(e spiegherebbe anche **`Z43`**, il metro che oscilla del 62 %)*. **LA MISURA NON SI FA QUI: e' nel mandato parallelo sull'embedding.** **NON INIZIATO, nessun codice toccato, nessun fronte aperto.** -> `doc/ASSIOMI.md` (la dichiarazione) |
| **Z48** | **⚠ QUALIFICATA il 2026-09-18: VALE PER IL BATCH. Il «guscio» qui misurato e' L'ANELLO DELLE MASSE SEMINATE, e NON e' la struttura dinamica osservata nella scena VIDEO — sono DUE OGGETTI DIVERSI.** Quello di `Z48` sta a `r ~ 8 = sep`, e' **FERMO** (al pavimento di `ritmo()`) ed e' fatto dei nodi che non maturano; quella osservata **nasce FRA le masse, si gonfia, le INGLOBA e poi si RICOMPRIME**. **E LA RAGIONE PER CUI IL BATCH NON POTEVA CONTENERLA E' MISURATA IN QUESTO STESSO REFERTO: `8 nodi e 10 archi in 1200 passi` — LA MITOSI E' FERMA**, mentre la scena video fa **`5112` mitosi** con `n` da `2391` a `7503`. **La struttura osservata e' fatta di MATERIA NUOVA, e nel batch quella materia NON NASCE: nessuna misura sul batch poteva trovarla.** **Senza questa riga, fra sei mesi si legge «il guscio e' un artefatto» e la struttura vera non viene piu' cercata.** -> la misura sulla scena giusta e' **`Z49`**. --- IL GUSCIO ESISTE E SI APPROFONDISCE, MA E' FATTO DEI NODI CHE NON MATURANO. E IL GAUGE E' SUL PAVIMENTO NEL 99.76 % DELLE CHIAMATE** (2026-09-18, blob `a1ae5090` **invariato**, run CONTINUO a 1200 passi, seed 900, **un seme**, **`--tau-luce` NON CERTIFICATO**, `--chi-basc` attivo) | **⚠ IL NUMERO CHE VIENE PRIMA DI TUTTI, dai contatori A8: `_ritmo_med_sul_pavimento = 2879` su `_ritmo_chiamate = 2886` — IL 99.76 %.** **`median(|f|)` cade sulla costante `1e-9` in quasi OGNI chiamata: non e' un caso degli istanti campionati, e' tutto il run.** **Il gauge del tempo proprio, in questa scena, NON E' una statistica del sistema: e' un numero.** *(E la cura di `Z42` ha lavorato **1202 volte**: `_ritmo_med_non_promosso = 1202` — milleduecento volte il pavimento NON e' stato promosso a gauge del passo dopo.)* **① LA MATURAZIONE, ed e' `Z46` vista da un'altra grandezza:** `p05 = ramp MEDIANO = 0.000200`, **identici e fermi** ai passi 120/800/1200, mentre il **`ramp p95` cresce di 15 volte** (`0.0177 -> 0.2607`) e **`Lam` di 1300**. **Nessun nodo con `|psi| = 0`** *(la previsione diceva «quasi nulla»: e' **ZERO** — `Z44` contava i neonati, e qui la mitosi e' ferma, 8 nodi in 1200 passi)* | **② IL GUSCIO ESISTE:** minimo di `|psi|` a **`r = 8.55`** al passo 120, poi **`r = 7.21`** a 800 e 1200 — **si sposta verso l'interno e poi RESTA FERMO** — e **si APPROFONDISCE**: il rapporto `|psi|` dentro/fuori **RADDOPPIA** fra 800 e 1200 (`~2200` -> `~4300`). *(La coerenza `|<n>|` NON distingue: vale `~1` dentro e fuori.)* **⚠ E I TRE FALSIFICATORI, APPLICATI PRIMA DI DESCRIVERE:** **(b) NON e' un minimo di STATISTICA** — il bin del minimo ha **piu'** nodi della mediana per bin (179/10, 31/11, 36/9); **(c)** al passo 120 il minimo era **AL BORDO** (0 bin oltre: **li' non era una parete**), a 800 e 1200 ci sono **4 bin oltre**; **(a) SCATTA, E' NETTO, E PEGGIORA COL TEMPO: `eta` del bin minimo su `eta` INTERNA vale `0.0095` -> `0.0012` -> `0.00073`** — **i nodi del guscio hanno `eta` MILLE VOLTE piu' bassa dell'interno** | **⚠ MA LA PREVISIONE SCRITTA PRIMA (`fb87640`) DICEVA: *«se il guscio ha `eta` piu' bassa, e' il FRONTE DI NASCITA, non una parete»*. QUI LA PRIMA META' E' VERA E LA SECONDA NO, e la ragione e' MISURATA: la mitosi e' FERMA (8 nodi in 1200 passi), quindi quei nodi NON SONO NATI DA POCO — SONO I NODI CHE NON HANNO MAI MATURATO, gli stessi `1116` di `Z46` a raggio mediano `8.004`.** **Il «guscio» COINCIDE con l'anello delle masse seminate ferme al pavimento di `ritmo()`: non e' un fronte di nascita e non e' una parete, e' LA REGIONE DEI NODI CONGELATI.** **③ LE FASI — e qui la mia previsione era ROVESCIATA:** gli sfasamenti **NON convergono a `2pi/3`** *(previsto, e regge)* ma **si STABILIZZANO** (fra 800 e 1200 cambiano di `0.10/0.11/0.01`); **MA avevo previsto coerenza INTERNA > coerenza FRA masse, e il misurato e' l'OPPOSTO: FRA le masse passa da `0.235` a `0.807`, INTERNA resta a `~0.20` — QUATTRO VOLTE PIU' ALTA FRA CHE DENTRO.** **Le tre masse si allineano fra loro piu' di quanto ciascuna sia coerente al proprio interno. NON SO SPIEGARLO, E NON LO SPIEGO.** **④ IL CONTRASTO:** `rho` al centro **x3400**, `rho` al guscio **PIATTA** (`3.3e-11 -> 2.8e-11`), **rapporto centro/guscio da `3.3e+03` a `1.36e+07`: quattro ordini in 1080 passi.** Stress `max|d-d0|/d0` da `18.9` a `50.2`, **nessun NaN, nessun runaway.** **⚠ LIMITE DELLA SONDA, DICHIARATO: cerca i `.pkl` per NOME e il `400` non esiste** *(il primo watcher li mislabello'; gli istanti veri sono `120/130/470/800/840/1200`)*: **`130`, `470` e `840` NON sono stati letti** — tre istanti bastano per «si sposta e si approfondisce», ma **la risoluzione e' minore di quella disponibile.** **NESSUNA CURA, NESSUN VERDETTO DI FISICA, NESSUNA IDENTIFICAZIONE.** -> `doc/REFERTO_struttura_1200.md` |
| **Z49** | **IL CICLO C'E', MA NON E' NEL CORPO: E' NELLA CODA. E l'ANELLO SI SFALDA mentre il CENTRO SI ACCENDE di cinque ordini** (2026-09-18, blob `a1ae5090` **invariato**, scena VIDEO, **400 frame = 2400 passi**, `n` 2391 -> 8018, **un seme**, **`--tau-luce` NON CERTIFICATO**, `--chi-basc` attivo) | **LA SCENA E' LA STESSA, confermato a quattro cifre:** `d MEDIA` misurata `0.9275 / 1.4502 / 1.6739 / 1.7537 / 1.4871` contro `0.934 / 1.454 / 1.676 / 1.753 / 1.483` della tabella dei fotogrammi. **⚠ MA IL CICLO NON E' DOVE SEMBRAVA:** `d MEDIANA` **SALE SEMPRE** (`0.863 -> 1.351`), mentre **`d p95` CROLLA da `6.40` a `2.95`** e con lui la MEDIA. **LA DISTANZA TIPICA NON SI RICOMPRIME MAI: la «ricompressione» e' IL RIASSORBIMENTO DELLA CODA LUNGA.** **E' esattamente la trappola del par.4 del mandato** *(«se bimodale, non riportare mediane»)*: **media e mediana dicono cose OPPOSTE, e solo la coppia descrive il fenomeno.** **E la dilatazione non inverte soltanto: RIMBALZA** — `-7.44 %` (370), `-2.70 %` (375), `-4.21 %` (380), **`+2.80 %` (385)**, `-1.13 %` (400): **la tabella si fermava al 375 e non poteva vederlo** | **⚠ LA REGIONE INTERNA SI SVUOTA E POI SI RIEMPIE** (`900 -> 291 -> 224 -> 221 -> 559 -> 907` nodi) **e SI ACCENDE: `rho_spin` da `5.1e-07` a `5.6e-02` — CINQUE ORDINI in 780 passi — e `|psi|` da `3.2e-03` a `0.666`.** **Il minimo di popolazione (frame 270) COINCIDE col massimo di `d MEDIA`: riempimento e ricompressione sono lo stesso intervallo.** **⚠ E L'ANELLO SI SFALDA: il GRADO passa da `496` a `2`** (`496/160/87/2/2/2`) **mentre la regione interna lo mantiene** (`127 -> 109`). **Non e' che la struttura «ingloba» le masse: le masse SI DISGREGANO e la materia si concentra al centro** *(descrizione dei numeri, NON un meccanismo: il meccanismo non e' misurato)* | **⚠ DI COSA E' FATTA — E' L'OPPOSTO DI `Z48`, e il discriminante lo conferma:** `eta` INTERNA `28.60` contro `eta` ANELLO `9.81` — **la regione interna e' TRE VOLTE piu' matura** — e **i nodi NUOVI stanno piu' nell'ANELLO (`76 %`) che al centro (`38 %`)**. **E `r` ricavato da `eta += DT*r` vale `1.08 -> 1.41` nella regione interna, cioe' `10^6` VOLTE IL PAVIMENTO: NON E' FERMA.** *(La previsione, fissata prima: «se il loro `r` valesse `1.414212e-06`, materia nuova sarebbe sbagliato». **Non lo vale.**)* **E `r` dell'ANELLO SCENDE (`0.71 -> 0.37`): il centro accelera mentre l'anello rallenta.** **⚠ `perc_chi`: LA SEPARAZIONE SI OTTIENE SENZA CONTATORI.** `frac +1` vale `0.000` al frame 10, mentre alla semina `rng.choice([-1,1])` darebbe `~0.50`: **`chi_basc` ha GIA' RISCRITTO tutti i 2391 nodi entro il frame 10.** **L'antimateria di questa scena viene dal BASCULAMENTO, non dalla generazione** — e cresce `0.000 -> 0.1645`, coerente col `18.4 %` dei fotogrammi. *(⚠ E' una DEDUZIONE da due numeri, non un conteggio: un conteggio diretto richiederebbe contatori durante il run, e NON e' stato fatto.)* **✅ E LA MIA RISERVA SUI RAGGI ASSOLUTI CADE, e lo dico:** `R_anello` MISURATO varia solo dell'`8 %` (`7.67 -> 8.04 -> 7.37`), quindi **lettura ASSOLUTA e COMOVENTE danno gli stessi numeri**. *(Vale **solo** perche' l'anello non si e' spostato abbastanza: NON e' una licenza generale — par.4 resta.)* **E `R_anello` fa il ciclo da solo: `7.56` -> `8.04` -> `7.37`.** **La coerenza GLOBALE `|<nb>|` CROLLA** (`0.995 -> 0.190`) **mentre la `coer_l` LOCALE sale a `0.669`: due grandezze diverse, si citano separate e non si mediano.** **NESSUNA CURA, NESSUN VERDETTO DI FISICA, NESSUNA IDENTIFICAZIONE.** -> `doc/REFERTO_struttura_video.md` |
| **Z50** | **NON E' UNA Y: nella regione interna il modo dominante e' il DIPOLO, e l'`A_3 = 0.97` SONO LE TRE MASSE** (2026-09-18, blob `a1ae5090` **invariato**, **nessun run nuovo**: i sei `.pkl` a tre masse gia' esistenti; **`--tau-luce` NON CERTIFICATO**, **un seme**) | **⚠ E PRIMA DEL RISULTATO, UN DIFETTO MIO, trovato perche' il suo esito era ASSURDO:** avevo derivato la soglia di «regione densa» **per analogia** con `lambda_vuoto = mean(|psi|^2)` (`:486-494`), applicandola a `rho_spin`. **L'analogia NON regge: `rho_spin` ha MEDIA `1.4432e+01` contro MEDIANA `6.4175e-03`, rapporto `2249`.** **La media di `rho_spin` non e' il livello di vuoto: e' una statistica della CODA**, e selezionava **1479 nodi TUTTI a `r ~ 7.7`** *(top-200: raggio `p05/med/p95 = 6.88/7.68/8.67`)*, cioe' **l'ANELLO** — dando *«nodi interni densi = 0»* a ogni istante. **NON era un fatto sul sistema, era la soglia.** **Controprova: con la MEDIANA i nodi interni selezionati sono `623` su `811` al frame 400, con `rho_spin` mediana `5.56e-02` = 8.7 volte la mediana globale: LA REGIONE INTERNA DENSA ESISTE, era la soglia a non vederla.** *(E' P1 applicato a una soglia: **un'analogia va verificata sulla DISTRIBUZIONE che seleziona, non sulla forma della formula**. La media resta nell'output come **controesempio dichiarato**.)* | **IL RISULTATO, con soglia sulla MEDIANA e col NULLO accanto** *(`A_m` per punti casuali vale `~1/sqrt(N_eff)`, non 0)*: **CON L'ANELLO `A_3 = 0.965` contro un nullo di `0.029` — TRENTATRE VOLTE — ma sono le tre masse seminate, che stanno a `0/120/240` PER COSTRUZIONE.** **NELLA SOLA REGIONE INTERNA: `A_3 = 0.231` contro un nullo di `0.120` (meno di DUE volte), mentre `A_1 = 0.400` e' 3.3 VOLTE il nullo.** **Il modo dominante dentro e' il DIPOLO, non il tre.** **E L'ISTOGRAMMA LO MOSTRA DIRETTAMENTE: UN lobo a `+52.5/+67.5 gradi` (altezza `3.25`/`4.00` contro una media di 1), un rialzo secondario a `+172.5`, e il resto fra `0.23` e `0.86`.** **Il picco NON e' su una massa: e' FRA due masse. Tre bracci a `0/120/240` NON CI SONO.** **-> SCATTA LA TERZA LETTURA del mandato: «`A_3` grande SOLO includendo l'anello -> e' l'artefatto delle tre masse seminate».** **ROBUSTEZZA: fra soglia `10x` e `100x` la mediana i valori cambiano (`A_1` `0.400 -> 0.611`, `A_3` `0.231 -> 0.437`) MA L'ORDINE NO: `A_1 > A_3` in entrambe.** **⚠ MA LA STATISTICA INTERNA E' DEBOLE: `N_eff` scende a `21` alla soglia stretta (nullo `0.22`), e al frame 375 vale `10.9` (nullo `0.30`): li' NON si misura nulla, e NON lo leggo.** **E LA REGIONE INTERNA DENSA COMPARE TARDI: `0/0/0/0/18/370` nodi ai sei istanti** — **coerente con `Z49`**, e **tutto cio' che si dice sulla forma interna riguarda gli ULTIMI 150 frame.** **⚠ COSA NON DICE: NON dice che il video sia sbagliato.** Dice che **la firma a tre bracci non e' misurabile nella regione interna con questa definizione di «densa»**, e che `A_3 = 0.97` **e' spiegato dalle tre masse.** **E il modo che ho trovato — il DIPOLO — e' comunque una struttura ANISOTROPA, non un disco.** **NON e' un verdetto sulla predizione di Luca**, che riguarda il **CICLO** e si decide col run a due masse (in corso): **questa misura toglie UN MECCANISMO CANDIDATO (la Y topologica), non la predizione.** **NESSUNA IDENTIFICAZIONE** *(la Y a 120 gradi e' Fermat-Steiner e compare ovunque: era scritto nel task history PRIMA, proprio perche' se fosse uscita non sarebbe stata una spiegazione)*. -> `doc/REFERTO_forma_Y.md` |
| **Z8** | **Lo 0.3457 % di nodi ancora al pavimento `1e-6` NON e' caratterizzato** (2026-09-17) | Dopo il cablaggio di ① il pavimento smette di dominare, ma **non sparisce**. Non e' stato misurato **quali** nodi restino sotto: se fossero il vuoto profondo sarebbe fisiologico (come per il vincolo causale della plasticita'), se fossero sparsi sarebbe un residuo da capire | una misura della **densita' e del contrasto `rho/peq`** dei nodi al pavimento, contro quelli sopra. **Non urgente** — `A3b` si applica a un clamp che domina, e questo non domina piu' |
| **Z2** | **`spinta`: A2 e A3 sono stati tolti, A1 NO** (2026-09-17) | La correzione (2) ha sostituito `0.02*median(self.d0)*rep` con `0.02*self.d0*_rep`: cadono la statistica globale (A2) e la normalizzazione sul proprio insieme (A3). **Il coefficiente `0.02` resta un NUMERO SCELTO** («limitata, 2% per passo»): **A1 resta violato**, ed e' scritto nel codice accanto alla correzione invece di essere sostituito in silenzio con un altro numero | una **derivazione** del coefficiente da grandezze di stato, oppure la dimostrazione che quel limite e' al **confine fisico** come `tau_p >= d/cs`. **Un numero scelto che ne rimpiazza un altro non e' una bonifica** |
| **Z3** | **`peq` DEGENERE: un fallback a DUE REGIMI, scoperto da un contatore cablato apposta** (2026-09-17) | Nella plasticita' viscoelastica `peq` puo' essere `<= 1e-30`: la protezione e' contata (P5). **Misurato: 0.0000 % nei .pkl MATURI (300 passi) e 2.50 % nel run GIOVANE (40 passi).** E il contatore dell'**intersezione** dice che **il 99.91 %** degli scatti del vincolo causale, nel sistema giovane, sta **proprio su quegli archi** (207563 su 207753): li' `rho_arco = 0` e `t_visco = 0`, quindi il `max` sceglie `t_luce`. **Quindi «il vincolo scatta nel vuoto profondo» e' vero nel regime MATURO, e nel regime GIOVANE descrive invece IL PUNTO IN CUI `peq` NON E' DEFINITO.** Due cose diverse, distinguibili **solo contandole** | **non e' un difetto aperto, e' un comportamento da conoscere**: si chiude quando la frazione e' misurata su un sistema maturo **con il blob attuale** (i .pkl sono di `a44adc31`). Nessuna azione sul codice e' indicata oggi |
| **Z4** | **`_floor_d0`: SOSPESA, e i due rami violano assiomi DIVERSI** (2026-09-17) | `PAV_COM = False` (default) da' `0.05`: **A1** (numero scelto) e scala **assoluta**. `PAV_COM = True` da' `f*median(d0)`: **A2** + **A3**. **Nessuno dei due si salva aggiustando l'altro**, ed e' la ragione per cui sostituirli entrambi renderebbe `PAV_COM` **inerte**. Il pavimento **morde**: nel run di sigillo `min(d0) = 0.05` **esatto** | **tre decisioni di Luca**, in `doc/PROPOSTA_floor_d0.md`: (1) il pavimento e' un **vincolo di buona definizione** (e allora `dt_e*cs_arco` e' la candidata, e A1 la assolve) o una **legge di scenario** (e allora i due rami vanno tenuti)? (2) si accetta `PAV_COM` come **NO-OP dichiarato**? (3) si accetta un secondo snapshot `_cs_arco_ultimo` sul simulatore? |
| **Z5** | **A3 NON E' UN CASO PARTICOLARE DI A2 — risolta una delle domande aperte degli ASSIOMI** (2026-09-17) | `doc/ASSIOMI.md`, sezione APERTO #2, chiedeva se **A3 discenda da A2**. **Basta un controesempio, e il codice ne ha uno:** `u_nodo = I / media_dei_VICINI` (`:2607`) **soddisfa A2** (la media e' sui vicini topologici: nessuna scorciatoia globale) e **viola A3** (che nomina esplicitamente «media dei primi vicini» fra le statistiche di posizione vietate). **Quindi A3 e' INDIPENDENTE.** ⚠ **Ma `u_nodo` NON e' percio' da correggere:** sta dentro `_cs_nodo`, cioe' dentro cio' che **definisce** la struttura causale, e **A4** dice che quel livello si giudica a parte; inoltre la sua mediana misurata e' **1e-4**, non 1 — **il punto fisso non si forma** | **chiusa per DIMOSTRAZIONE**, non per misura: si riaprirebbe solo se A3 venisse riformulato. **✅ RIPORTATA: `doc/ASSIOMI.md` APERTO #2 e' BARRATO col controesempio** (verificato dal disco il 2026-09-18). La nota *«va riportata alla prossima revisione»* era **stale**: lo era gia'. |

### IL TEMPO DI VALUTAZIONE E' ESSO STESSO UNA GRANDEZZA FISICA

**Quattro volte, in settori diversi, lo stesso difetto: una grandezza LETTA IN UN MOMENTO DIVERSO
da quello che dichiara.**

| caso | cosa era sfasato | quanto scattava | conseguenza |
|---|---|---|---|
| `_cs_nodo_prev` | cache scartata a ogni mitosi | **71.88 %** | `cs = CS_M` costante |
| `_psi_spin_prec` | cache non estesa alla mitosi | **95.33 %** | FASE 5 / 4pi inerte per mesi |
| i due `theta` (C19) | tempo di coordinata vs proprio | — | un numero che si muove per la CONVENZIONE |
| lo sfasamento `eta` | pesi valutati a un `eta` diverso | **sempre, sui neonati** | il neonato pesava `2e-4` invece di 0 |

**Nessuno di questi produceva un errore.** Il sistema girava, i sigilli passavano, i numeri
sembravano ragionevoli. **E ogni volta si stava misurando un'altra fisica.**

**PRESIDIO:** quando una grandezza entra in una legge, **non basta dire QUALE grandezza e': va detto
A QUALE TEMPO e' valutata**, e **il codice deve renderlo vero** — non lasciarlo all'ordine delle
chiamate. **E' la ragione per cui gli snapshot esistono (`_phi_t`, `_tw_t`, `_phivel_t`, `_peq_t`):
il difetto non e' che manchino, e' che chi li deve usare a volte non li usa.**

> **⚠ E TUTTE E QUATTRO SONO EMERSE PER CASO, indagando altro.** La scansione sistematica —
> *«quali grandezze sono lette in piu' punti del passo, e con quale stato?»* — **non e' mai stata
> fatta.** E' il lavoro previsto della voce **Z19**, accanto al rigiro dei sigilli storici (**Z11**).

## D.5 — IL PROGRAMMA LUNGO

| # | | |
|---|---|---|
| **S** | **SPECULATIVA — `cs` ACROMATICO e la cosmologia** (Luca, 2026-09-17) | **NON E' UN FRONTE E NON VA TRATTATA COME TALE.** Linea di pensiero registrata perche' contiene **una predizione falsificabile** (`cs` e `r` non dipendono dalla frequenza → **qualunque effetto di propagazione e' uguale in tutte le bande**) e perche' due obiezioni ovvie sono gia' esaminate: la variazione di `alpha` **non si applica** (e' costruita da processi che scalano tutti con `cs`) e il cromatismo **non si applica**. **Il falsificatore APERTO e' uno: la SFOCATURA angolare** — la deviazione viene da `grad(cs)`, un **vettore**, mentre `r` e' uno **scalare**: non e' «troppo piccolo», e' di **rango sbagliato**. **Nessun anello e' misurato; manca il termine di espansione metrica** (`acc = cs^2*lap + src - beta*vd`, **verificato a `:3242`: nessun termine `∝ d`**) | **NESSUN LAVORO PIANIFICATO.** Non ha un criterio di chiusura, quindi **per la regola di §5-quater non e' un fronte**: sta qui, nella sezione a parte, insieme a **L**. **Non aprire ramificazioni da questa voce.** → `doc/SPECULAZIONI_cs_acromatico.md` |
| **L** | **l'azione unica / la lagrangiana** da cui derivare tutto, memorie incluse (Mori-Zwanzig) | E' cio' che renderebbe il modello **falsificabile** invece che soltanto coerente. **Non e' un task: e' la direzione.** Per la regola 3 non e' un fronte — non ha un osservabile che lo chiuda — ed e' qui per questo, in una sezione a parte |

---

# REGOLE DI MANUTENZIONE — **la parte che conta**

1. **Una voce si sposta di sezione solo con un RISCONTRO committato** (numero + file), **mai per
   impressione**.
2. **Chiuse per DIMOSTRAZIONE e chiuse per MISURA non si mescolano MAI.** Le prime sono definitive;
   le seconde portano **sempre** la condizione che le renderebbe da rifare.
3. **Ogni voce ha: stato, il documento/commit che la sostiene, e il criterio che la chiuderebbe.**
   Una voce **senza criterio di chiusura non e' un fronte: e' un desiderio** → sezione a parte
   (oggi: **L**).
4. **Si apre un fronte nuovo solo dichiarando quale osservabile lo chiuderebbe.**
5. **Il registro si aggiorna nello STESSO commit del riscontro** che lo cambia (§5-bis).
6. **ORDINE DI LAVORO:** la catena **D.1 ha precedenza assoluta**; **H in parallelo**, perche' e'
   piccolo e mette in dubbio ogni misura; **tutto il resto non si apre finche' A non e' chiuso.**
7. **Un fronte fermo per piu' di 3 sessioni va o chiuso o dichiarato DORMIENTE**, con la ragione.
   Un fronte aperto e mai toccato e' **rumore nel registro**.

> **E una regola che viene dall'esperienza di oggi:** un numero entra qui **solo se e' nel repo**.
> Due numeri del mandato che ha creato questo file non ci sono (vedi nota su **A2**): non li ho
> scritti. **Il registro non e' il posto dove i numeri nascono.**
