================================================================================================
COLLAUDO DEL PRESIDIO DELL'INDICE -- i DUE versi   (2026-09-26)
================================================================================================

  indice: 794 fra id e alias      esclusi: 49      forme nude AMBIGUE: 0

  sentinelle scelte ORA e verificate ignote: `QX700`, `QX701`

  DEVE PASSARE  un ID noto: `D01`                          -> passa    PASS
  DEVE PASSARE  una forma ESCLUSA: `A-B`                   -> passa    PASS
  DEVE FALLIRE  una sentinella ignota: `QX700`             -> RIFIUTA  PASS
                  ignoti: QX700
  DEVE FALLIRE  una seconda sentinella: `QX701`            -> RIFIUTA  PASS
                  ignoti: QX701

  DEVE FALLIRE  il HOOK VERO su una riga con `QX700`      -> uscita 1, ID segnalato True
                il documento e' tornato identico: True
                esito: PASS

  PUNTO 3, NEI DUE VERSI (sentinella `QX700`):
    DEVE FALLIRE  l'ID nuovo SOLO in `STATO_RUN` -> uscita 1, segnalato True   PASS
    DEVE PASSARE  lo stesso ID **con la RIGA nell'indice** -> uscita 0   PASS
    i due documenti sono tornati identici (sha1): True   PASS

  ESITO: 8/8 PASS -- il presidio IMPEDISCE e non solo avvisa

COSA QUESTO COLLAUDO *NON* DICE:
  - **non dice che l'indice sia completo**: dice che un ID FUORI dall'indice viene
    RIFIUTATO e uno DENTRO passa.
  - il presidio guarda **solo le righe AGGIUNTE** ai documenti vivi e il messaggio: il
    debito vecchio resta visibile nell'indice, e **non blocca ogni commit**. E' una scelta,
    e senza di essa il presidio verrebbe aggirato il primo giorno (`A9`).
